// P9-003 — placeholder. Real coverage lives in
// packages/api/test/agreements/recurrence.test.ts per repo convention
// (vitest only picks up test/**/*.test.ts; src/**/__tests__/ is intentional
// scaffolding so contributors find the spec next to the module).
export {};
