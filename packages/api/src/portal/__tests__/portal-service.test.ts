/**
 * P10-001 — placeholder.
 *
 * The vitest config picks up tests from `test/**` only (see
 * packages/api/vitest.config.ts). Real tests live at
 * `packages/api/test/portal/portal-service.test.ts` and
 * `packages/api/test/portal/portal-routes.test.ts`. This file exists
 * because the dispatch addendum lists this path explicitly so the
 * worktree contract stays satisfied.
 */
export {};
