/**
 * Stripe Terminal availability + collect result types.
 * Platform collect logic lives in useTerminalCollect(.native).
 */
export {
  isTerminalSdkAvailable,
  TERMINAL_UNAVAILABLE_REASON,
  type TerminalCollectResult,
} from './terminalTypes';
