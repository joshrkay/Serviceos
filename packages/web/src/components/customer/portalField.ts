/**
 * Shared baseline for portal/customer kit form controls: just the 44px tap
 * target. Focus/fill colour comes from the kit defaults (ServiceOS brand
 * blue) — the portal is brand-aligned, not tenant-neutral.
 */
export const PORTAL_FIELD = 'min-h-11';
