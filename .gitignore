node_modules/
dist/
*.js.map
*.d.ts

# Ephemeral H5 voice-load report (regenerated on each run; do not commit
# a single run's capture — staging numbers belong in voice-capacity.md).
voice-load-report.json

# Multi-agent dispatch worktrees (created by /dispatch-story; ephemeral)
.claude/worktrees/

# Visual brainstorming companion (mockups persist locally, not in git)
.superpowers/
!jest.config.ts
!**/vite-env.d.ts
.env
.env.*
!.env.example
!.env.qa.example
*.tsbuildinfo
coverage/

# Training corpus pipeline (local venv)
serviceos_training/.venv/
serviceos_training/.pytest_cache/

# Playwright
/playwright-report/
/test-results/
/playwright/.cache/

# Ephemeral test-DB state files (written by e2e/fixtures/setup-test-db.ts and
# seed-journey-fixtures.ts; consumed by teardown-test-db.ts). Never commit.
e2e/fixtures/.test-db-state.json
e2e/fixtures/.journey-fixtures.env

# QA matrix run artifacts (reports folders are kept; transient outputs ignored)
/qa/artifacts/*
!/qa/artifacts/.gitkeep
/qa/reports/*/
!/qa/reports/.gitkeep

# Voice Quality v1 corpus runner — generated JSON report (VQ-009)
packages/api/voice-quality-report.json
# Voice Quality v1 Layer 2 corpus runner — generated reports (VQ2-016)
packages/api/voice-quality-layer2-report.json
packages/api/voice-quality-layer2-vitest-report.json
packages/api/voice-quality-layer2-failures/

# P2-1 Filler engine — pre-rendered PCM clips (generated at deploy time by
# scripts/render-fillers.ts; never committed).
packages/api/src/ai/agents/customer-calling/fillers/*.mp3
packages/api/src/ai/agents/customer-calling/fillers/*.bin
packages/api/src/ai/agents/customer-calling/fillers/*.pcm
