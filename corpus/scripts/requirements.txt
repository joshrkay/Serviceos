# Core HTTP + parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# PDF text extraction
pdfplumber>=0.10.0

# Zstandard decompression (Pushshift Reddit dumps)
zstandard>=0.22.0

# Progress bars
tqdm>=4.66.0

# Apify client (Step 6 — reviews, requires APIFY_API_TOKEN)
apify-client>=1.7.0

# YouTube transcript extraction (must also install yt-dlp CLI)
yt-dlp>=2024.1.0

# Data processing
pandas>=2.0.0

# Analytics
posthog>=3.0.0
python-dotenv>=1.0.0
