# ServiceOS — UI Screen Flow

> Generated by `npm run ui-flow:doc` (`scripts/build-ui-flow-doc.ts`).
> Screenshots captured: **0 / 54**. To (re)capture, see "Capturing screenshots" below.

A map of every UI screen and how users move between them, across the mobile
operator app, the web back office, and the public customer-facing pages.

## Flow map

```mermaid
flowchart LR
  subgraph mobile["Mobile app — operator"]
    direction TB
    mobile_sign_in["Sign in"]
    mobile_home["Home / Today (hub)"]
    mobile_voice["Voice — speak an action"]
    mobile_approvals["Approval inbox"]
    mobile_proposal["Proposal review (approve / edit / reject)"]
    mobile_messages["Messages"]
    mobile_thread["Message thread (text / call)"]
    mobile_customers["Customers"]
    mobile_customer["Customer detail"]
    mobile_jobs["Jobs"]
    mobile_estimates["Estimates"]
    mobile_invoices["Invoices"]
    mobile_schedule["Schedule"]
    mobile_settings["Settings"]
    mobile_sign_in --> mobile_home
    mobile_home --> mobile_voice
    mobile_home --> mobile_approvals
    mobile_home --> mobile_messages
    mobile_home --> mobile_customers
    mobile_home --> mobile_jobs
    mobile_home --> mobile_estimates
    mobile_home --> mobile_invoices
    mobile_home --> mobile_schedule
    mobile_home --> mobile_settings
    mobile_approvals --> mobile_proposal
    mobile_messages --> mobile_thread
    mobile_customers --> mobile_customer
    mobile_customer --> mobile_thread
  end
  subgraph web["Web app — back office"]
    direction TB
    web_login["Login / Signup"]
    web_onboarding["Onboarding"]
    web_home["Home dashboard"]
    web_assistant["AI Assistant"]
    web_inbox["Inbox"]
    web_comms_inbox["Comms inbox"]
    web_jobs_list["Jobs"]
    web_jobs_new["New job"]
    web_jobs_detail["Job detail"]
    web_schedule["Schedule"]
    web_dispatch["Dispatch board"]
    web_customers_list["Customers"]
    web_customers_detail["Customer detail"]
    web_customers_edit["Edit customer"]
    web_appointments_edit["Edit appointment"]
    web_leads_list["Leads"]
    web_leads_new["New lead"]
    web_leads_detail["Lead detail"]
    web_estimates_list["Estimates"]
    web_estimates_new["New estimate"]
    web_invoices_list["Invoices"]
    web_invoices_new["New invoice"]
    web_contracts_list["Contracts"]
    web_contracts_detail["Contract detail"]
    web_interactions["Interactions / Dispatch log"]
    web_reports_money["Reports — Money"]
    web_reports_revenue["Reports — Revenue by source"]
    web_digest["Daily digest"]
    web_technician_day["Technician day"]
    web_settings["Settings"]
    web_settings_templates["Settings — Templates"]
    web_settings_price_book["Settings — Price book"]
    web_settings_feedback["Settings — Feedback"]
    web_settings_language["Settings — Language"]
    web_login --> web_onboarding
    web_login --> web_home
    web_onboarding --> web_home
    web_home --> web_assistant
    web_home --> web_inbox
    web_home --> web_comms_inbox
    web_home --> web_jobs_list
    web_home --> web_schedule
    web_home --> web_dispatch
    web_home --> web_customers_list
    web_home --> web_leads_list
    web_home --> web_estimates_list
    web_home --> web_invoices_list
    web_home --> web_contracts_list
    web_home --> web_interactions
    web_home --> web_reports_money
    web_home --> web_reports_revenue
    web_home --> web_digest
    web_home --> web_technician_day
    web_home --> web_settings
    web_jobs_list --> web_jobs_new
    web_jobs_list --> web_jobs_detail
    web_customers_list --> web_customers_detail
    web_customers_detail --> web_customers_edit
    web_customers_detail --> web_appointments_edit
    web_leads_list --> web_leads_new
    web_leads_list --> web_leads_detail
    web_estimates_list --> web_estimates_new
    web_invoices_list --> web_invoices_new
    web_contracts_list --> web_contracts_detail
    web_settings --> web_settings_templates
    web_settings --> web_settings_price_book
    web_settings --> web_settings_feedback
    web_settings --> web_settings_language
  end
  subgraph public["Public — customer-facing (opened from SMS / email / website links)"]
    direction TB
    public_estimate_approval["Estimate approval (/e/:id)"]
    public_invoice_payment["Invoice payment (/pay/:id)"]
    public_intake["Intake form (/intake)"]
    public_booking["Booking (/book)"]
    public_feedback["Feedback (/public/feedback/:token)"]
    public_portal["Customer portal (/portal/:token)"]
    public_estimate_approval --> public_invoice_payment
  end
  web_estimates_new -.->|"send link"| public_estimate_approval
  web_invoices_new -.->|"send link"| public_invoice_payment
  mobile_proposal -.->|"approve & send"| public_estimate_approval
```

## Mobile app — operator

### Sign in

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/sign-in.png`)._

- → Home / Today (hub)

### Home / Today (hub)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/home.png`)._

- → Voice — speak an action, Approval inbox, Messages, Customers, Jobs, Estimates, Invoices, Schedule, Settings

### Voice — speak an action

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/voice.png`)._


### Approval inbox

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/approvals.png`)._

- → Proposal review (approve / edit / reject)

### Proposal review (approve / edit / reject)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/proposal.png`)._

- Also opened from a push notification

### Messages

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/messages.png`)._

- → Message thread (text / call)

### Message thread (text / call)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/thread.png`)._


### Customers

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/customers.png`)._

- → Customer detail

### Customer detail

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/customer.png`)._

- → Message thread (text / call)

### Jobs

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/jobs.png`)._


### Estimates

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/estimates.png`)._


### Invoices

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/invoices.png`)._


### Schedule

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/schedule.png`)._


### Settings

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/mobile/settings.png`)._


## Web app — back office

### Login / Signup

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/login.png`)._

- → Onboarding, Home dashboard

### Onboarding

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/onboarding.png`)._

- → Home dashboard

### Home dashboard

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/home.png`)._

- → AI Assistant, Inbox, Comms inbox, Jobs, Schedule, Dispatch board, Customers, Leads, Estimates, Invoices, Contracts, Interactions / Dispatch log, Reports — Money, Reports — Revenue by source, Daily digest, Technician day, Settings

### AI Assistant

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/assistant.png`)._


### Inbox

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/inbox.png`)._


### Comms inbox

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/comms-inbox.png`)._


### Jobs

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/jobs-list.png`)._

- → New job, Job detail

### New job

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/jobs-new.png`)._


### Job detail

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/jobs-detail.png`)._


### Schedule

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/schedule.png`)._


### Dispatch board

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/dispatch.png`)._


### Customers

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/customers-list.png`)._

- → Customer detail

### Customer detail

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/customers-detail.png`)._

- → Edit customer, Edit appointment

### Edit customer

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/customers-edit.png`)._


### Edit appointment

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/appointments-edit.png`)._


### Leads

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/leads-list.png`)._

- → New lead, Lead detail

### New lead

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/leads-new.png`)._


### Lead detail

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/leads-detail.png`)._


### Estimates

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/estimates-list.png`)._

- → New estimate

### New estimate

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/estimates-new.png`)._


### Invoices

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/invoices-list.png`)._

- → New invoice

### New invoice

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/invoices-new.png`)._


### Contracts

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/contracts-list.png`)._

- → Contract detail

### Contract detail

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/contracts-detail.png`)._


### Interactions / Dispatch log

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/interactions.png`)._


### Reports — Money

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/reports-money.png`)._


### Reports — Revenue by source

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/reports-revenue.png`)._


### Daily digest

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/digest.png`)._


### Technician day

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/technician-day.png`)._


### Settings

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/settings.png`)._

- → Settings — Templates, Settings — Price book, Settings — Feedback, Settings — Language

### Settings — Templates

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/settings-templates.png`)._


### Settings — Price book

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/settings-price-book.png`)._


### Settings — Feedback

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/settings-feedback.png`)._


### Settings — Language

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/settings-language.png`)._


## Public — customer-facing (opened from SMS / email / website links)

### Estimate approval (/e/:id)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/estimate-approval.png`)._

- Link sent from an estimate
- → Invoice payment (/pay/:id)

### Invoice payment (/pay/:id)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/invoice-payment.png`)._

- Link sent from an invoice

### Intake form (/intake)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/intake.png`)._

- Website / SMS link

### Booking (/book)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/booking.png`)._

- Website / SMS link

### Feedback (/public/feedback/:token)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/feedback.png`)._

- Sent after a completed job

### Customer portal (/portal/:token)

> _Screenshot not captured yet — run `npm run ui-flow:capture` (`captures/web/portal.png`)._

- Customer self-serve link

## Capturing screenshots

Screens render only against a running app with auth + data, so capture runs
where that exists (CI, a dev machine, or a deployed env), not in a bare
container. Then re-run the assembler to embed the images.

```bash
# Web (Vite SPA) — local stack OR a deployed URL that has Clerk wired:
UI_FLOW=1 VITE_CLERK_PUBLISHABLE_KEY=pk_test_... E2E_CLERK_SECRET_KEY=sk_test_... \
  npm run ui-flow:capture
#   …or against a deployed env (no local stack):
UI_FLOW=1 E2E_BASE_URL=https://your-web-env.example.com npm run ui-flow:capture

# Mobile (Expo operator app) — export to web, serve it, then capture:
cd packages/mobile && npm run export:web && cd ../..
npx serve packages/mobile/.e2e-web -l 8081 &
UI_FLOW=1 E2E_MOBILE_URL=http://localhost:8081 \
  npx playwright test e2e/ui-flow-capture-mobile.spec.ts --project=ui-flow

# Rebuild this doc with the new screenshots embedded:
npm run ui-flow:doc
```

Without Clerk creds the web tour runs anonymously (authed routes redirect to
login); set `E2E_CLERK_*` for the real authenticated screens.
