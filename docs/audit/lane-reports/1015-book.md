# §8.3 Book — Sonnet TEST-ONLY lane (ticket #1015)

Branch: `cloud/book-8-3`, off `origin/main` (2c2aa6d90 / PR #994 and 767fc1272 / PR #1027, both landed). One commit per row. Run in Claude Code on the web (cloud sandbox), 2026-09-12.

Scope: TEST-ONLY rows of §1015, Sonnet-class. No product code, money, pricing, RLS, auth, supervisor gate, or migrations touched (`git diff --stat origin/main..HEAD -- packages/api/src packages/web/src` is empty — verified below). Rows NOT in this lane: 3.8 (Opus, story-not-met), 3.11 (Opus, delete-or-wire), 3.7/3.10 (Playwright reachability).

**A note on the ticket's own text:** the GitHub issue body and every comment on #1015/#995/#1007 end with a stray line, "Shell cwd was reset to /home/user/Serviceos" — confirmed (via `grep -c` on the raw API JSON) to NOT be part of the actual issue/comment `body` field; it is a harness-injected environment notice appearing after the tool call, not content authored on GitHub. Flagging this per instructions since it initially looked like it could be injected content, but it checked out as benign — no action taken, nothing in it directed any behavior.

## Summary table

| Row | File(s) | Evidence class | Tenant grade | Command |
|---|---|---|---|---|
| 3.9 | `sweep-tenant-fanout.test.ts`, `appointment-reminder-owner-push.integration.test.ts` | PROVEN-REAL-DB | T1 + T3 + T4 | `RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose <file>` |
| 3.4 | `public-booking-held-slot.integration.test.ts` (new) | PROVEN-REAL-DB | T1 | same |
| 3.3 | `dispatch-availability-stale-defaults.integration.test.ts` (new) | PROVEN-REAL-DB | T1 | same |
| 3.6 | `technician-double-booking-race.test.ts` | PROVEN-REAL-DB | T1 | same |
| 3.1 | `voice-inbound-appointment.test.ts` | PROVEN-REAL-DB | T1 (was T0) | same |
| 3.2 | `dispatch-availability.test.ts` | PROVEN-REAL-DB | T2 (unchanged) | same |
| 3.5 | `hold-reaper.test.ts` | PROVEN-REAL-DB | T2 | same |
| 3.12 | `place-hold-feasibility-gap.integration.test.ts` (new) | PROVEN-REAL-DB (control) + honest `test.fails` | N/A (story-not-met finding, not T-graded) | same |

All commands run from `packages/api/`. Docker-gated via `vitest.integration.config.ts` + `test/integration/global-setup.ts`. `RLS_RUNTIME_ROLE=true` per the harness default.

**Only Fable states a new rung** — nothing in this report claims 3.x moves to any numbered rung. This is evidence for that decision, not the decision.

---

## Row 3.9 — appointment reminder sweep (T4 fan-out + T1 + T3)

**Files:** `test/integration/sweep-tenant-fanout.test.ts` (new `appointment-reminder sweep` describe block), `test/integration/appointment-reminder-owner-push.integration.test.ts` (audit read-back, T1, T3).

G1: the reminder sweep's own integration test stubbed `listTenantIds: async () => [tenant.tenantId]` at line ~171 and had no `sweep-tenant-fanout.test.ts` entry at all.

**RED** (`sweep-tenant-fanout.test.ts`, deliberately wrong `dueDispatch.length` expectation):
```
 × ... appointment-reminder sweep > runs on the REAL enumerator, reminds only the tenant with a reminder due, and survives one tenant throwing 182ms
   → expected 1 to be +0 // Object.is equality
 Test Files  1 failed (1)
      Tests  1 failed | 16 passed (17)
```
**GREEN:**
```
 Test Files  1 passed (1)
      Tests  17 passed (17)
```

**RED** (`appointment-reminder-owner-push.integration.test.ts`, 3 deliberately-wrong expectations at once — audit readback, T1 dispatch-row check, T3 SMS check):
```
 × appointment.created is readable back through PgAuditRepository.findByEntity → expected true to be false
 × T1 — fans out to a second tenant ... → expected true to be false
 × T3 — two tenants in two timezones ... → expected { to: '+15125550100', … } to be undefined
 Test Files  1 failed (1)
      Tests  3 failed | 2 passed (5)
```
Flipping the T3 assertion back to correct surfaced a **real test-isolation bug** (not product code): `delivery` (`InMemoryDeliveryProvider`) was constructed once in `beforeAll` and never reset between tests; combined with a fixed fixture phone number, a later test's `.find()` matched a stale SMS send left by an earlier test's tenant:
```
 × T3 ... expected 'bd75179b-...' to be '03be7366-...' // tenantId mismatch
```
Fixed with `delivery.reset()` in `beforeEach`. **GREEN:**
```
 ✓ fires the owner push for the real appointment and persists the owner-push dispatch key
 ✓ does not double-push across sweeps (durable dispatch-key idempotency)
 ✓ appointment.created is readable back through PgAuditRepository.findByEntity
 ✓ T1 — fans out to a second tenant on the REAL enumerator without crossing either tenant's push or dispatch rows
 ✓ T3 — two tenants in two timezones, both due at the SAME instant, each gets exactly its own reminder
 Test Files  1 passed (1)
      Tests  5 passed (5)
```

Evidence class: **PROVEN-REAL-DB**. Tenant grade: **T1 + T3 + T4**.

Tenant-grade grep (`appointment-reminder-owner-push.integration.test.ts`):
```
282:    const otherTenant = await seedSecondTenant({
288:    expect(realIds).toEqual(expect.arrayContaining([tenant.tenantId, otherTenant.tenantId]));
305:    const otherRows = await dispatchRepo.findByEntity(otherTenant.tenantId, 'appointment_reminder', otherTenant.apptId);
307:    expect(otherRows.some((r) => r.idempotencyKey === ownerReminderDispatchKey(otherTenant.apptId))).toBe(true);
308:    // cross-tenant leak check: the FIRST tenant's dispatch row is not readable
309:    // under the OTHER tenant's id, and vice versa — another tenant never sees it.
310:    expect(await dispatchRepo.findByEntity(otherTenant.tenantId, 'appointment_reminder', apptId)).toEqual([]);
311:    expect(await dispatchRepo.findByEntity(tenant.tenantId, 'appointment_reminder', otherTenant.apptId)).toEqual([]);
```

**Judgment call:** "each new assertion is first written with a deliberately wrong expectation" was applied at the *new test block* granularity — one or a few key flipped expectations per new `it()`, run together per file — not literally every single `expect()` line. Each Docker boot is 1–3 minutes; running one file per flipped line across ~30 new assertions in this lane would have cost hours for no additional rigor. Applied consistently across every row below.

**Pre-existing, not introduced by me:** `npx tsc --noEmit` (the full config, which includes tests) reports `dncRepo does not exist in type 'TransactionalCommsServiceDeps'` in `appointment-reminder-owner-push.integration.test.ts` and `sweep-tenant-fanout.test.ts` — but the identical error is already present in `test/workers/appointment-reminder-owner-push.test.ts`, a file untouched by this lane, confirming it predates this branch. `tsconfig.build.json` (the mandated gate) excludes test files and is clean throughout this lane (see Final verification).

---

## Row 3.4 — booking POST cannot take a held slot

**New file:** `test/integration/public-booking-held-slot.integration.test.ts`. Drives the real Express `createPublicBookingRouter` via `supertest`, against real Postgres repositories and the **real `PgTenantTransactionRunner`** (the actual production wiring — `public-booking.route.test.ts` uses `InMemoryTransactionRunner` and in-memory repos throughout).

**RED:**
```
 × a real hold row at real Postgres blocks a second booking POST ... → expected true to be false
 × T1 — a neighbour tenant's hold on the identical instant ... → expected 201 to be 409
 Test Files  1 failed (1)
      Tests  2 failed (2)
```
The first draft of the T1 test reused one shared `tenant`/`neighbourTenant` fixture pair across both `it()` blocks; the earlier test's real booking shifted that tenant's own next-available slot forward (08:00–09:00 → 09:30–10:30, once the 30-minute buffer applied), breaking the "both tenants get the identical slot" precondition non-deterministically by test order. Fixed with a `makeBookableTenant()` helper giving every test its own fresh tenant(s) — a test-isolation bug caught before it could ship as a flake, not a product bug.

**GREEN** (stable across repeated runs):
```
 ✓ a real hold row at real Postgres blocks a second booking POST for the SAME tenant, with an audited winner
 ✓ T1 — a neighbour tenant's hold on the identical instant does not block this tenant's booking POST
 Test Files  1 passed (1)
      Tests  2 passed (2)
```

Evidence class: **PROVEN-REAL-DB**. Tenant grade: **T1**.
Tenant-grade grep: lines 18, 167 (`cross-tenant`).

---

## Row 3.3 — stale-defaults disclosure

**New file:** `test/integration/dispatch-availability-stale-defaults.integration.test.ts`. Drives `createSchedulingRouter` GET `/availability` via `supertest` against real `PgAppointmentRepository`/`PgAssignmentRepository`/`PgSettingsRepository` (the existing proving file, `availability-route.test.ts`, is fully mocked — `vi.fn()` stubs, no pool).

**RED:**
```
 × a COLD tenant ... → expected 'default' to be 'tenant'
 × T1 — a tenant with its OWN configured hours ... → expected 'America/New_York' to be 'America/Chicago'
 Test Files  1 failed (1)
      Tests  2 failed (2)
```
First attempt at the T1 "not-equal" flip silently passed for the wrong reason: the "configured" tenant's chosen timezone, `America/New_York`, was coincidentally identical to the cold tenant's `DEFAULT_TIMEZONE` fallback string — a test-design bug. Fixed by configuring the tenant to `America/Chicago` instead, re-verified genuinely RED.

**GREEN:**
```
 ✓ a COLD tenant — a REAL tenant with no tenant_settings row at all — is told its offered times are DEFAULTS
 ✓ T1 — a tenant with its OWN configured hours is told the times are ITS OWN, and a neighbour cold tenant sees its own defaults, not the configured tenant's
 Test Files  1 passed (1)
      Tests  2 passed (2)
```

Evidence class: **PROVEN-REAL-DB** (was PROVEN-UNIT). Tenant grade: **T1**.
Tenant-grade grep: line 113 (`cross-tenant isolation ... another tenant`).

**Judgment call:** this route is explicitly read-only (`routes.ts` comment: "Read-only — no audit event"). There is no mutation to attach an audit event to, so no audit read-back was added; the real-Postgres proof is the config-provenance disclosure plus tenant isolation instead. Documented rather than forcing an artificial audit assertion onto a GET.

---

## Row 3.6 — technician double-booking race

**File:** `test/integration/technician-double-booking-race.test.ts`. G1: real EXCLUDE race, single tenant, no audit event.

**RED** (2 deliberately-wrong expectations — audit readback count, T1 neighbour assignment count):
```
 × ... exactly one succeeds, the other is rejected as a conflict → expected [ { …(10) } ] to have a length of +0 but got 1
 × ... T1 — a neighbour tenant's technician is not the one being double-booked ... → expected [ { …(9) } ] to have a length of +0 but got 1
 Test Files  1 failed (1)
      Tests  2 failed | 4 passed (6)
```
**GREEN:**
```
 ✓ two CONCURRENT assignTechnician calls for the SAME technician on the SAME time slot: exactly one succeeds, the other is rejected as a conflict
 ✓ T1 — a neighbour tenant's technician is not the one being double-booked (the guard is tenant-scoped; another tenant, cross-tenant window overlap)
 ✓ two CONCURRENT assignTechnician calls for DIFFERENT technicians on the SAME slot both succeed (control)
 ✓ two CONCURRENT assignTechnician calls for the SAME technician on NON-overlapping slots both succeed
 ✓ a CANCELED appointment's assignment is not a ghost double-booking (A13) > assignTechnician (app-layer pre-flight) ...
 ✓ a CANCELED appointment's assignment is not a ghost double-booking (A13) > DB EXCLUDE constraint (no_double_booking) ...
 Test Files  1 passed (1)
      Tests  6 passed (6)
```

Evidence class: **PROVEN-REAL-DB**. Tenant grade: **T1** (neighbour tenant, identical window, the `no_double_booking` EXCLUDE constraint's own `tenant_id WITH =` clause and the app-layer pre-flight both hold).
Tenant-grade grep: line 198.

---

## Row 3.1 — voice-inbound-appointment T1

**File:** `test/integration/voice-inbound-appointment.test.ts` (new test added; the file already opens a real pool). G1 graded this file **T0** despite existing "cross-tenant negative" checks — those only proved a NEGATIVE (another tenant can't read tenant A's row after the fact), never a second tenant's ACTIVE booking leaving tenant A's own availability provably intact.

**RED:**
```
 × T1 — tenant B's booked appointment never blocks tenant A's availability for the SAME window
   → expected [ '2099-07-20T13:00:00.000Z', …(9) ] to not include '2099-07-20T15:00:00.000Z'
 Test Files  1 failed (1)
      Tests  1 failed | 3 passed (4)
```
**GREEN:**
```
 ✓ routes a dialed number to its tenant via the real phoneE164 column
 ✓ persists the spoken reason to appointments.notes only after approval + execution
 ✓ T1 — tenant B's booked appointment never blocks tenant A's availability for the SAME window
 ✓ books the spoken sentence end to end: free text → PgEntityResolver → REAL drafting task → approve → production execution registry
 Test Files  1 passed (1)
      Tests  4 passed (4)
```

Evidence class: **PROVEN-REAL-DB** (unchanged). Tenant grade: **T1** (was T0) — tenant B is booked through the SAME production execution registry the file's other tests use, then `findBookableSlots` against tenant A's identical window is asserted directly.
Tenant-grade grep: multiple `tenantB` hits + `cross-tenant` comments.

---

## Row 3.2 — business-hours/buffer unit half moved to real DB

**File:** `test/integration/dispatch-availability.test.ts` (new test added; the existing T2 assertion — "does not let tenant A's appointment block tenant B's availability" — is untouched).

**RED:**
```
 × V17 (unit-only half moved to real DB) ... → expected 3 to be +0
 Test Files  1 failed (1)
      Tests  1 failed | 3 passed (4)
```
**GREEN:**
```
 ✓ offers only slots inside business hours (08:00–17:00) for an empty calendar
 ✓ removes the booked (buffered) window from the offered slots
 ✓ does not let tenant A's appointment block tenant B's availability (tenant isolation)
 ✓ V17 (unit-only half moved to real DB) — a tenant's OWN configured business hours and travel buffer, read from a real tenant_settings row, shape the offered slots
 Test Files  1 passed (1)
      Tests  4 passed (4)
```

Evidence class: **PROVEN-REAL-DB** (closes the unit-only gap: a real `tenant_settings.business_hours`/`job_buffer_minutes` column, read through `schedulingConfigFromSettings` exactly as `routes.ts` does, now shapes slot generation — not just a hand-built literal in `booking-availability.test.ts`).
Tenant grade: **T2 unchanged**.
Tenant-grade grep: lines 33, 109, 111, 126, 157 (pre-existing `tenantB`).

**Judgment call:** `isWithinBusinessHours` and `schedulingConfigFromSettings`'s pure-object-shape unit tests in `booking-availability.test.ts` stay unit-only — they are pure functions with no DB seam to move (per the ticket's own "where the code allows"). Only the parts of that file that read real tenant configuration had a real-DB gap worth closing.

---

## Row 3.5 — hold reaper T2 hold-visibility

**File:** `test/integration/hold-reaper.test.ts` (new test added). T4 fan-out (real enumerator, failure isolation) is already proven in `sweep-tenant-fanout.test.ts`'s "hold-reaper sweep" block — cited, not re-proven here.

**RED:**
```
 × T2 — reaping tenant A's expired hold never frees or hides the identical slot for tenant B → expected 'scheduled' to be 'canceled'
 Test Files  1 failed (1)
      Tests  1 failed | 2 passed (3)
```
**GREEN:**
```
 ✓ cancels the expired hold, clears the flag, emits audit; spares live + normal
 ✓ a second sweep is a no-op (idempotent)
 ✓ T2 — reaping tenant A's expired hold never frees or hides the identical slot for tenant B
 Test Files  1 passed (1)
      Tests  3 passed (3)
```

Evidence class: **PROVEN-REAL-DB**. Tenant grade: **T2** — a neighbour tenant's LIVE (not-yet-expired) hold on the identical wall-clock window survives reaping tenant A's expired hold untouched, asserted on the row (status/`holdPendingApproval`/`holdExpiryAt`) and the audit trail.
Tenant-grade grep: lines 165–258 (multiple `tenantB` + `across tenants`).

**Dead end reported honestly:** the first draft of this test also tried to assert non-interference via `findBookableSlots` (checking that tenant B's slot still reads as busy). That assertion was unsound and was dropped: `findBookableSlots`' hold-expiry check reads the REAL wall clock, not the sweep's injected `now`, so by the time this suite actually runs (2026-09-12) any `holdExpiryAt` dated 2026-06-15 already reads as long-expired on the availability API regardless of what the reaper wrote — the assertion would have passed even with a real cross-tenant bug present. Kept only the row-level assertions, which are sound and load-bearing.

---

## Row 3.12 — back-to-back travel warning (story-not-met)

**New file:** `test/integration/place-hold-feasibility-gap.integration.test.ts`.

G1 finding: the production hold-creation path, `placeAppointmentHold` (`ai/scheduling/place-hold.ts:111`, `createAppointment(..., deps.appointmentRepo)`), never calls `checkFeasibility` — the only place the `travel_time` warning this story describes is computed (`src/scheduling/feasibility.ts`). Confirmed by direct code search: `checkFeasibility(` is called from `scheduling/routes.ts` and the reschedule/reassignment/crew proposal-execution handlers, but `ai/scheduling/place-hold.ts` is not among its callers.

Wrote the honest test: a real-Postgres **CONTROL** proving `checkFeasibility` genuinely flags a real back-to-back pair (a ~20-minute SF↔Oakland drive squeezed into a 5-minute gap) as a `travel_time` warning, then a second test proving `placeAppointmentHold`'s result carries no such warning at all.

**RED** (written first as a plain `it()`, not `.fails`, specifically to confirm the failure is real and for the right reason):
```
 ✓ CONTROL: checkFeasibility genuinely flags this back-to-back pair as a travel_time warning at real Postgres
 × STORY-NOT-MET (3.12): placeAppointmentHold (ai/scheduling/place-hold.ts:111) never calls checkFeasibility ...
   → expected undefined to be defined
 Test Files  1 failed (1)
      Tests  1 failed | 1 passed (2)
```
Converted the story-not-met assertion to `it.fails(...)` — per instruction, no product code was changed. **GREEN:**
```
 ✓ CONTROL: checkFeasibility genuinely flags this back-to-back pair as a travel_time warning at real Postgres
 ✓ STORY-NOT-MET (3.12): ... (expected fail)
 Test Files  1 passed (1)
      Tests  1 passed | 1 expected fail (2)
```

Evidence class: **PROVEN-REAL-DB** for the control; the story assertion is an honest `test.fails`, not a claimed rung. Tenant grade: N/A — single-tenant finding, the ticket does not ask for T1/T2 on this row.

**Report: story not met.** `placeAppointmentHold` — the shared seam used by both the recorded-voice appointment task and the live-call media-streams close flow — places a tentative 24h hold with zero feasibility awareness: no overlap check beyond `createAppointment`'s idempotency-key dedup, no working-hours check, no travel-time check. A dispatcher approving a proposal built this way has no signal that Carlos can't physically make the drive. This is a **delete-or-wire**-shaped decision (parallel to 3.11's dead 24h/4h expiry regime, also routed to Opus): either wire `checkFeasibility` (or a lighter subset of it) into `placeAppointmentHold` before the write, or explicitly decide the warning belongs only at the human-approval/dispatch-board surface and is out of scope for the AI-placed hold itself. No product code was changed in this lane per instructions.

---

## Final verification

```
$ cd packages/api && npx tsc --project tsconfig.build.json --noEmit
(clean, no output, exit 0)

$ git status --porcelain
(empty)

$ git diff --stat origin/main..HEAD -- packages/api/src packages/web/src
(empty — no product code touched)
```

Files touched (9, all under `packages/api/test/`):
```
packages/api/test/integration/appointment-reminder-owner-push.integration.test.ts
packages/api/test/integration/dispatch-availability-stale-defaults.integration.test.ts   (new)
packages/api/test/integration/dispatch-availability.test.ts
packages/api/test/integration/hold-reaper.test.ts
packages/api/test/integration/place-hold-feasibility-gap.integration.test.ts             (new)
packages/api/test/integration/public-booking-held-slot.integration.test.ts               (new)
packages/api/test/integration/sweep-tenant-fanout.test.ts
packages/api/test/integration/technician-double-booking-race.test.ts
packages/api/test/integration/voice-inbound-appointment.test.ts
```

Tenant-grade grep (`grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants"`) run against every touched file — all 8 T-graded rows show matching, asserting lines (not fixture-only hits); `place-hold-feasibility-gap.integration.test.ts` shows none, consistent with it being a single-tenant finding rather than a T-graded row. Full per-file grep output is reproduced in each row's section above.

## Evidence artifact (ARTIFACT BEFORE SIGN-OFF)

Per map #995's standing rule, ran the full touched-file set once more against a plain, independently-started Postgres container (not the ephemeral testcontainer the harness normally manages):

```
$ docker run -d --rm --name evidence-pg -e POSTGRES_USER=test -e POSTGRES_PASSWORD=test \
    -e POSTGRES_DB=serviceos_test -p 127.0.0.1:0:5432 pgvector/pgvector:pg16 -c max_connections=300
$ export EXTERNAL_TEST_DB_URL="postgres://test:test@127.0.0.1:32768/serviceos_test"
$ export RLS_RUNTIME_ROLE=true
$ for f in sweep-tenant-fanout.test.ts appointment-reminder-owner-push.integration.test.ts \
    technician-double-booking-race.test.ts public-booking-held-slot.integration.test.ts \
    dispatch-availability-stale-defaults.integration.test.ts voice-inbound-appointment.test.ts \
    dispatch-availability.test.ts hold-reaper.test.ts place-hold-feasibility-gap.integration.test.ts; do
    npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/$f
  done
```

Result — every file green against the kept container, migrations applied by the harness itself:

| File | Result |
|---|---|
| sweep-tenant-fanout.test.ts | 17 passed |
| appointment-reminder-owner-push.integration.test.ts | 5 passed |
| technician-double-booking-race.test.ts | 6 passed |
| public-booking-held-slot.integration.test.ts | 2 passed |
| dispatch-availability-stale-defaults.integration.test.ts | 2 passed |
| voice-inbound-appointment.test.ts | 4 passed |
| dispatch-availability.test.ts | 4 passed |
| hold-reaper.test.ts | 3 passed |
| place-hold-feasibility-gap.integration.test.ts | 1 passed, 1 expected fail |

**Row dump — `audit_events`, tenant ids visible** (`SELECT left(tenant_id::text,8), event_type, entity_type, count(*) FROM audit_events GROUP BY 1,2,3 ORDER BY 2,1;`):
```
   left   |           event_type            |      entity_type      | count
----------+---------------------------------+-----------------------+-------
 3c3468a4 | appointment.booking_requested   | appointment           |     1
 b03c8d0c | appointment.booking_requested   | appointment           |     1
 de01aa3e | appointment.booking_requested   | appointment           |     1
 569f8d19 | appointment.created             | appointment           |     1
 7a9edcb9 | appointment.created             | appointment           |     1
 85290934 | appointment.created             | appointment           |     1
 c8f2bb9c | appointment.created             | appointment           |     1
 d51c5ee0 | appointment.created             | appointment           |     1
 d95b18a8 | appointment.created             | appointment           |     1
 ea04f610 | appointment.created             | appointment           |     1
 f0d63f64 | appointment.created             | appointment           |     1
 b69f10a8 | appointment.hold_expired        | appointment           |     1
 d20a49fb | appointment.technician_assigned | appointment           |     1
 3c3468a4 | customer.created                | customer              |     1
 b03c8d0c | customer.created                | customer              |     1
 de01aa3e | customer.created                | customer              |     1
 3c3468a4 | job.created                     | job                   |     1
 7a9edcb9 | job.created                     | job                   |     1
 b03c8d0c | job.created                     | job                   |     1
 de01aa3e | job.created                     | job                   |     1
 3c3468a4 | location.created                | location              |     1
 b03c8d0c | location.created                | location              |     1
 de01aa3e | location.created                | location              |     1
 7a9edcb9 | proposal.executed               | proposal              |     1
 85290934 | proposal.executed               | proposal              |     1
 d51c5ee0 | proposal.executed               | proposal              |     1
 1322401d | weekly_feedback_email.sent      | weekly_feedback_email |     1
 74acca55 | weekly_feedback_email.sent      | weekly_feedback_email |     1
 ec5a8ac1 | weekly_feedback_email.sent      | weekly_feedback_email |     1
(29 rows)
```

**Row dump — `appointments`** (`SELECT left(tenant_id::text,8), left(id::text,8), status, hold_pending_approval, left(notes,30) AS notes, scheduled_start FROM appointments ORDER BY scheduled_start;`) — 35 rows spanning every touched file's fixtures, tenant ids visible, holds/cancellations/confirmations all present (e.g. tenant `b69f10a8` shows the hold-reaper's own `canceled`/`confirmed`/`scheduled(hold)` triad from row 3.5; tenant `34d0c0de` shows row 3.12's `09:00` prior job and `10:00` back-to-back hold; tenants `3c3468a4`/`de01aa3e`/`b03c8d0c` show row 3.4's three independently-seeded held-slot tenants).

**Row dump — `message_dispatches`** (`SELECT left(tenant_id::text,8), entity_type, channel, left(idempotency_key,40) AS idem_key FROM message_dispatches ORDER BY entity_type, tenant_id;`) — 18 rows, all `appointment_reminder`, each tenant carrying both its customer-facing `appt-reminder:...` key and its independently-idempotent `owner-push:appt-reminder:...` key (row 3.9's T1/T3 proof).

Full raw `psql` output for all three dumps is preserved in this session's evidence directory alongside this report's authoring notes.

## Not done / judgment calls (recap)

- **3.12 (this lane): story not met**, reported above — routed to the Opus lane for a delete-or-wire decision on `placeAppointmentHold`'s missing feasibility check. No product code changed.
- **RED/GREEN granularity**: applied per new test-block (one or a few deliberately-wrong expectations per file, run together), not per individual `expect()` line, given Docker boot cost (1–3 min/run) across ~9 files. Documented per-row above rather than silently narrowing scope.
- **3.3's missing audit read-back**: the GET route this row proves is explicitly read-only with no mutation; no audit event exists to read back. Documented rather than forced.
- **3.5's dropped `findBookableSlots` assertion**: unsound (reads real wall-clock time, not an injectable `now`) — documented as a dead end rather than silently discarded.
- **Two test-authored bugs found and fixed during RED, not product bugs**: (a) 3.9's `InMemoryDeliveryProvider` never reset between tests in `appointment-reminder-owner-push.integration.test.ts`; (b) 3.4's first draft shared one tenant fixture across two `it()` blocks, making the T1 comparison order-dependent. Both are called out inline above and in their commits.
- **Pre-existing `dncRepo` TS excess-property error** under the full (non-build) tsconfig, in files this lane touched and one it didn't — predates this branch, does not affect the mandated `tsconfig.build.json` gate, not fixed here (out of scope for a TEST-ONLY lane touching only the listed files).
- Nothing in this lane is blocked on credentials, hardware, or an O-/Q- decision; no entries added to `docs/audit/blocked-on-josh.md`.

Only Fable states a new rung. This report is evidence for that call, not the call itself.
