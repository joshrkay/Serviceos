# G1 §8.2/8.3 Docker-integration lane — raw execution log

Worktree: `/Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane` (branch `lane/rung-map-env` @ 2c2aa6d90 = origin/main)
Invocation base (cwd `packages/api`):
`DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose <path>`

Disk checks (`df -h / | tail -1`):
- after run 3: `/dev/disk3s1s1   228Gi    12Gi   2.8Gi    81%    455k   29M    2%   /`
- after run 6: `/dev/disk3s1s1   228Gi    12Gi   2.8Gi    81%    455k   29M    2%   /`
- after run 9: `/dev/disk3s1s1   228Gi    12Gi   2.8Gi    81%    455k   29M    2%   /`
- after run 12: `/dev/disk3s1s1   228Gi    12Gi   2.8Gi    81%    455k   29M    2%   /`
- final (after run 13): `/dev/disk3s1s1   228Gi    12Gi   2.8Gi    81%    455k   29M    2%   /`
Free space held steady at 2.8Gi throughout — never approached the 600MiB stop threshold. No retries were needed (no container-start failures).

Pre-check for 2.1's Confirm cell: `grep -n '"routes a dialed number"'` (exact quoted substring as a standalone match) → **no exact match**. But `grep -n "routes a dialed" voice-inbound-appointment.test.ts` → line 232: `it('routes a dialed number to its tenant via the real phoneE164 column', ...)`. The vitest `-t "routes a dialed number"` filter does substring matching, so it **would** match this one test (title is longer than the quoted fragment, but contains it verbatim). Full `it(`/`test(` title list in the file (3 total, lines 232/240/370) confirmed by grep.

Row 2.7 file-discovery grep: `ls test/integration | grep -i -E "dropped|recovery"` → only `dropped-call-worker.test.ts` matched (no "recovery"-named file existed independently).
Row 3.9 file-discovery grep: `ls test/integration | grep -i reminder` → `appointment-reminder-owner-push.integration.test.ts`, `payment-reminder-dedup.test.ts`, `setup-reminder-sweep.test.ts`. Per instruction, ran only the appointment-reminder owner-push file.

---

## 1. `test/integration/voice-inbound-appointment.test.ts` (rows 2.1, 3.1, 3.10 — run once)

Command:
```
cd .../packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/voice-inbound-appointment.test.ts
```
Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:35:37
   Duration  4.70s (transform 1.41s, setup 0ms, import 1.97s, tests 127ms, environment 0ms)
```
✓ lines (3 total, all shown):
```
 ✓ test/integration/voice-inbound-appointment.test.ts > Integration — inbound voice appointment-setting (real Postgres) > routes a dialed number to its tenant via the real phoneE164 column 4ms
 ✓ test/integration/voice-inbound-appointment.test.ts > Integration — inbound voice appointment-setting (real Postgres) > persists the spoken reason to appointments.notes only after approval + execution 22ms
 ✓ test/integration/voice-inbound-appointment.test.ts > Integration — inbound voice appointment-setting (real Postgres) > books the spoken sentence end to end: free text → PgEntityResolver → REAL drafting task → approve → production execution registry 64ms
```
Failures: none.
Non-test stderr noted: `Appointment validation warnings on create: Appointment is scheduled in the past` (during the booking test — informational, not a failure).

Tenant-grade greps:
```
grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" voice-inbound-appointment.test.ts
14: *      cross-tenant system_lookup path).
315:    // invisible to another tenant. Same scoped-read form the sibling voice

grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" voice-inbound-appointment.test.ts
254:        timezone: 'America/Chicago',
298:    expect(booked!.timezone).toBe('America/Chicago');
397:        // Below the legacy 0.9 auto-approve threshold, so the booking lands in
412:      tenantSchedulingResolver: async () => ({ timezone: BOOKING_TZ }),
439:    expect(payload.timezone).toBe(BOOKING_TZ);
489:    expect(booked!.timezone).toBe(BOOKING_TZ);

grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" voice-inbound-appointment.test.ts
no match
```
Counts: `getSharedTestDb|TEST_DB_URL|new Pool(|withTestDb|testDb` = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **4**.
Anomalies: none. Cross-tenant grep hits are comments, not actual cross-tenant test assertions — this file does not itself contain a live second-tenant negative test (comments only).

---

## 2. `test/integration/identify-caller.test.ts` (row 2.3)

Command: `... test/integration/identify-caller.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:35:48
   Duration  1.99s (transform 10ms, setup 0ms, import 24ms, tests 29ms, environment 0ms)
```
✓ lines (4 total, all shown):
```
 ✓ test/integration/identify-caller.test.ts > Postgres integration — identifyCaller phone_normalized reconciliation > matches a customer stored in +1 E.164 form (generated column keeps the leading 1) 3ms
 ✓ test/integration/identify-caller.test.ts > Postgres integration — identifyCaller phone_normalized reconciliation > still matches a customer stored in bare 10-digit form 2ms
 ✓ test/integration/identify-caller.test.ts > Postgres integration — identifyCaller phone_normalized reconciliation > returns unknown for a caller with no stored customer 1ms
 ✓ test/integration/identify-caller.test.ts > Postgres integration — identifyCaller phone_normalized reconciliation > does NOT match a US customer when a non-NANP international caller shares the last 10 digits 2ms
```
Failures: none.

Tenant-grade greps: all three → **no match**.
Counts: pool/testDb pattern = **3**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **1**.
Anomalies: no cross-tenant test in this file at all — matches the Confirm cell's note that "the voice unknown→lead leg is untested at real DB; only the SMS caller is." No tenant-isolation assertion present here.

---

## 3. `test/integration/mms-to-quote.int.test.ts` (row 2.8)

Command: `... test/integration/mms-to-quote.int.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:35:56
   Duration  2.54s (transform 320ms, setup 0ms, import 421ms, tests 104ms, environment 0ms)
```
✓ lines (3 total, all shown):
```
 ✓ test/integration/mms-to-quote.int.test.ts > Postgres integration — MMS-to-quote (U2) > unknown customer MMS → draft_estimate proposal persisted with tenant_id + audit 36ms
 ✓ test/integration/mms-to-quote.int.test.ts > Postgres integration — MMS-to-quote (U2) > known customer MMS → drafts against the resolved customer (no duplicate created) 21ms
 ✓ test/integration/mms-to-quote.int.test.ts > Postgres integration — MMS-to-quote (U2) > ambiguous sender (shared phone) → clarification proposal, no draft_estimate 19ms
```
Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant → no match
timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
131:    // the auto-approve threshold, so it lands in the owner queue.
listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **1**.
Anomalies: no cross-tenant test in this file.

---

## 4. `test/integration/inbound-sms-capture.test.ts` (row 2.10)

Command: `... test/integration/inbound-sms-capture.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:36:05
   Duration  3.51s (transform 930ms, setup 0ms, import 1.37s, tests 167ms, environment 0ms)
```
✓ lines (7 total, all shown):
```
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > resolves the sender phone to a customer and threads the inbound text (real columns pinned) 23ms
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > appends a second inbound text onto the same open thread 23ms
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > find-or-creates a lead for an unknown number and threads onto it (deduped on repeat) 32ms
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > threads as unmatched when no lead repo is wired 11ms
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > collapses concurrent unmatched-number captures to one open thread (migration 200 index) 18ms
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > collapses concurrent new-lead captures to one open thread (migration 200 index) 18ms
 ✓ test/integration/inbound-sms-capture.test.ts > Postgres integration — U4 inbound SMS capture > does not bleed a captured thread across tenants 16ms
```
Failures: none.

Tenant-grade greps: all three patterns → **no match** (the tenant-isolation test at line 7 above exists but does not use the literal strings the grep patterns test for — "bleed a captured thread across tenants" instead of "cross-tenant"/"tenantB"/etc.).
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **2**.
Anomalies: this file does have a genuine cross-tenant-bleed test ("does not bleed a captured thread across tenants"), but it doesn't match any of the three prescribed tenant-grade grep patterns — a false negative in the grep-based heuristic, worth flagging.

---

## 5. `test/integration/dispatch-availability.test.ts` (row 3.2)

Command: `... test/integration/dispatch-availability.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:36:15
   Duration  2.54s (transform 155ms, setup 0ms, import 210ms, tests 73ms, environment 0ms)
```
✓ lines (3 total, all shown):
```
 ✓ test/integration/dispatch-availability.test.ts > Postgres integration — dispatch availability (findBookableSlots) > offers only slots inside business hours (08:00–17:00) for an empty calendar 17ms
 ✓ test/integration/dispatch-availability.test.ts > Postgres integration — dispatch availability (findBookableSlots) > removes the booked (buffered) window from the offered slots 3ms
 ✓ test/integration/dispatch-availability.test.ts > Postgres integration — dispatch availability (findBookableSlots) > does not let tenant A's appointment block tenant B's availability (tenant isolation) 3ms
```
Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant →
33:  let tenantB: { tenantId: string; userId: string };
109:    tenantB = await createTestTenant(pool);
111:    await seedTenant(tenantB);
126:      { tenantId: tenantB.tenantId, fromDate: day, toDate: day, timezone, durationMin: 60, maxSlots: 20 },
157:      { tenantId: tenantB.tenantId, fromDate: day, toDate: day, timezone, durationMin: 60, maxSlots: 20 },

timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
34:  const timezone = 'UTC';
42:    await settingsRepo.update(tenant.tenantId, { timezone });
95:      timezone,
126:      { tenantId: tenantB.tenantId, fromDate: day, toDate: day, timezone, durationMin: 60, maxSlots: 20 },
139:      { tenantId: tenantA.tenantId, fromDate: day, toDate: day, timezone, durationMin: 60, maxSlots: 20 },
157:      { tenantId: tenantB.tenantId, fromDate: day, toDate: day, timezone, durationMin: 60, maxSlots: 20 },

listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **2**.
Anomalies: none — real two-tenant isolation test confirmed live (tenantA + tenantB both created via `createTestTenant`).

---

## 6. `test/integration/hold-reaper.test.ts` (row 3.5)

Command: `... test/integration/hold-reaper.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:36:24
   Duration  2.35s (transform 129ms, setup 0ms, import 178ms, tests 62ms, environment 0ms)
```
✓ lines (2 total, all shown):
```
 ✓ test/integration/hold-reaper.test.ts > Postgres integration — held-slot reaper (U6) > cancels the expired hold, clears the flag, emits audit; spares live + normal 19ms
 ✓ test/integration/hold-reaper.test.ts > Postgres integration — held-slot reaper (U6) > a second sweep is a no-op (idempotent) 2ms
```
Failures: none.
Non-test stdout noted (structured log lines): `{"level":"info","message":"Hold-reaper sweep completed",...,"tenants":1,"reaped":1,"failed":0}` and a second sweep line with `"reaped":0`.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant → no match
timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
92:        timezone: 'America/New_York',
listTenantIds: async () => [ →
122:      listTenantIds: async () => [tenant.tenantId],
148:      listTenantIds: async () => [tenant.tenantId],
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **1**.
Anomalies: no cross-tenant negative test in this file (single-tenant fixture only, per `listTenantIds` returning one tenant array).

---

## 7. `test/integration/slot-conflict-checker.test.ts` (row 3.5)

Command: `... test/integration/slot-conflict-checker.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:36:33
   Duration  2.38s (transform 134ms, setup 0ms, import 185ms, tests 61ms, environment 0ms)
```
✓ lines (4 total, all shown):
```
 ✓ test/integration/slot-conflict-checker.test.ts > Postgres integration — slot conflict checker (U6) > an active appointment blocks the overlapping slot (customer_busy) 5ms
 ✓ test/integration/slot-conflict-checker.test.ts > Postgres integration — slot conflict checker (U6) > a canceled appointment does NOT block the slot 2ms
 ✓ test/integration/slot-conflict-checker.test.ts > Postgres integration — slot conflict checker (U6) > an expired hold does NOT block the slot (released) 2ms
 ✓ test/integration/slot-conflict-checker.test.ts > Postgres integration — slot conflict checker (U6) > a live hold DOES block the slot 5ms
```
Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant → no match
timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
99:        timezone: 'America/New_York',
listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **1**.
Anomalies: no cross-tenant test in this file (single-tenant fixture).

---

## 8. `test/integration/technician-double-booking-race.test.ts` (row 3.6)

Command: `... test/integration/technician-double-booking-race.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  06:36:41
   Duration  2.83s (transform 213ms, setup 0ms, import 279ms, tests 257ms, environment 0ms)
```
✓ lines (5 total, all shown):
```
 ✓ test/integration/technician-double-booking-race.test.ts > Postgres integration — technician double-booking race (TEST-02) > two CONCURRENT assignTechnician calls for the SAME technician on the SAME time slot: exactly one succeeds, the other is rejected as a conflict 84ms
 ✓ test/integration/technician-double-booking-race.test.ts > Postgres integration — technician double-booking race (TEST-02) > two CONCURRENT assignTechnician calls for DIFFERENT technicians on the SAME slot both succeed (control — not an over-broad lock) 34ms
 ✓ test/integration/technician-double-booking-race.test.ts > Postgres integration — technician double-booking race (TEST-02) > two CONCURRENT assignTechnician calls for the SAME technician on NON-overlapping slots both succeed 27ms
 ✓ test/integration/technician-double-booking-race.test.ts > Postgres integration — technician double-booking race (TEST-02) > a CANCELED appointment's assignment is not a ghost double-booking (A13) > assignTechnician (app-layer pre-flight): a canceled appointment at the SAME slot does not block a new assignment for the same technician 40ms
 ✓ test/integration/technician-double-booking-race.test.ts > Postgres integration — technician double-booking race (TEST-02) > a CANCELED appointment's assignment is not a ghost double-booking (A13) > DB EXCLUDE constraint (no_double_booking): a raw assignment INSERT on the overlapping slot succeeds once the conflicting appointment is canceled 15ms
```
Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant → no match
timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
82:      timezone: 'UTC',
listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **1**.
Anomalies: no cross-tenant test in this file — single-tenant race/concurrency proof only, consistent with the row's focus on the DB EXCLUDE constraint rather than tenant isolation.

---

## 9. `test/integration/entity-resolution.test.ts` (row 3.7)

Command: `... test/integration/entity-resolution.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  94 passed (94)
   Start at  06:36:52
   Duration  8.17s (transform 2.17s, setup 0ms, import 3.36s, tests 1.31s, environment 0ms)
```
✓ lines: 94 total. First 40 shown verbatim (all passed, no ✗):
```
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > exact display_name match resolves with score 1.0 3ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > transcription typo resolves via trigram similarity 2ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > duplicate display_names → ambiguous with both candidates 7ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > unknown reference → not_found with the raw reference 2ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > never resolves across tenants 2ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > archived customers are excluded 4ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > exact full-name match resolves with role hint 3ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > transcription typo resolves via trigram similarity 2ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > two technicians with the same name → ambiguous with both candidates 3ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > soft-deleted users never resolve 2ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > never resolves across tenants 2ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > router end-to-end: spoken technician name lands as a verified toTechnicianId 15ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > a FIRST-NAME-ONLY reference resolves against a full name (7fbff6e) 5ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > a PARTIAL first name stays below the floor and does not resolve 3ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > two technicians sharing only a FIRST name → ambiguous, never a guess 4ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > overflow guard (six confident matches escalate, five do not) > SIX technicians sharing a first name → not_found, not an arbitrary five-candidate picker 8ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > overflow guard (six confident matches escalate, five do not) > FIVE technicians sharing a first name → ambiguous with all five, not escalated 5ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > resolves jobs by summary (schema column, not the nonexistent title) 10ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > an ambiguous job NAME answers with APPOINTMENT candidates, not job candidates 26ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3 positive: a named job reference with ONE upcoming appointment resolves to that appointment 14ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3: a named job reference with MULTIPLE upcoming appointments → ambiguous, ≤5 candidates carrying date + assigned tech 22ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3 (the defect fix): a NAME-BEARING reference matching NO job returns not_found — even when the tenant has exactly one upcoming appointment (the case that would otherwise resolve via the nameless fallback) 13ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3 regression pin: a NAMELESS reference with exactly one upcoming appointment still resolves (SCH-03 unbroken) 11ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-4: two technicians sharing a FIRST NAME → reassign_appointment drafting short-circuits to voice_clarification (technician picker) 17ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-4 (identical full name, retained): two technicians with the SAME full name → reassign_appointment drafting also short-circuits to voice_clarification 15ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > "move the Garcia job" resolves through customers → jobs when the SUMMARY is "AC repair" and only the CUSTOMER carries the name 14ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a LAST-NAME-ONLY reference clears the confirm floor, which whole-string similarity() provably does not 22ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > two jobs for the SAME customer stay a one-tap clarification, in appointment kind 23ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a customer-named reference never crosses tenants 14ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an ARCHIVED customer cannot answer a named reference 13ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > "the 2pm" resolves the appointment at 2pm in the TENANT's zone, not at 14:00 UTC 35ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > "tomorrow at 2" resolves TOMORROW's 2pm in the tenant zone 44ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > two appointments at the stated time stay a one-tap clarification carrying what distinguishes them 26ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a canceled appointment is not a reschedule target even at the stated time 14ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a clock-time reference never crosses tenants 15ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a clock time that matches nothing REFUSES — it never answers with a different appointment 17ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > a tenant with NO tenant_settings row does not resolve "the 2pm" 11ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > an explicit NULL timezone refuses the same way — migration 263 made "unchosen" representable precisely so it could be told apart from a real Eastern tenant 13ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > an UNRECOGNIZED timezone string refuses too — a garbage zone is not a chosen one 13ms
 ✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > CONTROL: the identical fixture DOES resolve once the tenant actually states Eastern — so the refusals above are the missing zone, not an unmatchable appointment 14ms
```
(...54 more ✓ lines omitted for brevity, all passed — 94 total, 0 failures. Full raw log saved at /Users/joshuakay/.claude/jobs/c58f3967/tmp/runs/09-entity-resolution.log.)

Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant → no match

timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
561:          timezone: 'America/Chicago',
635:        timezone: 'America/Chicago', status: 'scheduled', holdPendingApproval: false,
916:      timezone?: string;
920:      if (opts.timezone) {
922:          `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
924:          [crypto.randomUUID(), t.tenantId, 'Test Shop', opts.timezone],
1031:        timezone: 'America/Chicago',
1227:        timezone: TZ,
1258:        timezone: TZ,
1284:        timezone: TZ,
1314:        timezone: TZ,
1332:        timezone: TZ,
1337:        `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
1363:        timezone: TZ,
1388:    // -- an UNSET tenant timezone must REFUSE, never default ---------------
1403:    describe('an unset tenant timezone refuses instead of defaulting to Eastern', () => {
1407:        // Omitting `timezone` seeds no tenant_settings row at all.
1422:      it('an explicit NULL timezone refuses the same way — migration 263 made "unchosen" representable precisely so it could be told apart from a real Eastern tenant', async () => {
1428:          `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
1442:      it('an UNRECOGNIZED timezone string refuses too — a garbage zone is not a chosen one', async () => {
1448:          `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
1466:          timezone: EASTERN,

listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **22**.
Anomalies: multiple "never resolves across tenants" / "never crosses tenants" tests exist (lines confirmed in the ✓ output above), but they don't match the literal `tenantB|otherTenant|...` grep pattern — same false-negative pattern seen in file 4. `createTestTenant(` = 22 is by far the highest count of any file in this run, consistent with 94 tests across many `describe` blocks.

---

## 10. `test/integration/cancel-appointment-voice.test.ts` (rows 3.7, 3.10)

Command: `... test/integration/cancel-appointment-voice.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:37:09
   Duration  3.15s (transform 646ms, setup 0ms, import 847ms, tests 107ms, environment 0ms)
```
✓ lines (4 total, all shown):
```
 ✓ test/integration/cancel-appointment-voice.test.ts > Integration — voice cancel_appointment (real Postgres) > drafts a cancel_appointment proposal with the resolved appointment and reason 39ms
 ✓ test/integration/cancel-appointment-voice.test.ts > Integration — voice cancel_appointment (real Postgres) > approve → execute cancels the appointment and emits exactly one appointment.canceled audit event 34ms
 ✓ test/integration/cancel-appointment-voice.test.ts > Integration — voice cancel_appointment (real Postgres) > cross-tenant negative: another tenant cannot read or affect the canceled appointment 31ms
 ✓ test/integration/cancel-appointment-voice.test.ts > Integration — voice cancel_appointment (real Postgres) > cancel_appointment is action-classed 'irreversible' and decideInitialStatus never auto-approves it 2ms
```
Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant →
24: *   3. Cross-tenant negative: another tenant can neither read the appointment
268:  it('cross-tenant negative: another tenant cannot read or affect the canceled appointment', async () => {
288:      payload: { appointmentId, reason: 'cross-tenant cancel attempt' },
289:      summary: 'cross-tenant cancel attempt',

timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
32: *      supervisor mode/presence, or tenant threshold override. Per CLAUDE.md
38: *      irreversible proposal never even reaches the threshold/supervisor
165:      timezone: TENANT_TIMEZONE,
191:      timezone: TENANT_TIMEZONE,
315:    // tenant threshold override — 'approved' must never come out. Contrast

listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **4**.
Anomalies: this file's cross-tenant grep hit IS a live executing test (line 268), unlike files 1/9 where the hits were comments only.

---

## 11. `test/integration/reschedule-appointment-voice.test.ts` (row 3.10)

Command: `... test/integration/reschedule-appointment-voice.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:37:18
   Duration  3.13s (transform 648ms, setup 0ms, import 849ms, tests 150ms, environment 0ms)
```
✓ lines (3 total, all shown):
```
 ✓ test/integration/reschedule-appointment-voice.test.ts > Integration — voice reschedule_appointment (real Postgres) > drafts a reschedule_appointment proposal with the deterministically resolved window 79ms
 ✓ test/integration/reschedule-appointment-voice.test.ts > Integration — voice reschedule_appointment (real Postgres) > approve → execute updates the scheduled window and emits exactly one appointment.rescheduled audit event 34ms
 ✓ test/integration/reschedule-appointment-voice.test.ts > Integration — voice reschedule_appointment (real Postgres) > cross-tenant negative: another tenant cannot read or affect the rescheduled appointment 36ms
```
Failures: none.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant →
25: *   3. Cross-tenant negative: another tenant can neither read the appointment
299:  it('cross-tenant negative: another tenant cannot read or affect the rescheduled appointment', async () => {
325:      summary: 'cross-tenant reschedule attempt',

timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
18: *      and the spoken phrase deterministically (tenant timezone + a fixed
185:      timezone: TENANT_TIMEZONE,
209:      timezone: TENANT_TIMEZONE,
229:      timezone: TENANT_TIMEZONE,

listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **2**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **4**.
Anomalies: none — matches the pattern of file 10 (live cross-tenant negative test present).

---

## 12. `test/integration/dropped-call-worker.test.ts` (row 2.7 — matched via `ls | grep -iE "dropped|recovery"`)

Command: `... test/integration/dropped-call-worker.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  22 passed (22)
   Start at  06:37:30
   Duration  3.93s (transform 958ms, setup 0ms, import 1.43s, tests 513ms, environment 0ms)
```
✓ lines (22 total, all shown — under 40):
```
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > happy path: sweeps a due row, sends one SMS, stamps sent_at + sid, emits sent audit 25ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > suppresses with booking_completed when resolvedSince reports a booking 15ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > suppresses with rate_limited when the per-caller limiter rejects 17ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > idempotent on re-sweep: a sent row is never re-drained (partial index excludes it) 18ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > tenant isolation: a single cross-tenant sweep stamps each row under its own tenant 22ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > send failure leaves the row pending so the next sweep retries — and no rate-limit token is burned 11ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery worker — integration > scheduling is idempotent per (tenant_id, voice_session_id) via the ON CONFLICT path 5ms
 ✓ test/integration/dropped-call-worker.test.ts > resolved-since checker — integration (voice_sessions / proposals) > signal 1: a session that resolved to completed suppresses as booking_completed 20ms
 ✓ test/integration/dropped-call-worker.test.ts > resolved-since checker — integration (voice_sessions / proposals) > signal 2: an executed proposal from the persisted context suppresses (JSONB round-trip) 21ms
 ✓ test/integration/dropped-call-worker.test.ts > resolved-since checker — integration (voice_sessions / proposals) > signal 3: a newer completed call-back for the same customer suppresses 19ms
 ✓ test/integration/dropped-call-worker.test.ts > resolved-since checker — integration (voice_sessions / proposals) > unresolved drop (outcome=dropped, no proposals, no call-back) proceeds to send 21ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery — end-to-end wiring (flag gate + production adapters) > flag off everywhere → row skipped and left pending; tenant override on → sends 51ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery — end-to-end wiring (flag gate + production adapters) > tenant override OFF wins over an enabled platform flag (kill switch) 10ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery — end-to-end wiring (flag gate + production adapters) > a DNC caller is suppressed as opted_out through the production gate 19ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery — end-to-end wiring (flag gate + production adapters) > concurrent sweeps: no SKIP LOCKED means both may fetch, but markSent is single-winner and both sends share one idempotency key 50ms
 ✓ test/integration/dropped-call-worker.test.ts > recovery threader — integration (conversations / conversation_links) > outbound recovery and a later inbound reply land on the SAME conversation 28ms
 ✓ test/integration/dropped-call-worker.test.ts > recovery threader — integration (conversations / conversation_links) > re-threading is idempotent against the real unique indexes (links + one open thread) 25ms
 ✓ test/integration/dropped-call-worker.test.ts > recovery rate-limiter — integration (phone_rate_limits) > check() is non-consuming: N checks leave zero rows and stay allowed 9ms
 ✓ test/integration/dropped-call-worker.test.ts > recovery rate-limiter — integration (phone_rate_limits) > record() after a send consumes exactly one token; the next check is denied 8ms
 ✓ test/integration/dropped-call-worker.test.ts > recovery rate-limiter — integration (phone_rate_limits) > concurrent record()s for one caller admit exactly the limit (advisory-lock atomicity) 13ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery — starvation guard (integration) > findExpired returns only stale rows; findDueTenantIds/findDueForTenants only fresh, scoped 12ms
 ✓ test/integration/dropped-call-worker.test.ts > dropped-call recovery — starvation guard (integration) > an enabled tenant sends even behind a >batch-size disabled-tenant backlog 61ms
```
Failures: none. This is the file that exercises the "flag off everywhere → sends when tenant override on" transition explicitly (row 2.7's "flag-on transition" claim), confirmed live above.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant →
168: * gating cross-tenant reads — if the policy were dropped, the assertion fails.
362:  it('tenant isolation: a single cross-tenant sweep stamps each row under its own tenant', async () => {
363:    const tenantB = await createTestTenant(pool);
365:    const sessionB = await scheduleRow(pool, tenantB.tenantId, '+15552220002');
387:      [tenantB.tenantId, sessionB],
404:    const auditB = await asTenant(pool, tenantB.tenantId, (client) =>
493:    // findDue drains cross-tenant: clear pending rows left by earlier

timezone|business_hours|labor_rate|threshold|pack|digest_enabled → no match

listTenantIds: async () => [ → no match
```
Counts: pool/testDb pattern = **7**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **8**.
Anomalies: none — cross-tenant isolation test is live and confirmed (`tenantB` created via real `createTestTenant`, real Postgres row-level assertions).

---

## 13. `test/integration/appointment-reminder-owner-push.integration.test.ts` (row 3.9 — "owner push" half only, per instruction)

Command: `... test/integration/appointment-reminder-owner-push.integration.test.ts`
Summary:
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:37:42
   Duration  2.47s (transform 283ms, setup 0ms, import 373ms, tests 156ms, environment 0ms)
```
✓ lines (2 total, all shown):
```
 ✓ test/integration/appointment-reminder-owner-push.integration.test.ts > Postgres integration — appointment-reminder owner push (U4) > fires the owner push for the real appointment and persists the owner-push dispatch key 91ms
 ✓ test/integration/appointment-reminder-owner-push.integration.test.ts > Postgres integration — appointment-reminder owner push (U4) > does not double-push across sweeps (durable dispatch-key idempotency) 63ms
```
Failures: none.
Non-test stderr noted (x2, once per test): `Appointment validation warnings on create: Appointment is scheduled in the past` — informational only.

Tenant-grade greps:
```
tenantB|otherTenant|secondTenant|cross-tenant|another tenant → no match
timezone|business_hours|labor_rate|threshold|pack|digest_enabled →
88:      `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone)
137:        timezone: 'America/Chicago',
listTenantIds: async () => [ →
171:      listTenantIds: async () => [tenant.tenantId],
```
Counts: pool/testDb pattern = **4**; `InMemoryAuditRepository` = **0**; `createTestTenant(` = **1**.
Anomalies: this file is single-tenant only (2 tests, `listTenantIds` scoped to one tenant) — no cross-tenant test present, consistent with the row's note that "the customer reminder is not" durable at real DB (only the owner push half was in scope here, per instructions).

---

# Results table

| # | File | Rows | Test Files | Tests | Pass | Fail |
|---|------|------|-----------|-------|------|------|
| 1 | voice-inbound-appointment.test.ts | 2.1, 3.1, 3.10 | 1 passed (1) | 3 passed (3) | 3 | 0 |
| 2 | identify-caller.test.ts | 2.3 | 1 passed (1) | 4 passed (4) | 4 | 0 |
| 3 | mms-to-quote.int.test.ts | 2.8 | 1 passed (1) | 3 passed (3) | 3 | 0 |
| 4 | inbound-sms-capture.test.ts | 2.10 | 1 passed (1) | 7 passed (7) | 7 | 0 |
| 5 | dispatch-availability.test.ts | 3.2 | 1 passed (1) | 3 passed (3) | 3 | 0 |
| 6 | hold-reaper.test.ts | 3.5 | 1 passed (1) | 2 passed (2) | 2 | 0 |
| 7 | slot-conflict-checker.test.ts | 3.5 | 1 passed (1) | 4 passed (4) | 4 | 0 |
| 8 | technician-double-booking-race.test.ts | 3.6 | 1 passed (1) | 5 passed (5) | 5 | 0 |
| 9 | entity-resolution.test.ts | 3.7 | 1 passed (1) | 94 passed (94) | 94 | 0 |
| 10 | cancel-appointment-voice.test.ts | 3.7, 3.10 | 1 passed (1) | 4 passed (4) | 4 | 0 |
| 11 | reschedule-appointment-voice.test.ts | 3.10 | 1 passed (1) | 3 passed (3) | 3 | 0 |
| 12 | dropped-call-worker.test.ts | 2.7 | 1 passed (1) | 22 passed (22) | 22 | 0 |
| 13 | appointment-reminder-owner-push.integration.test.ts | 3.9 | 1 passed (1) | 2 passed (2) | 2 | 0 |
| **Total** | | | **13/13 files passed** | **161 tests** | **161** | **0** |

No re-runs were needed — every run passed on the first attempt (no container-start failures encountered).

# Pool / InMemory / createTestTenant counts per file

| File | pool/testDb count | InMemoryAuditRepository | createTestTenant( |
|------|---:|---:|---:|
| voice-inbound-appointment.test.ts | 2 | 0 | 4 |
| identify-caller.test.ts | 3 | 0 | 1 |
| mms-to-quote.int.test.ts | 2 | 0 | 1 |
| inbound-sms-capture.test.ts | 2 | 0 | 2 |
| dispatch-availability.test.ts | 2 | 0 | 2 |
| hold-reaper.test.ts | 2 | 0 | 1 |
| slot-conflict-checker.test.ts | 2 | 0 | 1 |
| technician-double-booking-race.test.ts | 2 | 0 | 1 |
| entity-resolution.test.ts | 2 | 0 | 22 |
| cancel-appointment-voice.test.ts | 2 | 0 | 4 |
| reschedule-appointment-voice.test.ts | 2 | 0 | 4 |
| dropped-call-worker.test.ts | 7 | 0 | 8 |
| appointment-reminder-owner-push.integration.test.ts | 4 | 0 | 1 |

`InMemoryAuditRepository` = 0 in every file — none of these 13 integration files use an in-memory audit repo; all are wired to real Postgres pools.

# Anomalies (consolidated)

1. **2.1 Confirm-cell filter**: `-t "routes a dialed number"` is not a literal/complete test title, but as a vitest substring filter it *would* match the actual title `routes a dialed number to its tenant via the real phoneE164 column` (line 232). No exact/standalone match exists.
2. **Tenant-grade grep false negatives**: `inbound-sms-capture.test.ts` (file 4) and `entity-resolution.test.ts` (file 9) each contain genuine cross-tenant-isolation tests ("does not bleed a captured thread across tenants", multiple "never resolves/crosses tenants" cases) that do **not** match the prescribed `tenantB|otherTenant|secondTenant|cross-tenant|another tenant` pattern — the tests use different phrasing ("bleed... across tenants", "crosses tenants") than the grep anticipates. The grep heuristic under-counts real tenant-isolation coverage for these two files.
3. **Files 1 and 9** (`voice-inbound-appointment.test.ts`, `entity-resolution.test.ts`) have cross-tenant grep hits that are comments only, not live test assertions in that specific grep match — real tenant isolation there is instead evidenced by "never resolves/crosses tenants" `it(...)` titles that don't match the pattern (see anomaly 2).
4. **Disk space**: stable at 2.8Gi free across all four checkpoints — never within an order of magnitude of the 600MiB stop threshold.
5. **No test failures, no retries, no Docker container-start failures** across all 13 files / 161 tests in this lane's execution window.
6. Two files (`voice-inbound-appointment.test.ts`, `appointment-reminder-owner-push.integration.test.ts`) emit a benign stderr warning during execution — `Appointment validation warnings on create: Appointment is scheduled in the past` — not a test failure, just informational logging from the appointment-creation path when fixtures backdate timestamps.
