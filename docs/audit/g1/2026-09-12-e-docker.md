# G1 Entry Audit — §8.8 Bill / §8.9 Close — Docker-gated integration run log

Worktree: `/Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane` (branch `lane/rung-map-env` @ `2c2aa6d90` = `origin/main`)
All commands run from `packages/api` with:
`DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose <file>`

No failures occurred; no file required a re-run. Disk space checked every third run, stayed at 2.5–2.7 GiB free the whole session (never below 600 MiB threshold).

Raw tee'd logs also saved under `/Users/joshuakay/.claude/jobs/c58f3967/tmp/raw/`.

---

## Row 8.1 — `integration/draft-invoice-execution.test.ts`

**Command:** `... integration/draft-invoice-execution.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  9 passed (9)
   Start at  06:45:18
   Duration  3.73s (transform 668ms, setup 0ms, import 921ms, tests 194ms, environment 0ms)
```

**✓/✗ lines (9 of 9 shown):**
```
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — voice draft_invoice → approve → execute → persist + audit > persists the invoice row with real columns + integer-cent totals 2ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — voice draft_invoice → approve → execute → persist + audit > persists the invoice line items 1ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — voice draft_invoice → approve → execute → persist + audit > emits exactly one invoice.created audit event (regression guard for handler+registry audit wiring) 1ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — voice draft_invoice → approve → execute → persist + audit > does not expose the invoice to another tenant (scoped read) 3ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — draft_invoice price-scale correction persists correctly (#909) > the exact live shape ("450 dollars" for an uncatalogued line) persists as 45000 cents, not 450 23ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — B6: customer-only draft_invoice (no jobId) auto-opens a job > opens a job for the customer and persists the invoice + audit when jobId is absent 26ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — B6: customer-only draft_invoice (no jobId) auto-opens a job > fails with a clear message when the customer has no service location 5ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — B6: customer-only draft_invoice (no jobId) auto-opens a job > the jobId-present path is unchanged (existing job used verbatim, no auto-create) 19ms
 ✓ test/integration/draft-invoice-execution.test.ts > Postgres integration — Tradesperson wave 1, Task 4: apply_credit persists a negative line > appends a negative non-taxable line via the PRODUCTION registry and persists reduced totals 12ms
```

**Tenant-grade greps (raw):**
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants`:
```
197:  it('does not expose the invoice to another tenant (scoped read)', async () => {
```
- `timezone|business_hours|labor_rate|threshold|pack|digest_enabled`: no match
- `listTenantIds:\s*async\s*\(\)\s*=>\s*\[`: no match

**Counts:** pool/testdb=6, InMemoryAuditRepository=0, createTestTenant(=5
**Audit grep (raw):**
```
190:      `SELECT event_type FROM audit_events
191:        WHERE entity_type = 'invoice' AND entity_id = $1 AND event_type = 'invoice.created'`,
471:      `SELECT event_type FROM audit_events
472:        WHERE entity_type = 'invoice' AND entity_id = $1 AND event_type = 'invoice.created'`,
475:    expect(auditRows).toHaveLength(1);
478:      `SELECT event_type FROM audit_events
479:        WHERE entity_type = 'job' AND entity_id = $1 AND event_type = 'job.created'`,
739:      `SELECT event_type FROM audit_events
740:        WHERE entity_type = 'invoice' AND entity_id = $1 AND event_type = 'credit.applied'`,
743:    expect(auditRows).toHaveLength(1);
```

**Anomalies:** None. Matches row claim: exactly one `invoice.created` audit event asserted (`toHaveLength(1)`).

---

## Row 8.3 — `integration/update-job-execution.test.ts`

**Command:** `... integration/update-job-execution.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  11 passed (11)
   Start at  06:45:54
   Duration  4.00s (transform 830ms, setup 0ms, import 1.12s, tests 342ms, environment 0ms)
```

**✓/✗ lines (11 of 11 shown), plus 2 warn-level log lines interleaved (verbatim):**
```
{"level":"warn","message":"update_job completed a job but completion effects (auto-invoice + milestone minting) were SKIPPED — completion deps not wired","timestamp":"2026-09-12T13:45:58.462Z","service":"update-job-handler","environment":"test","tenantId":"273391b2-079b-4b39-9fa7-9b7be99c5245","jobId":"c7c94c48-93e1-49c3-a1a2-7345ed192c83"}
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > changes the real job row status + priority via the production registry 23ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > changes the real job row title + description 10ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > emits a job.updated audit event (regression guard for handler+registry audit wiring) 18ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > does not expose the job to another tenant (scoped read) and a cross-tenant jobId fails cleanly 6ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > a completed transition stamps completed_at AND runs completion effects (auto-drafts an invoice proposal) 78ms
{"level":"warn","message":"update_job completed a job but completion effects (auto-invoice + milestone minting) were SKIPPED — completion deps not wired","timestamp":"2026-09-12T13:45:58.659Z","service":"update-job-handler","environment":"test","tenantId":"1ac300ba-1a25-4f1d-b742-5c0b542acfca","jobId":"52a8582b-c95f-4c26-9dca-bfa2f7c0307a"}
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > the ILIKE display-text search CANNOT reach this job from the spoken name — only the customer traversal can 5ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > the REAL resolver reaches the job from the spoken surname, past a prefix decoy 3ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > drafting: the RESOLVER id lands on the payload and lifts the gate — the model's hallucinated id loses 6ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > end to end: the drafted proposal approves and the REAL job row completes, with both audit events 38ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > an ILLEGAL lifecycle move is REFUSED end to end — a resolved job is not a licence to skip the state machine 22ms
 ✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > cross-tenant: the same sentence resolves to nothing for another tenant, and a borrowed jobId stays GATED 15ms
```

**Tenant-grade greps (raw):**
- tenantB/etc:
```
11: * completion effects, the job.updated audit event, and the cross-tenant
228:  it('does not expose the job to another tenant (scoped read) and a cross-tenant jobId fails cleanly', async () => {
242:        summary: 'cross-tenant attempt',
252:    // The row is untouched — no cross-tenant write leaked through.
759:  it('cross-tenant: the same sentence resolves to nothing for another tenant, and a borrowed jobId stays GATED', async () => {
```
- timezone/etc: `297:      timezone: 'UTC',`
- listTenantIds: no match

**Counts:** pool/testdb=3, InMemoryAuditRepository=0, createTestTenant(=5
**Audit grep (raw):**
```
212:      `SELECT count(*)::int AS n FROM audit_events
213:        WHERE entity_type = 'job' AND entity_id = $1 AND event_type = 'job.updated'`,
221:      `SELECT count(*)::int AS n FROM audit_events
222:        WHERE entity_type = 'job' AND entity_id = $1 AND event_type = 'job.updated'`,
709:      `SELECT count(*)::int AS n FROM audit_events
710:        WHERE entity_type = 'job' AND entity_id = $1 AND event_type = 'job.updated'`,
716:      `SELECT count(*)::int AS n FROM audit_events
717:        WHERE entity_type = 'job' AND entity_id = $1 AND event_type = 'job.status_changed'`,
```

**Anomalies:** Ran again as instructed (previously 11/11 in the §8.6 lane) — reproduced 11/11. **Both runs of the "completed transition" test log a warning that completion effects (auto-invoice + milestone minting) were SKIPPED — completion deps not wired**, yet the test still passes. This directly touches row 8.3's acceptance clause "completion effects auto-draft an invoice proposal": the warning text says the auto-invoice effect is not wired in this harness/path, worth flagging to whoever assigns the rung.

---

## Row 8.4 — `integration/payment-credit-guards.test.ts` and `integration/invoice-webhook-paid.test.ts`

### 8.4a `integration/payment-credit-guards.test.ts`

**Command:** `... integration/payment-credit-guards.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  8 passed (8)
   Start at  06:46:08
   Duration  3.06s (transform 267ms, setup 0ms, import 364ms, tests 183ms, environment 0ms)
```

**✓/✗ lines (8 of 8):**
```
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > two concurrent full-balance payments credit the invoice exactly once (never 2x) 32ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > the SQL overpay predicate rejects a second full-balance credit deterministically 11ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > a voided invoice takes no credit — void -> paid is impossible at the SQL layer 11ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > a failed compensation row does NOT block the capped retry for the same provider reference 23ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > reconcileBalanceAtomic never resurrects a void — the guard is in the UPDATE itself 20ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > setPaymentLinkIfPayable persists only on a payable, unchanged, link-free invoice 29ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > payment-link CAS clear (P0-9/R3): clears only while the column holds the expected id 15ms
 ✓ test/integration/payment-credit-guards.test.ts > Postgres integration — atomic credit guards (P0-3 / P0-6) > a partial credit within the balance still applies (guards do not over-reject) 9ms
```

**Tenant-grade greps (raw):** all three — no match / no match / no match
**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=1
**Audit grep (raw):** no match

**Anomalies:** No cross-tenant test and zero audit-event assertions in this file, despite covering the payment-link persistence half of row 8.4's acceptance criterion.

### 8.4b `integration/invoice-webhook-paid.test.ts`

**Command:** `... integration/invoice-webhook-paid.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:46:23
   Duration  3.25s (transform 336ms, setup 0ms, import 613ms, tests 157ms, environment 0ms)
```

**✓/✗ lines (3 of 3), with interleaved info logs (verbatim):**
```
{"level":"info","message":"Stripe webhook received","timestamp":"2026-09-12T13:46:27.095Z","service":"webhooks","environment":"test","eventId":"evt_b3f1ce7e-71cb-4c69-be8b-bc534d9f2a34","type":"checkout.session.completed"}
{"level":"info","message":"Invoice marked paid via Stripe checkout","timestamp":"2026-09-12T13:46:27.110Z","service":"webhooks","environment":"test","tenantId":"086278f8-3439-4d04-8bcb-d737aa07eb0b","invoiceId":"8f2531db-4272-4180-99d3-525c4f4e00d2","amountTotal":50000}
{"level":"info","message":"Stripe webhook received","timestamp":"2026-09-12T13:46:27.131Z","service":"webhooks","environment":"test","eventId":"evt_fc1a5caf-75e6-4a5f-80fb-8df812c3d66e","type":"checkout.session.completed"}
{"level":"info","message":"Invoice marked paid via Stripe checkout","timestamp":"2026-09-12T13:46:27.145Z","service":"webhooks","environment":"test","tenantId":"086278f8-3439-4d04-8bcb-d737aa07eb0b","invoiceId":"a83913ee-fe26-4fd8-bcba-61e2d37576f6","amountTotal":50000}
{"level":"info","message":"Duplicate Stripe event — skipping","timestamp":"2026-09-12T13:46:27.147Z","service":"webhooks","environment":"test","eventId":"evt_fc1a5caf-75e6-4a5f-80fb-8df812c3d66e","type":"checkout.session.completed"}
 ✓ test/integration/invoice-webhook-paid.test.ts > Postgres integration — W1-2 invoice webhook → paid > signed checkout.session.completed flips open invoice to paid (real columns) 51ms
 ✓ test/integration/invoice-webhook-paid.test.ts > Postgres integration — W1-2 invoice webhook → paid > replay of the same Stripe event id does not double-apply (durable idempotency) 33ms
{"level":"info","message":"Stripe webhook received","timestamp":"2026-09-12T13:46:27.164Z","service":"webhooks","environment":"test","eventId":"evt_ef31d21f-d386-4ee3-84a9-4ecde268b253","type":"payment_intent.succeeded"}
{"level":"info","message":"Invoice marked paid via payment_intent.succeeded (async settlement)","timestamp":"2026-09-12T13:46:27.179Z","service":"webhooks","environment":"test","tenantId":"086278f8-3439-4d04-8bcb-d737aa07eb0b","invoiceId":"f8f5f9fa-a786-4337-bae5-9af7a2a59d30","amountCents":50000,"paymentIntentId":"pi_evt_ef31d21f-d386-4ee3-84a9-4ecde268b253"}
{"level":"info","message":"Duplicate Stripe event — skipping","timestamp":"2026-09-12T13:46:27.192Z","service":"webhooks","environment":"test","eventId":"evt_ef31d21f-d386-4ee3-84a9-4ecde268b253","type":"payment_intent.succeeded"}
 ✓ test/integration/invoice-webhook-paid.test.ts > Postgres integration — W1-2 invoice webhook → paid > Connect direct charge (payment_intent.succeeded with event.account) settles the real ledger + idempotent 45ms
```

**Tenant-grade greps (raw):**
- tenantB/etc: no match
- timezone/etc: `12: * Run via: cd packages/api && npm run test:integration -- invoice-webhook-paid` (a comment matching "pack", not a real hit)
- listTenantIds: no match

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=1
**Audit grep (raw):** no match

**Anomalies:** No cross-tenant assertion, no audit-event assertion in either 8.4 file.

---

## Row 8.6 — payment-concurrency files (grep: `payment.*(concurr|race|lost|overpay|clamp)|concurrent.*payment`)

Grep against `ls packages/api/test/integration` returned only **3 files**, not the "five Docker-gated files" the PRD row claims:
```
payment-concurrent-credit.test.ts
payment-duplicate-race.test.ts
payment-reversal-concurrent.test.ts
```
(Two other files exist that are conceptually adjacent — `deposit-concurrent-credit.test.ts` and `deposit-credit-atomic.test.ts` — but neither matches the literal specified regex, since neither contains "payment" in the filename. Not run, per instructions to run only what the given grep matches.)

### `payment-concurrent-credit.test.ts`
**Summary:**
```
 Test Files  1 passed (1)
      Tests  1 passed (1)
   Start at  06:46:36
   Duration  2.79s (transform 222ms, setup 0ms, import 306ms, tests 67ms, environment 0ms)
```
**✓ lines (1 of 1):**
```
 ✓ test/integration/payment-concurrent-credit.test.ts > Postgres integration — concurrent distinct payments both credit the invoice > a $100 cash entry racing a $150 ACH webhook both credit the invoice (no lost update) 24ms
```
Tenant greps: no match / no match / no match. Counts: pool/testdb=2, InMemory=0, createTestTenant(=1. Audit grep: no match.

### `payment-duplicate-race.test.ts`
**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:46:47
   Duration  4.31s (transform 351ms, setup 0ms, import 476ms, tests 99ms, environment 0ms)
```
**✓ lines (3 of 3):**
```
 ✓ test/integration/payment-duplicate-race.test.ts > Postgres integration — duplicate Stripe payment is rejected + handled > a second recordPayment for the same intent credits the invoice only once 32ms
 ✓ test/integration/payment-duplicate-race.test.ts > Postgres integration — duplicate Stripe payment is rejected + handled > a failed attempt does not block the later successful retry of the same intent 17ms
 ✓ test/integration/payment-duplicate-race.test.ts > Postgres integration — duplicate Stripe payment is rejected + handled > the raw duplicate INSERT is rejected by the partial unique index (23505) 2ms
```
Tenant greps: no match / no match / no match. Counts: pool/testdb=3, InMemory=0, createTestTenant(=1. Audit grep: no match.

### `payment-reversal-concurrent.test.ts`
**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:47:01
   Duration  2.90s (transform 231ms, setup 0ms, import 315ms, tests 151ms, environment 0ms)
```
**✓ lines (3 of 3):**
```
 ✓ test/integration/payment-reversal-concurrent.test.ts > Postgres integration — payment reversal is atomic and self-healing > (a) a reversal racing a concurrent credit — both apply, no lost update 37ms
 ✓ test/integration/payment-reversal-concurrent.test.ts > Postgres integration — payment reversal is atomic and self-healing > (b) a redelivery after a crash-before-decrement reopens the invoice from the ledger 57ms
 ✓ test/integration/payment-reversal-concurrent.test.ts > Postgres integration — payment reversal is atomic and self-healing > (c) decrementAmountPaidAtomic clamps at 0 and leaves a terminal invoice untouched 21ms
```
Tenant greps: no match / no match / no match. Counts: pool/testdb=2, InMemory=0, createTestTenant(=1. Audit grep: no match.

**Row total: 3 files, 7 tests, all pass.**

**Anomalies:** PRD row 8.6 claims "five Docker-gated files, nine tests"; the specified grep pattern only matches 3 files totaling 7 tests. Either the PRD is counting the two `deposit-*-credit*.test.ts` files toward this row's evidence (which the given regex excludes), or the claim overstates the file/test count. None of the 3 matched files has any tenant-isolation or audit-event assertion.

---

## Row 8.8 — `integration/payment-refunds.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  06:47:18
   Duration  2.80s (transform 178ms, setup 0ms, import 228ms, tests 105ms, environment 0ms)
```

**✓ lines (5 of 5):**
```
 ✓ test/integration/payment-refunds.test.ts > Postgres integration — payment_refunds idempotency ledger (P0-4) > P0-4 interleave: an earlier refund retried after a later refund is deduped 13ms
 ✓ test/integration/payment-refunds.test.ts > Postgres integration — payment_refunds idempotency ledger (P0-4) > two concurrent deliveries of one refund id apply exactly once (FOR UPDATE + unique claim) 14ms
 ✓ test/integration/payment-refunds.test.ts > Postgres integration — payment_refunds idempotency ledger (P0-4) > a rejected over-refund strands no claim row — a corrected retry still applies 12ms
 ✓ test/integration/payment-refunds.test.ts > Postgres integration — payment_refunds idempotency ledger (P0-4) > RLS: refund claims are invisible to another tenant 11ms
 ✓ test/integration/payment-refunds.test.ts > Postgres integration — migration 264 backfills legacy refund claims > a pre-ledger refund (last_refund_stripe_id only) gains a claim on re-run and dedupes redelivery 19ms
```

**Tenant-grade greps (raw):**
```
12: *  4. RLS: the claim ledger is invisible cross-tenant
210:  it('RLS: refund claims are invisible to another tenant', async () => {
218:    // A same-id refund attempt from another tenant must not see (or be
```
timezone/etc: no match. listTenantIds: no match.

**Counts:** pool/testdb=3, InMemoryAuditRepository=0, createTestTenant(=3
**Audit grep (raw):** no match

**Anomalies:** Confirms the row's own confirm note — interleave, stranded-claim, RLS and migration-backfill all covered — but there is **no audit-event assertion** in this file, even though the acceptance criterion is about the ledger/status never lying (implying an audit trail). "Status is never flipped" is proven by absence-of-side-effect assertions on the invoice row, not by any audit_events query.

---

## Row 8.9 — `integration/payment-reminder-dedup.test.ts` (grep: `dunning|payment-reminder`)

Grep matched exactly 1 file: `payment-reminder-dedup.test.ts`.

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  06:47:52
   Duration  3.23s (transform 685ms, setup 0ms, import 860ms, tests 77ms, environment 0ms)
```

**✓ lines (5 of 5):**
```
 ✓ test/integration/payment-reminder-dedup.test.ts > Postgres integration — manual send_payment_reminder dedup > records the ledger row (real columns) and sends exactly once for the first manual reminder 15ms
 ✓ test/integration/payment-reminder-dedup.test.ts > Postgres integration — manual send_payment_reminder dedup > refuses a SECOND manual reminder inside the 72h window — no second send 8ms
 ✓ test/integration/payment-reminder-dedup.test.ts > Postgres integration — manual send_payment_reminder dedup > rejects a direct duplicate insert of the same ledger key (UNIQUE violation) 3ms
 ✓ test/integration/payment-reminder-dedup.test.ts > Postgres integration — manual send_payment_reminder dedup > emits an invoice.reminder_sent audit row for the delivered reminder 1ms
 ✓ test/integration/payment-reminder-dedup.test.ts > Postgres integration — manual send_payment_reminder dedup > scopes the ledger to the tenant — another tenant sees no events 4ms
```

**Tenant-grade greps (raw):**
```
250:  it('scopes the ledger to the tenant — another tenant sees no events', async () => {
```
timezone/etc: no match. listTenantIds: no match.

**Counts:** pool/testdb=3, InMemoryAuditRepository=0, createTestTenant(=2
**Audit grep (raw):**
```
243:      `SELECT event_type FROM audit_events
244:        WHERE entity_type = 'invoice' AND entity_id = $1 AND event_type = 'invoice.reminder_sent'`,
```

**Anomalies (confirms PRD claim):** Verified directly — the file's stored ledger key is `manualReminderStepKey(firstProposalId)` (line 208: `expect(rows[0].step_key).toBe(manualReminderStepKey(firstProposalId))`), i.e. a `manual:<proposalId>`-style key. There is **no test anywhere in this file that inserts or asserts against the automated cadence keys `'3:sms' | '7:sms' | '14:sms'`** cited in the row's acceptance criterion, and no test that exercises a duplicate insert of `'7:sms'` raising 23505. This corroborates the PRD's own flag: cadence-key uniqueness has no real-DB proof; only the manual-reminder path is DB-proven.

---

## Row 9.1 (D) — `integration/thank-you-sms-worker.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  8 passed (8)
   Start at  06:48:13
   Duration  3.04s (transform 239ms, setup 0ms, import 332ms, tests 205ms, environment 0ms)
```

**✓ lines (8 of 8):**
```
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > selects only jobs whose completed_at is older than the delay and whose tenant has the toggle on 70ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > skips a tenant whose send_thank_you_sms is FALSE 6ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > is idempotent — a second sweep on the same data does not re-send 23ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > writes a notification.thank_you_sms.sent audit event scoped to the job entity 24ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > transitionJobStatus stamps completed_at on the in_progress → completed transition 13ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > T4-F01 — claim-before-send against real send_claims > reclaims a stale send_claims row and sends (crash between claim and send) 22ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > T4-F01 — claim-before-send against real send_claims > Codex P2 (PR #705) — does NOT resend when a "sent" claim exists but thank_you_sms_sent_at is still NULL, and RECONCILES the missing stamp (crash between send and mark) 17ms
 ✓ test/integration/thank-you-sms-worker.test.ts > thank-you-sms worker — integration > T4-F01 — claim-before-send against real send_claims > two concurrent sweeps against the same eligible job send exactly once 30ms
```

**Tenant-grade greps (raw):** tenantB/etc: no match. timezone/etc: `62:      \`INSERT INTO tenant_settings (id, tenant_id, business_name, timezone)`. listTenantIds: no match.

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=1
**Audit grep (raw):**
```
186:    expect(events.some((e) => e.eventType === 'notification.thank_you_sms.sent')).toBe(true);
```

**Anomalies:** No consent-gate ("block mode") test in this file — row 9.1's acceptance mentions "the send survives the central consent gate in block mode," which the row itself attributes to the separate `workers/thank-you-sms-worker.test.ts` (U lane), not in scope for this run. No cross-tenant test in this file either.

---

## Row 9.2 — `integration/review-request-sweep.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:48:26
   Duration  3.71s (transform 98ms, setup 0ms, import 139ms, tests 35ms, environment 0ms)
```

**✓ lines (2 of 2):**
```
 ✓ test/integration/review-request-sweep.test.ts > review-request sweep (US-345, DB-level) > column default: tenant_settings.send_review_request defaults TRUE (migration 214) 1ms
 ✓ test/integration/review-request-sweep.test.ts > review-request sweep (US-345, DB-level) > enqueues feedback_send only for the eligible job, stamps it, and is idempotent 7ms
```

**Tenant-grade greps (raw):**
```
56:  let tenantB: { tenantId: string; userId: string };
66:    tenantB = await createTestTenant(pool);
77:      [tenantB.tenantId],
81:    const idsB = await seedCustomerLocation(pool, tenantB.tenantId, tenantB.userId);
85:    jobOldB = await seedCompletedJob(pool, tenantB.tenantId, idsB, hoursAgo(48)); // opted out
105:    // runReviewRequestSweep is a GLOBAL cross-tenant sweep, so in the full
111:        payload.tenantId === tenantA.tenantId || payload.tenantId === tenantB.tenantId,
140:        payload.tenantId === tenantA.tenantId || payload.tenantId === tenantB.tenantId,
```
timezone/etc: no match. listTenantIds: no match.

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=2
**Audit grep (raw):** no match

**Anomalies:** Confirms the row's own confirm note — "no audit assertion." tenantB is used here purely to seed a second tenant's job for the global-sweep test, not as a cross-tenant-leak assertion.

---

## Row 9.4 — `integration/google-reviews-worker.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:48:39
   Duration  2.59s (transform 145ms, setup 0ms, import 193ms, tests 116ms, environment 0ms)
```

**✓ lines (7 of 7):**
```
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > happy path: sweep persists new reviews + advances the per-tenant cursor 24ms
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > idempotent on re-sweep: a second sweep with the same upstream data persists nothing new 14ms
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > throttling: a tenant whose backoff_until is in the future is skipped — no Google call, throttled++ 6ms
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > 429 from Google: throttled++ AND poll state stamps backoff_until + consecutive_429_count 9ms
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > silently skips tenants with no integration row (not counted as failed) 3ms
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > tenant isolation: reviews persisted under tenant A are invisible to tenant B under RLS 20ms
 ✓ test/integration/google-reviews-worker.test.ts > Google Reviews worker — integration > findRecent: rating + since filters, newest-first order, LIMIT — against real columns 17ms
```

**Tenant-grade greps (raw):**
```
15: *      — cross-tenant queries via the production read path return zero.
155: * cross-tenant reads.
375:    const tenantB = await createTestTenant(pool);
379:        [tenantB.tenantId, { accountId: '789', locationId: '012' }],
417:      listTenantIds: async () => [tenantA.tenantId, tenantB.tenantId],
428:      tenantB.tenantId,
442:    const externalIdsUnderB = await asTenant(pool, tenantB.tenantId, (client) =>
506:    const tenantB = await createTestTenant(pool);
507:    expect(await reviewRepo.findRecent(tenantB.tenantId, { limit: 10 })).toEqual([]);
```
timezone/etc: no match.
listTenantIds: `[tenantA.tenantId]` on 6 occurrences (lines 232,282,292,314,339,363) plus `[tenantA.tenantId, tenantB.tenantId]` once (417).

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=3
**Audit grep (raw):** no match

**Anomalies:** Matches the row's own confirm note — "classification and drafting are unit-only" (no audit assertion here either; only RLS/cursor/backoff behavior is DB-proven).

---

## Row 9.5 — service-credit files (grep: `service-credit`)

`ls packages/api/test/integration | grep -i "service-credit"` returned **zero matches**. Broader checks for the same concept (`grep -li goodwill *.test.ts` → only `draft-invoice-execution.test.ts`; `grep -li -E "credit.*cap|cap.*credit" *.test.ts` → `deposit-credit-atomic.test.ts`, `payment-credit-guards.test.ts`) confirm no file in `test/integration/` implements a goodwill/service-credit cap test. **No file run for this row** — nothing to run.

**Anomaly:** Directly corroborates the row's own confirm note ("the existing test calls itself a smoke test that stubs `pool.connect()`") — there is no Docker-gated test for this row at all under the specified or any adjacent naming.

---

## Row 9.6 — digest files (grep: `digest`, capped at 2)

Grep matched 2 files: `daily-digest-worker.test.ts`, `digest-reflection.test.ts`. Both run (cap allowed 2, both are load-bearing to the row).

### `daily-digest-worker.test.ts`
**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:50:22
   Duration  5.70s (transform 1.73s, setup 0ms, import 1.81s, tests 1.25s, environment 0ms)
```
**✓ lines (3 of 3):**
```
 ✓ test/integration/daily-digest-worker.test.ts > Postgres integration — daily-digest worker > inserts a daily_digests row and claims the message_dispatches row when due 74ms
 ✓ test/integration/daily-digest-worker.test.ts > Postgres integration — daily-digest worker > does not duplicate the digest or re-send on a second sweep (UNIQUE + IS NULL guards) 1164ms
 ✓ test/integration/daily-digest-worker.test.ts > Postgres integration — daily-digest worker > skips a tenant whose digest_enabled is false (PgSettings read path) 8ms
```
Tenant greps: tenantB/etc no match. timezone/etc:
```
13: *   3. PgSettingsRepository.findByTenant returning the digest_enabled /
133:      `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone,
134:        owner_phone, digest_enabled, digest_time, digest_channel)
166:    expect(stored!.payload.timezone).toBe(TZ);
242:  it('skips a tenant whose digest_enabled is false (PgSettings read path)', async () => {
244:      `UPDATE tenant_settings SET digest_enabled = FALSE WHERE tenant_id = $1`,
```
listTenantIds: `[tenant.tenantId]` at lines 151, 196, 213, 253 (single-tenant stub each time).
Counts: pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=1. Audit grep: no match.

**Anomaly (significant):** The file's own header states it deliberately uses `emptyComputeDeps` — "stubbed compute deps (already proven by the unit test)" — because "This file drives runDailyDigestSweep with the production Pg repos for (1)-(3) [uniqueness / claim-guard / settings-read] ... so the SQL the worker actually executes in production is pinned end-to-end," explicitly leaving payload-content composition to the unit suite. A direct grep for `"wasn't sure"|"learned today"|notSure|learnedToday` in this file returns **no match** — the only payload fields asserted are `payload.date` and `payload.timezone`. This means **this specific file does NOT prove the two named narrative sections** ("what I wasn't sure about" / "what I learned today") at real Postgres — see `digest-reflection.test.ts` below for where that actually happens.

### `digest-reflection.test.ts`
**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  06:50:51
   Duration  4.92s (transform 352ms, setup 0ms, import 519ms, tests 140ms, environment 0ms)
```
**✓ lines (5 of 5):**
```
 ✓ test/integration/digest-reflection.test.ts > Postgres integration — digest reflection sections (N-005) > findConfidenceMarkedForDay returns only today's low/very_low proposals (JSONB predicate + window) 11ms
 ✓ test/integration/digest-reflection.test.ts > Postgres integration — digest reflection sections (N-005) > findAutonomousLaneApprovedForDay returns only today's eligible-lane proposals, tenant-scoped (D-015 amendment) 15ms
 ✓ test/integration/digest-reflection.test.ts > Postgres integration — digest reflection sections (N-005) > findAppliedInstructionsForDay returns only today's applied-instruction proposals, tenant-scoped (WS10) 14ms
 ✓ test/integration/digest-reflection.test.ts > Postgres integration — digest reflection sections (N-005) > estimate findByTenant sentFrom/sentTo scans today's sent estimates (sent_at range) 25ms
 ✓ test/integration/digest-reflection.test.ts > Postgres integration — digest reflection sections (N-005) > computeDigestPayload composes quotesSent + unsureAbout + learnedToday from the real DB (PRD §12) 43ms
```
Tenant greps: tenantB/etc:
```
198:    // RLS + explicit tenant_id predicate: another tenant never sees this row.
250:    // RLS + explicit tenant_id predicate: another tenant never sees this row.
```
timezone/etc:
```
49:// Tenant timezone UTC keeps the local-day window equal to the UTC calendar day.
168:      sourceContext: { autonomousLaneEvaluation: { eligible: true, threshold: 0.95 } },
173:      sourceContext: { autonomousLaneEvaluation: { eligible: false, reason: 'below_threshold' } },
179:      sourceContext: { autonomousLaneEvaluation: { eligible: true, threshold: 0.95 } },
195:      autonomousLaneEvaluation: { eligible: true, threshold: 0.95 },
294:      lessonType: 'labor_rate_changed',
298:      payload: { kind: 'labor_rate_changed', beforeCents: 12000, afterCents: 14500 },
312:      settingsRepo: { findByTenant: async () => ({ timezone: 'UTC' }) } as unknown as SettingsRepository,
```
listTenantIds: no match.
Counts: pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=3. Audit grep: no match.

**Row 9.6 summary anomaly:** The last test name literally reads "`computeDigestPayload composes quotesSent + unsureAbout + learnedToday from the real DB (PRD §12)`" — so the two named sections ARE real-DB proven, but in `digest-reflection.test.ts`, not in `daily-digest-worker.test.ts`. The PRD row's confirm text doesn't distinguish the two files; an auditor reading only `daily-digest-worker.test.ts` (the more obviously-named file) would wrongly conclude the sections aren't proven at all.

---

## Row 9.7 — `integration/weekly-feedback-builder.test.ts`, `integration/hfcr-weekly-send-worker.test.ts`, `integration/sweep-tenant-fanout.test.ts`

### `weekly-feedback-builder.test.ts`
**Summary:**
```
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  06:48:51
   Duration  2.60s (transform 86ms, setup 0ms, import 123ms, tests 59ms, environment 0ms)
```
**✓ lines (5 of 5):**
```
 ✓ test/integration/weekly-feedback-builder.test.ts > Postgres integration — weekly feedback builder > returns an all-zero snapshot for an empty tenant (every column resolves) 9ms
 ✓ test/integration/weekly-feedback-builder.test.ts > Postgres integration — weekly feedback builder > counts answered inbound calls inside the week window only 6ms
 ✓ test/integration/weekly-feedback-builder.test.ts > Postgres integration — weekly feedback builder > repeatCorrections (WS22) > omits repeatCorrections when no correctionRepo is passed (backward compatible) 4ms
 ✓ test/integration/weekly-feedback-builder.test.ts > Postgres integration — weekly feedback builder > repeatCorrections (WS22) > joins the real PgCorrectionRepository and reports total/repeats/rate 12ms
 ✓ test/integration/weekly-feedback-builder.test.ts > Postgres integration — weekly feedback builder > repeatCorrections (WS22) > omits repeatCorrections when the tenant has zero corrections this week ("omit if zero") 7ms
```
Tenant greps: all three no match. Counts: pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=3. Audit grep: no match.
(This file is also one of the ≤2 matches used for row 9.3 below — result not re-run, reused here.)

### `hfcr-weekly-send-worker.test.ts`
**Summary:**
```
 Test Files  1 passed (1)
      Tests  6 passed (6)
   Start at  06:49:03
   Duration  2.56s (transform 153ms, setup 0ms, import 219ms, tests 69ms, environment 0ms)
```
**✓ lines (6 of 6):**
```
 ✓ test/integration/hfcr-weekly-send-worker.test.ts > HFCR weekly send worker — integration > happy path: tenant with hands-free revenue gets one SMS and one hfcr_weekly_sends row 13ms
 ✓ test/integration/hfcr-weekly-send-worker.test.ts > HFCR weekly send worker — integration > idempotent on re-sweep: the second sweep finds the ledger row and sends nothing 8ms
 ✓ test/integration/hfcr-weekly-send-worker.test.ts > HFCR weekly send worker — integration > skips a tenant with no hands-free activity (sent=0; no ledger row written) 5ms
 ✓ test/integration/hfcr-weekly-send-worker.test.ts > HFCR weekly send worker — integration > skips a tenant with no owner phone configured (no SMS; no ledger row) 5ms
 ✓ test/integration/hfcr-weekly-send-worker.test.ts > HFCR weekly send worker — integration > send-then-record: a failing sendSms leaves NO ledger row behind so the next sweep retries the week 6ms
 ✓ test/integration/hfcr-weekly-send-worker.test.ts > HFCR weekly send worker — integration > tenant isolation: a ledger row for tenant A is invisible via tenant B findByWeek (RLS) 11ms
```
Tenant greps (raw):
```
12: *   2. RLS on hfcr_weekly_sends — cross-tenant findByWeek reads return
121: * cross-tenant null check below could pass with the policy dropped.
123: * predicate makes the policy itself the only thing gating cross-tenant reads.
310:    const tenantB = await createTestTenant(pool);
338:    const tenantIdsUnderB = await asTenant(pool, tenantB.tenantId, (client) =>
345:    expect(tenantIdsUnderA).not.toContain(tenantB.tenantId);
```
timezone/etc: no match. listTenantIds: `[tenantA.tenantId]` × 7 (lines 184,212,227,246,268,294,321).
Counts: pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=2. Audit grep: no match.

### `sweep-tenant-fanout.test.ts` (16 tests — T3/T4 proof for 9.1/9.2/9.4/9.6/9.7/3.5/7.10)
**Summary:**
```
 Test Files  1 passed (1)
      Tests  16 passed (16)
   Start at  06:49:14
   Duration  3.06s (transform 356ms, setup 0ms, import 467ms, tests 313ms, environment 0ms)
```
**✓ lines (16 of 16 — count matches PRD claim exactly):**
```
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — per-tenant sweep fan-out (T4) > runs on the REAL tenant selector and serves each tenant on its OWN timezone + digest_time 71ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — per-tenant sweep fan-out (T4) > keeps going when one tenant throws — the other tenants are still served 55ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — weekly-feedback sweep fan-out (T4) > serves each enabled tenant at its OWN stored address, greeting and opt-out 46ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — weekly-feedback sweep fan-out (T4) > keeps going when one tenant throws — the other tenants are still served 42ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > hold-reaper sweep > reaches every tenant through the real enumerator 4ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > hold-reaper sweep > keeps going when one tenant throws 4ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > estimate-reminder sweep > reaches every tenant through the real enumerator 3ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > estimate-reminder sweep > keeps going when one tenant throws 3ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > hfcr weekly-send sweep > reaches every tenant through the real enumerator 6ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > hfcr weekly-send sweep > keeps going when one tenant throws 3ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > google-reviews sweep > reaches every tenant through the real enumerator 4ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — enumerator-driven sweep fan-out (T4) > google-reviews sweep > keeps going when one tenant throws 3ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — cross-tenant query sweep fan-out (T4) > thank-you-SMS sweep > groups a multi-tenant result set and SENDS for each tenant under its own settings 36ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — cross-tenant query sweep fan-out (T4) > thank-you-SMS sweep > keeps going when one tenant throws — the other tenant is still SENT 19ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — cross-tenant query sweep fan-out (T4) > review-request sweep > enqueues across tenants from one cross-tenant query 6ms
 ✓ test/integration/sweep-tenant-fanout.test.ts > Postgres integration — cross-tenant query sweep fan-out (T4) > review-request sweep > keeps going when one row throws 6ms
```
Tenant greps (raw):
```
107: * the cross-tenant-query sweeps with distinct `completed_at` values — which
584: * cross-tenant SQL query, group the rows by tenant, and loop. They are
588: * the others. T4 for this shape reads: the cross-tenant query returns rows for
592:describe('Postgres integration — cross-tenant query sweep fan-out (T4)', () => {
769:      // The survivor's send is still correctly scoped after another tenant threw.
796:    it('enqueues across tenants from one cross-tenant query', async () => {
```
timezone/etc (raw, 13 lines) — notably includes:
```
7: *      Every other sweep test stubs `listTenantIds: async () => [oneId]`,
174:  it('runs on the REAL tenant selector and serves each tenant on its OWN timezone + digest_time', async () => {
175:    const chicago = await seedDigestTenant({ timezone: 'America/Chicago', digestTime: '18:00', enabled: true });
176:    const phoenix = await seedDigestTenant({ timezone: 'America/Phoenix', digestTime: '16:00', enabled: true });
```
listTenantIds: comment only (line 7, quoted above) — this file itself does NOT stub `listTenantIds: async () => [...]`; it drives the REAL tenant selector, per its own header note.
Counts: pool/testdb=5, InMemoryAuditRepository=0, createTestTenant(=6. Audit grep: no match.

**Anomalies:** None beyond what's already logged — 16/16 matches the PRD's explicit test-count claim exactly.

---

## Row 9.8 / 9.9 — `integration/correction-loop.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:49:27
   Duration  2.68s (transform 192ms, setup 0ms, import 270ms, tests 120ms, environment 0ms)
```

**✓ lines (4 of 4):**
```
 ✓ test/integration/correction-loop.test.ts > Postgres integration — correction loop (migration 180) > persists a lesson with real columns and a labor edit changes the next same-day draft 41ms
 ✓ test/integration/correction-loop.test.ts > Postgres integration — correction loop (migration 180) > FORCE RLS isolates correction_lessons across tenants 14ms
 ✓ test/integration/correction-loop.test.ts > Postgres integration — correction loop (migration 180) > executor path: a drafted-vs-executed payload edit cascades, and the next same-day draft reflects it 28ms
 ✓ test/integration/correction-loop.test.ts > Postgres integration — correction loop (migration 180) > findBySourceProposal returns every lesson a proposal recorded (drives undo) 17ms
```

**Tenant-grade greps (raw):**
```
5: *   - correction_lessons columns + FORCE RLS isolate across tenants.
145:  it('FORCE RLS isolates correction_lessons across tenants', async () => {
```
timezone/etc:
```
113:    expect(stored!.lessonType).toBe('labor_rate_changed');
116:    expect(stored!.payload).toMatchObject({ kind: 'labor_rate_changed', afterCents: 13500 });
205:    expect(drafts[0].lessonType).toBe('labor_rate_changed');
268:      new Set(['labor_rate_changed', 'banned_phrase']),
```
listTenantIds: no match.

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=3
**Audit grep (raw):**
```
124:    expect(applyAudits.some((a) => a.eventType === 'correction_lesson.applied')).toBe(true);
142:    expect(undoAudits.some((a) => a.eventType === 'correction_lesson.reverted')).toBe(true);
```
`PgAuditRepository` is imported and instantiated (`import { PgAuditRepository } from '../../src/audit/pg-audit'`, `auditRepo = new PgAuditRepository(pool)`), confirming both audit ends run through the production repo, as the row's own confirm note states.

**Anomalies:**
1. Row 9.9's acceptance criterion "a second undo is a no-op" is **not tested anywhere in this file** — `undoCorrectionLesson(` is called exactly twice in the whole file (line 133 in test 1, line 229 in test 3 inside a `for` loop over a list that runs it once per lesson); neither call site is followed by a second undo attempt on the same lesson.
2. "It drops from the day" (i.e., leaves `findAppliedForDay`) IS tested (test 3, lines 235-237: `expect(await lessonRepo.findAppliedForDay(...)).not.toContain(lesson.id)`), so that half of 9.9's acceptance is covered.
3. Test 1 ("persists a lesson...") actually covers BOTH 9.8 (apply → next-draft reflects it, `correction_lesson.applied` audit) AND half of 9.9 (undo restores prior value exactly + `correction_lesson.reverted` emitted) in a single test body — there is no test whose name mentions "undo" or "revert" explicitly.

---

## Row 9.10 — `integration/correction-repetition-meta-proposal.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:49:58
   Duration  3.46s (transform 651ms, setup 0ms, import 806ms, tests 82ms, environment 0ms)
```

**✓ lines (2 of 2):**
```
 ✓ test/integration/correction-repetition-meta-proposal.test.ts > Postgres integration — WS20 correction-repetition meta-proposal > countByTarget counts same-SKU part_price lessons (JSONB query, real columns) 10ms
 ✓ test/integration/correction-repetition-meta-proposal.test.ts > Postgres integration — WS20 correction-repetition meta-proposal > full loop: 3rd correction → meta-proposal → approve → executor updates catalog 40ms
```

**Tenant-grade greps (raw):** tenantB/etc: no match. timezone/etc: `99:    // Third same-SKU correction crosses the threshold.` (matches "threshold", not a tenant-config knob). listTenantIds: no match.

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=1
**Audit grep (raw):**
```
145:    expect(catAudits.some((a) => a.eventType === 'catalog_item.updated')).toBe(true);
147:    expect(propAudits.some((a) => a.eventType === 'proposal.executed')).toBe(true);
```

**Anomalies:** No cross-tenant test in this file for the meta-proposal path.

---

## Row 9.11 — `integration/accounting-sync.test.ts`

**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  6 passed (6)
   Start at  06:50:10
   Duration  2.98s (transform 167ms, setup 0ms, import 224ms, tests 473ms, environment 0ms)
```

**✓ lines (6 of 6):**
```
 ✓ test/integration/accounting-sync.test.ts > QuickBooks accounting sync — integration > happy path: sweep pushes one customer + one sales receipt; sync_log records both as success 51ms
 ✓ test/integration/accounting-sync.test.ts > QuickBooks accounting sync — integration > idempotent on re-sweep: a second sweep finds an existing successful push and skips QB entirely 63ms
 ✓ test/integration/accounting-sync.test.ts > QuickBooks accounting sync — integration > customer cache: two paid invoices for the same customer trigger ONE QuickBooks customer create, TWO sales receipts 65ms
 ✓ test/integration/accounting-sync.test.ts > QuickBooks accounting sync — integration > pagination: syncs EVERY paid invoice, not just one page (backlog > pageSize is never dropped) 113ms
 ✓ test/integration/accounting-sync.test.ts > QuickBooks accounting sync — integration > failure path: QuickBooks 4xx on the receipt is captured as a failed sync_log row (caller is never thrown to) 59ms
 ✓ test/integration/accounting-sync.test.ts > QuickBooks accounting sync — integration > tenant isolation: sync_log rows written under tenant A are invisible to tenant B (RLS) 95ms
```

**Tenant-grade greps (raw):**
```
17: *   3. RLS on accounting_sync_log — cross-tenant queries return zero.
20: *      cross-tenant lookup.
130: * itself the only thing gating cross-tenant reads.
445:    const tenantB = await createTestTenant(pool);
447:    const integrationB = await seedActiveIntegration(tenantB.tenantId);
449:    const { jobId: jobB } = await seedCustomerJobLocation(tenantB, 'IsoB');
451:    const invoiceB = await seedPaidInvoice(tenantB, jobB, 'INV-ISO-B');
476:    const entityIdsUnderB = await asTenant(pool, tenantB.tenantId, (client) =>
```
timezone/etc: no match. listTenantIds: no match.

**Counts:** pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=2
**Audit grep (raw):** no match

**Anomalies:** No audit-event assertion in this file (row's acceptance is about zero-QB-calls / pagination / RLS, none of which needs one — consistent).

---

## Row 9.3 — feedback/review-gate files (grep: `feedback|review-gat|rating`, capped at 2)

Grep matched 2 files: `feedback.test.ts`, `weekly-feedback-builder.test.ts` (the latter already run above for row 9.7 — result reused, not re-run).

### `feedback.test.ts`
**Summary (verbatim):**
```
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  06:51:08
   Duration  2.63s (transform 77ms, setup 0ms, import 111ms, tests 92ms, environment 0ms)
```
**✓ lines (5 of 5):**
```
 ✓ test/integration/feedback.test.ts > Postgres integration — feedback > findByToken resolves WITHOUT a tenant context (public link flow, by design) 9ms
 ✓ test/integration/feedback.test.ts > Postgres integration — feedback > findByJob is tenant-scoped — another tenant cannot see the request 9ms
 ✓ test/integration/feedback.test.ts > Postgres integration — feedback > round-trips expiresAt and transitions status via markSubmitted 7ms
 ✓ test/integration/feedback.test.ts > Postgres integration — feedback > rejects a duplicate response for the same request (unique(request_id) replay guard) 11ms
 ✓ test/integration/feedback.test.ts > Postgres integration — feedback > countByRatingInRange aggregates per-star within the window and is tenant-scoped 34ms
```
Tenant greps (raw): `7: *   - findByJob IS tenant-scoped (cross-tenant isolation)` and `83:  it('findByJob is tenant-scoped — another tenant cannot see the request', async () => {`. timezone/etc: no match. listTenantIds: no match.
Counts: pool/testdb=2, InMemoryAuditRepository=0, createTestTenant(=4. Audit grep: no match.

Direct check: `grep -n "reviewLink|rating: 3|rating: 5|routeReview|gate" test/integration/feedback.test.ts` found only the file header comment ("Docker-gated integration tests") and two incidental hits (`rating: 5` used as a constructor arg in an unrelated fixture, and the test name "countByRatingInRange..."). **No test asserts the 3★-private / 5★-public routing behavior** that is row 9.3's actual acceptance criterion.

**Row 9.3 anomaly (confirms PRD claim):** This is purely a repository-level test file (`findByToken`, `findByJob`, `markSubmitted`, duplicate-response guard, `countByRatingInRange`). **No Docker-gated test exercises the rating-based gate** (rating 3 → no review link / rating 5 → link returned) anywhere in `feedback.test.ts` or `weekly-feedback-builder.test.ts`. This corroborates the row's own confirm note that the gate is "proven only by a mocked-repo route test."

---

# Summary table

| Row | File | Tests | Pass/Fail | Re-run needed |
|---|---|---|---|---|
| 8.1 | draft-invoice-execution.test.ts | 9 | 9/9 pass | No |
| 8.3 | update-job-execution.test.ts | 11 | 11/11 pass | No |
| 8.4 | payment-credit-guards.test.ts | 8 | 8/8 pass | No |
| 8.4 | invoice-webhook-paid.test.ts | 3 | 3/3 pass | No |
| 8.6 | payment-concurrent-credit.test.ts | 1 | 1/1 pass | No |
| 8.6 | payment-duplicate-race.test.ts | 3 | 3/3 pass | No |
| 8.6 | payment-reversal-concurrent.test.ts | 3 | 3/3 pass | No |
| 8.8 | payment-refunds.test.ts | 5 | 5/5 pass | No |
| 8.9 | payment-reminder-dedup.test.ts | 5 | 5/5 pass | No |
| 8.10 | late-fee-idempotency.test.ts | 1 | 1/1 pass | No |
| 8.10 | voice-collections-execution.test.ts | 3 | 3/3 pass | No |
| 9.1 | thank-you-sms-worker.test.ts | 8 | 8/8 pass | No |
| 9.2 | review-request-sweep.test.ts | 2 | 2/2 pass | No |
| 9.3 | feedback.test.ts | 5 | 5/5 pass | No |
| 9.3 | weekly-feedback-builder.test.ts (reused from 9.7) | 5 | 5/5 pass | No |
| 9.4 | google-reviews-worker.test.ts | 7 | 7/7 pass | No |
| 9.5 | (none — zero grep matches) | 0 | n/a | n/a |
| 9.6 | daily-digest-worker.test.ts | 3 | 3/3 pass | No |
| 9.6 | digest-reflection.test.ts | 5 | 5/5 pass | No |
| 9.7 | weekly-feedback-builder.test.ts | 5 | 5/5 pass | No |
| 9.7 | hfcr-weekly-send-worker.test.ts | 6 | 6/6 pass | No |
| 9.7 | sweep-tenant-fanout.test.ts | 16 | 16/16 pass | No |
| 9.8/9.9 | correction-loop.test.ts | 4 | 4/4 pass | No |
| 9.10 | correction-repetition-meta-proposal.test.ts | 2 | 2/2 pass | No |
| 9.11 | accounting-sync.test.ts | 6 | 6/6 pass | No |

**Grand total: 22 distinct files run (23 vitest invocations counting the deliberate re-run of update-job-execution.test.ts), 121 tests executed (counting weekly-feedback-builder.test.ts once), 0 failures, 0 re-runs required.**

## Per-file pool/InMemory/createTestTenant/audit-grep counts

| File | pool/testdb | InMemoryAuditRepository | createTestTenant( | audit expect/audit_events/eventType/event_type hits |
|---|---|---|---|---|
| draft-invoice-execution.test.ts | 6 | 0 | 5 | 7 |
| update-job-execution.test.ts | 3 | 0 | 5 | 4 |
| payment-credit-guards.test.ts | 2 | 0 | 1 | 0 |
| invoice-webhook-paid.test.ts | 2 | 0 | 1 | 0 |
| payment-concurrent-credit.test.ts | 2 | 0 | 1 | 0 |
| payment-duplicate-race.test.ts | 3 | 0 | 1 | 0 |
| payment-reversal-concurrent.test.ts | 2 | 0 | 1 | 0 |
| payment-refunds.test.ts | 3 | 0 | 3 | 0 |
| payment-reminder-dedup.test.ts | 3 | 0 | 2 | 1 |
| late-fee-idempotency.test.ts | 3 | 0 | 1 | 0 |
| voice-collections-execution.test.ts | 2 | 0 | 4 | 4 |
| thank-you-sms-worker.test.ts | 2 | 0 | 1 | 1 |
| review-request-sweep.test.ts | 2 | 0 | 2 | 0 |
| google-reviews-worker.test.ts | 2 | 0 | 3 | 0 |
| daily-digest-worker.test.ts | 2 | 0 | 1 | 0 |
| digest-reflection.test.ts | 2 | 0 | 3 | 0 |
| weekly-feedback-builder.test.ts | 2 | 0 | 3 | 0 |
| hfcr-weekly-send-worker.test.ts | 2 | 0 | 2 | 0 |
| sweep-tenant-fanout.test.ts | 5 | 0 | 6 | 0 |
| correction-loop.test.ts | 2 | 0 | 3 | 2 |
| correction-repetition-meta-proposal.test.ts | 2 | 0 | 1 | 2 |
| accounting-sync.test.ts | 2 | 0 | 2 | 0 |
| feedback.test.ts | 2 | 0 | 4 | 0 |

**InMemoryAuditRepository never appears in any of the 23 files** — every file that touches audit in this set goes through the real `PgAuditRepository`/`audit_events` table (confirmed explicitly for correction-loop.test.ts via `PgAuditRepository` import).

## Anomalies (consolidated)

1. **8.3** — Both passing runs of "a completed transition stamps completed_at AND runs completion effects" log `"completion effects (auto-invoice + milestone minting) were SKIPPED — completion deps not wired"`. The test still passes (it isn't asserting the auto-invoice draft itself in that harness path).
2. **8.4** — Neither `payment-credit-guards.test.ts` nor `invoice-webhook-paid.test.ts` has any cross-tenant or audit-event assertion.
3. **8.6** — The specified grep (`payment.*(concurr|race|lost|overpay|clamp)|concurrent.*payment`) matches only **3 files / 7 tests**, not the "five Docker-gated files, nine tests" the PRD row claims. `deposit-concurrent-credit.test.ts` and `deposit-credit-atomic.test.ts` are plausible candidates for the missing count but don't match the given pattern (no "payment" in filename) and were not run.
4. **8.8** — No audit-event assertion despite the row's "status is never flipped" claim relying on an audit trail concept.
5. **8.9** — Directly confirmed: the ledger key under real-DB test is `manualReminderStepKey(firstProposalId)` (a `manual:<proposalId>` shape), and there is no cadence-key (`3:sms`/`7:sms`/`14:sms`) test or 23505-on-duplicate-`7:sms` test anywhere in `payment-reminder-dedup.test.ts`. Fully corroborates the PRD's "largest single overclaim" flag.
6. **9.1** — No consent-gate ("block mode") test in the D-lane file; row attributes that coverage to the separate U-lane file (`workers/thank-you-sms-worker.test.ts`), out of this run's scope.
7. **9.3** — Confirmed: neither `feedback.test.ts` nor `weekly-feedback-builder.test.ts` has any test asserting rating-based review-link routing (3★ private / 5★ public). Only repository CRUD/aggregation is DB-proven.
8. **9.5** — Zero files match `service-credit` (or any close variant checked); nothing to run. Confirms the row's "smoke test that stubs `pool.connect()`" claim — there is no Docker-gated proof at all.
9. **9.6** — `daily-digest-worker.test.ts` explicitly stubs compute deps to empty and, by its own header comment, defers payload-content proof elsewhere; it asserts only `payload.date`/`payload.timezone`, NOT the two named sections. The actual real-DB proof of "what I wasn't sure about" / "what I learned today" (`unsureAbout`/`learnedToday`) lives in `digest-reflection.test.ts`'s test named `"computeDigestPayload composes quotesSent + unsureAbout + learnedToday from the real DB (PRD §12)"`. The PRD row's confirm text doesn't disambiguate which file carries this proof.
10. **9.7** — `sweep-tenant-fanout.test.ts` gave exactly 16/16 tests, matching the PRD's explicit count claim precisely — no discrepancy.
11. **9.8/9.9** — `correction-loop.test.ts`'s acceptance clause "a second undo is a no-op" (row 9.9) has **no corresponding test**; `undoCorrectionLesson(` is called exactly twice in the file, neither followed by a repeat call on the same lesson. The "drops from the day" half of 9.9 IS covered (line ~235-237).
12. No `InMemoryAuditRepository` usage found in any of the 23 files examined — audit assertions that do exist go through the real Postgres-backed repository/table.
