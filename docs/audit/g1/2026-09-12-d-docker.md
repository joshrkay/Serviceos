# G1 entry audit — §8.4 Dispatch / §8.7 Quote — Docker/integration lane (Row D)

Worktree: /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane
Branch: lane/rung-map-env @ 2c2aa6d90 (= origin/main)
node_modules installed; Docker via colima (DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock, TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock, RLS_RUNTIME_ROLE=true).

All commands run from `packages/api`, one file at a time. No re-runs were needed — every run below passed on the first attempt (0 failures across the whole lane).

Disk checks (df -h / | tail -1):
- baseline (before any run): `/dev/disk3s1s1   228Gi    12Gi   2.7Gi    82%    455k   29M    2%   /`
- after run 3 (dispatch.test.ts, dispatch-technician-day-window.test.ts, technician-location-authz.test.ts): `/dev/disk3s1s1   228Gi    12Gi   2.7Gi    82%    455k   29M    2%   /`
- after run 6 (tech-status-sms.test.ts, draft-estimate-execution.test.ts, mms-to-quote.int.test.ts): `/dev/disk3s1s1   228Gi    12Gi   2.7Gi    82%    455k   28M    2%   /`
- after run 9 (estimates.test.ts, estimate-phases.test.ts, estimate-nudge.test.ts): `/dev/disk3s1s1   228Gi    12Gi   2.7Gi    82%    455k   28M    2%   /`
- after run 12 (en-route-voice.test.ts, beta-verify-schema-fixes.test.ts, auto-pick-appointment-920.test.ts): `/dev/disk3s1s1   228Gi    12Gi   2.7Gi    82%    455k   28M    2%   /`

Free space stayed at ~2.7Gi throughout (never approached the 600 MiB stop threshold).

---

## 4.1 — integration/dispatch.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/dispatch.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:41:56
   Duration  2.86s (transform 148ms, setup 0ms, import 202ms, tests 125ms, environment 0ms)
```

**✓/✗ lines (2 total, 0 failures):**
```
 ✓ test/integration/dispatch.test.ts > Postgres integration — dispatch > Dispatch board > queries dispatch board data for a date 90ms
 ✓ test/integration/dispatch.test.ts > Postgres integration — dispatch > tenant isolation > rejects cross-tenant access to board data 4ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" dispatch.test.ts
95:    it('rejects cross-tenant access to board data', async () => {
96:      const otherTenant = await createTestTenant(pool);
100:        otherTenant.tenantId,

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" dispatch.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" dispatch.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" dispatch.test.ts  → 2
grep -c "InMemoryAuditRepository" dispatch.test.ts                                       → 0
grep -c "createTestTenant(" dispatch.test.ts                                             → 2
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" dispatch.test.ts       → 0
```

**Anomalies:** No audit-assertion hits at all for the story ("one board showing today") — 4.1's acceptance criterion doesn't mention audit, so 0 is expected, not a defect signal by itself. Cross-tenant negative is present and real (createTestTenant + otherTenant.tenantId passed into the board query).

---

## 4.4 — integration/dispatch-technician-day-window.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/dispatch-technician-day-window.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:42:04
   Duration  2.52s (transform 151ms, setup 0ms, import 201ms, tests 79ms, environment 0ms)
```

**✓/✗ lines (3 total, 0 failures):**
```
 ✓ test/integration/dispatch-technician-day-window.test.ts > Postgres integration — technician day window (tenant tz) > resolves the tenant timezone from settings 3ms
 ✓ test/integration/dispatch-technician-day-window.test.ts > Postgres integration — technician day window (tenant tz) > includes a 23:00-local appointment on the tenant date (not the next UTC day) 21ms
 ✓ test/integration/dispatch-technician-day-window.test.ts > Postgres integration — technician day window (tenant tz) > the OLD naive-UTC window would have dropped it (proves the fix matters) 3ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" dispatch-technician-day-window.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" dispatch-technician-day-window.test.ts
16: * must bucket the day in the TENANT timezone, not hardcoded UTC.
32:  const timezone = 'America/Los_Angeles';
47:    // Seed the tenant timezone the way the route resolves it (settings repo).
49:    await settingsRepo.update(tenant.tenantId, { timezone });
104:      timezone,
128:  it('resolves the tenant timezone from settings', async () => {
130:    expect(settings?.timezone).toBe(timezone);
135:    const { start, end } = getDayBoundaries(dateStr, settings?.timezone);

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" dispatch-technician-day-window.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" dispatch-technician-day-window.test.ts  → 2
grep -c "InMemoryAuditRepository" dispatch-technician-day-window.test.ts                                       → 0
grep -c "createTestTenant(" dispatch-technician-day-window.test.ts                                             → 1
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" dispatch-technician-day-window.test.ts       → 0
```

**Anomalies:** No cross-tenant grep hit in this file — the cross-tenant half of 4.4's acceptance criterion ("a tech of tenant B is not submittable by tenant A") is NOT covered here; it lives entirely in technician-location-authz.test.ts (see below). This file only proves the 23:00-local timezone-bucketing half.

---

## 4.4 — integration/technician-location-authz.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/technician-location-authz.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  6 passed (6)
   Start at  06:42:12
   Duration  4.35s (transform 103ms, setup 0ms, import 135ms, tests 42ms, environment 0ms)
```

**✓/✗ lines (6 total, 0 failures):**
```
 ✓ test/integration/technician-location-authz.test.ts > PgTechnicianLocationAuthorizer (real Postgres) > owner may submit for a technician by canonical users.id 25ms
 ✓ test/integration/technician-location-authz.test.ts > PgTechnicianLocationAuthorizer (real Postgres) > owner may also submit by the legacy clerk_user_id (back-compat) 4ms
 ✓ test/integration/technician-location-authz.test.ts > PgTechnicianLocationAuthorizer (real Postgres) > rejects an id that is not a technician (e.g. the owner themselves) 3ms
 ✓ test/integration/technician-location-authz.test.ts > PgTechnicianLocationAuthorizer (real Postgres) > rejects an unknown id 3ms
 ✓ test/integration/technician-location-authz.test.ts > PgTechnicianLocationAuthorizer (real Postgres) > does not leak across tenants — a technician of tenant B is not submittable by tenant A 5ms
 ✓ test/integration/technician-location-authz.test.ts > PgTechnicianLocationAuthorizer (real Postgres) > technician self-submit uses their canonical users.id (no DB needed) 1ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" technician-location-authz.test.ts
76:  it('does not leak across tenants — a technician of tenant B is not submittable by tenant A', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" technician-location-authz.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" technician-location-authz.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" technician-location-authz.test.ts  → 2
grep -c "InMemoryAuditRepository" technician-location-authz.test.ts                                       → 0
grep -c "createTestTenant(" technician-location-authz.test.ts                                             → 7
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" technician-location-authz.test.ts       → 0
```

**Anomalies:** Cross-tenant negative for 4.4 lives here (the exact acceptance-criterion wording is quoted verbatim in the test name). No audit assertions — this is an authorizer-only test, doesn't touch audit rows.

---

## 4.8 — integration/tech-status-sms.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/tech-status-sms.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:42:23
   Duration  4.75s (transform 954ms, setup 0ms, import 1.52s, tests 653ms, environment 0ms)
```

**✓/✗ lines (3 total, 0 failures):**
```
 ✓ test/integration/tech-status-sms.test.ts > Postgres integration — tech-status "I'm out" SMS (U1 / P6-028) > routes a verified tech OUT → unavailable block + reschedule proposal + audit 350ms
 ✓ test/integration/tech-status-sms.test.ts > Postgres integration — tech-status "I'm out" SMS (U1 / P6-028) > is idempotent — a second OUT the same tenant-local day is a no-op 83ms
 ✓ test/integration/tech-status-sms.test.ts > Postgres integration — tech-status "I'm out" SMS (U1 / P6-028) > anti-spoof — an OUT from an unregistered number is not actioned 7ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" tech-status-sms.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" tech-status-sms.test.ts
73:      timezone: 'America/New_York',
139:      timezone: 'America/New_York',

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" tech-status-sms.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" tech-status-sms.test.ts  → 2
grep -c "InMemoryAuditRepository" tech-status-sms.test.ts                                       → 0
grep -c "createTestTenant(" tech-status-sms.test.ts                                             → 1
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" tech-status-sms.test.ts       → 1
```

**Anomalies:** 3 tests total, but 4.8's acceptance criterion asks for "a reschedule proposal **per appointment**" (plural, one per affected customer) — the test names show one "routes a verified tech OUT" test, not one per multi-appointment scenario; whether the single test seeds >1 appointment for that tech is not established by the test names/greps alone. No cross-tenant grep hit in this file. No `listTenantIds` match.

---

## 7.1 — integration/draft-estimate-execution.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/draft-estimate-execution.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:42:36
   Duration  3.42s (transform 671ms, setup 0ms, import 908ms, tests 140ms, environment 0ms)
```

**Non-✓/✗ stderr/log line emitted before the test lines (verbatim):**
```
{"level":"warn","message":"resolveAutoApproveThreshold called with no supervision signal — falling through to the permissive LEGACY_AUTO_APPROVE_THRESHOLD (0.9) with no unsupervised hard-block. If this caller can create an autonomous, capture-class proposal, thread a real supervisorPresent (and, where available, supervisorMode) — see workers/voice-action-router.ts or routes/assistant.ts for the pattern. Logged once per proposal type/surface (key: draft_estimate::unknown).","timestamp":"2026-09-12T13:42:39.931Z","service":"proposals.auto-approve","environment":"test"}
```

**✓/✗ lines (4 total, 0 failures):**
```
 ✓ test/integration/draft-estimate-execution.test.ts > Postgres integration — voice draft_estimate → approve → execute → persist + audit > persists an estimate row with catalog-grounded integer-cents lines 30ms
 ✓ test/integration/draft-estimate-execution.test.ts > Postgres integration — voice draft_estimate → approve → execute → persist + audit > emits exactly one estimate.created audit event 17ms
 ✓ test/integration/draft-estimate-execution.test.ts > Postgres integration — voice draft_estimate → approve → execute → persist + audit > cross-tenant negative: another tenant cannot read the estimate through the scoped repo 20ms
 ✓ test/integration/draft-estimate-execution.test.ts > Postgres integration — voice draft_estimate → approve → execute → persist + audit > create_change_order: persists an estimate row flagged is_change_order and linked to the EXISTING job 16ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" draft-estimate-execution.test.ts
264:  it('cross-tenant negative: another tenant cannot read the estimate through the scoped repo', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" draft-estimate-execution.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" draft-estimate-execution.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" draft-estimate-execution.test.ts  → 2
grep -c "InMemoryAuditRepository" draft-estimate-execution.test.ts                                       → 0
grep -c "createTestTenant(" draft-estimate-execution.test.ts                                             → 2
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" draft-estimate-execution.test.ts       → 4
```

**Anomalies:** The auto-approve-threshold warn log fires on every run — logged, not failing, but flags that this path executes with "no supervision signal" in test — worth flagging given 7.2's confidence-cap gap noted in the PRD row text. Real Postgres audit assertions present (4 hits) and a real cross-tenant negative — matches PRD's "plus its audit event" claim for 7.1.

---

## 7.1 — integration/mms-to-quote.int.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/mms-to-quote.int.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:42:45
   Duration  2.89s (transform 320ms, setup 0ms, import 418ms, tests 109ms, environment 0ms)
```

**✓/✗ lines (3 total, 0 failures):**
```
 ✓ test/integration/mms-to-quote.int.test.ts > Postgres integration — MMS-to-quote (U2) > unknown customer MMS → draft_estimate proposal persisted with tenant_id + audit 39ms
 ✓ test/integration/mms-to-quote.int.test.ts > Postgres integration — MMS-to-quote (U2) > known customer MMS → drafts against the resolved customer (no duplicate created) 21ms
 ✓ test/integration/mms-to-quote.int.test.ts > Postgres integration — MMS-to-quote (U2) > ambiguous sender (shared phone) → clarification proposal, no draft_estimate 19ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" mms-to-quote.int.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" mms-to-quote.int.test.ts
131:    // the auto-approve threshold, so it lands in the owner queue.

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" mms-to-quote.int.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" mms-to-quote.int.test.ts  → 2
grep -c "InMemoryAuditRepository" mms-to-quote.int.test.ts                                       → 0
grep -c "createTestTenant(" mms-to-quote.int.test.ts                                             → 1
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" mms-to-quote.int.test.ts       → 1
```

**Anomalies:** No cross-tenant grep hit in this file at all (no createTestTenant used more than once, no otherTenant). 7.1's acceptance criterion doesn't require cross-tenant proof, only persistence + audit, which this file has (1 audit-assertion hit).

---

## 7.3 (D half) — packages/api/test/integration/estimates.test.ts

`find packages/api/test/integration -name 'estimates.test.ts'` → found at `packages/api/test/integration/estimates.test.ts` (i.e. it IS under test/integration), so it was run.

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/estimates.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  10 passed (10)
   Start at  06:42:54
   Duration  2.71s (transform 148ms, setup 0ms, import 197ms, tests 134ms, environment 0ms)
```

**✓/✗ lines (10 total, 0 failures):**
```
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > EE-1 — good-better-best > persists all tier rows and headlines at the default selection 10ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > EE-4 — line image_file_id > persists and round-trips a line image_file_id (real column) 6ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > CRUD > creates estimate and retrieves via findById 6ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > CRUD > filters by jobIds (customer filter) against the real DB 18ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > CRUD > updates estimate and reflects in findById 9ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > CRUD > finds estimates by tenant 7ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > tenant isolation > rejects cross-tenant access 6ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > RV-042 — acceptance invalidation clears accepted_selection to SQL NULL > updateEstimate(invalidateAcceptance) → accepted_selection IS NULL, acceptance fields cleared 18ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > U-G — estimate_line_items.pricing_source round-trips and is CHECK-constrained > persists every valid pricingSource and reads it back through the repo mapper 8ms
 ✓ test/integration/estimates.test.ts > Postgres integration — estimates > U-G — estimate_line_items.pricing_source round-trips and is CHECK-constrained > the DB CHECK rejects an invalid pricing_source on a raw UPDATE 11ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" estimates.test.ts
254:    it('rejects cross-tenant access', async () => {
255:      const otherTenant = await createTestTenant(pool);
275:      const found = await estimateRepo.findById(otherTenant.tenantId, estimate.id);

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" estimates.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" estimates.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" estimates.test.ts  → 2
grep -c "InMemoryAuditRepository" estimates.test.ts                                       → 0
grep -c "createTestTenant(" estimates.test.ts                                             → 2
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" estimates.test.ts       → 0
```

**Anomalies:** The "U-G — estimate_line_items.pricing_source" describe block (test names literally contain "pricing_source" and "DB CHECK … raw UPDATE") matches the PRD's cited `estimates.test.ts -t "pricing_source"` for 7.3's D leg — a DB CHECK-constraint test on a raw UPDATE is present here, confirming that half of 7.3's acceptance criterion. No audit-assertion grep hits (7.3's acceptance criterion doesn't require an audit event).

---

## 7.4 / 7.5 / 7.6 / 7.8 / 7.9 — integration/estimate-phases.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/estimate-phases.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  11 passed (11)
   Start at  06:43:02
   Duration  3.90s (transform 197ms, setup 0ms, import 254ms, tests 350ms, environment 0ms)
```

**✓/✗ lines (11 total, 0 failures):**
```
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 1 — soft delete > hides from reads but keeps the row with deleted_at set 26ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 1 — soft delete > refuses to delete an accepted estimate 17ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 1 — clone > persists a fresh draft with reset state and copied tier metadata 23ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 1 — auto-expiry worker > flips a sent estimate past valid_until to expired in the DB 22ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 2 — convert to invoice > creates a linked invoice, is idempotent, and credits a paid deposit 49ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 3 — good-better-best public approval > recomputes the accepted total from the selection and persists accepted_selection 37ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 3 — good-better-best public approval > after_approval — accepting writes the deposit onto the job and the view is payable 39ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 2 — convert race > two concurrent converts yield one invoice (unique-index backstop) 30ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 3 — one accepted estimate per job (atomic) > two concurrent approvals on the same job yield exactly one accepted 39ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 4 — public view reflects validity expiry > marks a lapsed sent estimate as expired and non-actionable on GET 18ms
 ✓ test/integration/estimate-phases.test.ts > Postgres integration — estimate phases (real DB effects) > Phase 4 — validity expiry precedence > expires (and refuses) a decline on an estimate past valid_until 18ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" estimate-phases.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" estimate-phases.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" estimate-phases.test.ts
185:        listTenantIds: async () => [tenant.tenantId],
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" estimate-phases.test.ts  → 2
grep -c "InMemoryAuditRepository" estimate-phases.test.ts                                       → 10
grep -c "createTestTenant(" estimate-phases.test.ts                                             → 1
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" estimate-phases.test.ts       → 0
```

**Anomalies:** This confirms the PRD's flagged gap directly: `InMemoryAuditRepository` appears 10 times in this file (the audit leg for 7.4/7.5/7.6/7.8/7.9 is in-memory, not real-DB), and there are zero cross-tenant grep hits and zero raw audit-assertion greps despite 11 real-Postgres tests passing for the write/race/expiry mechanics themselves. `listTenantIds: async () => [tenant.tenantId]` appears once (a fake/stub tenant lister feeding the auto-expiry worker its tenant list — single-tenant stub, not a real multi-tenant listing). No test name mentions "signature" (relevant to 7.6's flagged gap) or "conflict"/"settlement" mapping (relevant to 7.8's flagged gap) — those are not directly exercised by name in this file's test titles.

---

## 7.10 — integration/estimate-nudge.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/estimate-nudge.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  16 passed (16)
   Start at  06:43:13
   Duration  3.65s (transform 778ms, setup 0ms, import 1.04s, tests 271ms, environment 0ms)
```

**✓/✗ lines (16 total, 0 failures):**
```
 ✓ test/integration/estimate-nudge.test.ts > dispatchEstimateNudge (integration) — claim-before-send against real Postgres > happy path: sends once and persists reminder_count/last_reminder_at on the real row 18ms
 ✓ test/integration/estimate-nudge.test.ts > dispatchEstimateNudge (integration) — claim-before-send against real Postgres > reclaims a stale claim for the same occurrence (crash between claim and send) 14ms
 ✓ test/integration/estimate-nudge.test.ts > dispatchEstimateNudge (integration) — claim-before-send against real Postgres > Codex P2 (PR #705) — a "sent" claim for the same occurrence is RECONCILED (no resend, no throw, reminder_count finished) — crash between send and mark 13ms
 ✓ test/integration/estimate-nudge.test.ts > dispatchEstimateNudge (integration) — claim-before-send against real Postgres > two concurrent dispatchEstimateNudge calls for the same estimate: exactly one send, cadence advances once 19ms
 ✓ test/integration/estimate-nudge.test.ts > dispatchEstimateNudge (integration) — claim-before-send against real Postgres > repeatability: a second nudge (occurrence 2) after the first completes is a fresh, independent claim 20ms
 ✓ test/integration/estimate-nudge.test.ts > dispatchEstimateNudge (integration) — claim-before-send against real Postgres > Codex P1 (PR #705): a revised estimate (version bump + reminderCount reset) is re-notified — occurrence 1 does not collide with the prior revision's tombstone 20ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > the ILIKE display-text search CANNOT reach this estimate from the spoken name — only the customer→jobs→estimates traversal can 11ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > text fallback survives: a spoken ESTIMATE NUMBER with no resolved customer still lifts the gate 4ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > dispatches a real message_dispatches row for the estimate 3ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > emits a proposal.executed audit event with actor attribution 1ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > also emits the estimate.reminder_sent audit event (dispatchEstimateNudge bookkeeping) 1ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > 48h cooldown holds under voice: a second voice-approved nudge inside the window sends nothing and records the suppressed outcome 33ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — voice send_estimate_nudge ("Nudge the Khan estimate") → approve → execute → dispatch + audit; 48h cooldown holds under voice > cross-tenant negative: the dispatch row and the estimate are invisible to another tenant 5ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — a spoken SURNAME resolves its customer (CUSTOMER_REF_INTENTS) > spoken "Khan" resolves to "Aisha Khan" — the surname is the LAST word of the display name 2ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — a spoken SURNAME resolves its customer (CUSTOMER_REF_INTENTS) > a prefix-sharing customer is NOT reachable from the surname — "Khanna" is a different person 2ms
 ✓ test/integration/estimate-nudge.test.ts > Postgres integration — a spoken SURNAME resolves its customer (CUSTOMER_REF_INTENTS) > never resolves a spoken surname across tenants 3ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" estimate-nudge.test.ts
682:  it('cross-tenant negative: the dispatch row and the estimate are invisible to another tenant', async () => {
763:  it('never resolves a spoken surname across tenants', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" estimate-nudge.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" estimate-nudge.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" estimate-nudge.test.ts  → 4
grep -c "InMemoryAuditRepository" estimate-nudge.test.ts                                       → 2
grep -c "createTestTenant(" estimate-nudge.test.ts                                             → 5
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" estimate-nudge.test.ts       → 6
```

**Anomalies:** PRD calls this "nine real-DB tests" but the file itself runs 16 tests (matches all 5 stacked rows 7.4/no wait — this is 7.10 only). `InMemoryAuditRepository` appears twice in this nominally "real-DB" nudge file despite the PRD's characterization — worth a closer look at what those two references gate (present alongside 6 real audit-assertion hits and 4 real-pool references, so the file is mixed, not purely in-memory).

---

## 4.2 — createSchedulingProposal integration coverage (list only, per instructions — NOT run)

**Command:**
```
grep -rln "createSchedulingProposal" packages/api/test/integration
```

**Result:** no output (exit code 1 / no matches).

**Confirms the PRD claim:** zero files under `packages/api/test/integration` reference `createSchedulingProposal` — no integration test file was found, so none was run.

---

## 4.5 / 4.6 — "on my way" / "running late" candidate search

**Command:**
```
grep -rln -i "on my way\|on_my_way\|en_route\|running.late\|notify_delay" packages/api/test/integration | head -4
```

**Result (4 files):**
```
packages/api/test/integration/beta-verify-schema-fixes.test.ts
packages/api/test/integration/entity-resolution.test.ts
packages/api/test/integration/en-route-voice.test.ts
packages/api/test/integration/auto-pick-appointment-920.test.ts
```

Ran 3 of the 4 (per instructions, "run up to 3"). Selection was based on a quick `grep -nio` keyword-hit check before running (see below); `en-route-voice.test.ts` was overwhelmingly the primary hit (20 keyword matches vs. 1 each in the others) and was run first; `beta-verify-schema-fixes.test.ts` (1 hit: "running-late") and `auto-pick-appointment-920.test.ts` (1 hit: "notify_delay") were run to cover the other two keyword categories; `entity-resolution.test.ts` (1 hit: "notify_delay", same keyword as auto-pick-appointment-920.test.ts) was NOT run, to stay at 3.

Pre-run keyword-hit check (`grep -nio "on my way\|on_my_way\|en_route\|running.late\|notify_delay" <file>`):
- beta-verify-schema-fixes.test.ts: 1 hit — `14:running-late`
- entity-resolution.test.ts: 1 hit — `1506:notify_delay`
- en-route-voice.test.ts: 20 hits (on my way ×5, en_route ×15)
- auto-pick-appointment-920.test.ts: 1 hit — `27:notify_delay`

### en-route-voice.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/en-route-voice.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:43:25
   Duration  4.40s (transform 1.25s, setup 0ms, import 1.81s, tests 284ms, environment 0ms)
```

**✓/✗ lines (7 total, 0 failures):**
```
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > the seeded job is an ORDINARY one — "the Garcia job" is unreachable from its summary, so only the customer traversal can answer it 5ms
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > AC-4.1/4.2: a voice-triggered en-route emits the audit event (TECH actor) + a customer ETA SMS dispatch row 42ms
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > AC-4.3: idempotence — a second "on my way" in-window does not double-text (same delay_notice_state row, no duplicate audit event count explosion) 25ms
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > AC-4.4: cross-tenant negative — a tenant B technician resolves to "no upcoming appointment", never tenant A's row 10ms
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > #847 phone leg: a Gather session actor fires the audited act through the phone surface (real rows) 45ms
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > #847 phone leg: a non-technician actor is refused against the REAL users row and nothing fires 23ms
 ✓ test/integration/en-route-voice.test.ts > Integration — "on my way" by voice (real Postgres) > #847 chat leg: the auth subject (Clerk id) resolves through REAL users rows and fires the act 62ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" en-route-voice.test.ts
5: * test/integration/voice-inbound-appointment.test.ts; cross-tenant pattern
27: *      leaking or acting on another tenant's row — no audit event, no
28: *      delay_notice_state row is created for the cross-tenant attempt.
91:  let tenantB: TestTenant;
125:    tenantB = await createTestTenant(pool);
126:    // Tenant B needs a zone too, or the cross-tenant negative below would
129:    await ensureTenantSettings(tenantB.tenantId, settingsRepo);
130:    await settingsRepo.upsertIdentityFields(tenantB.tenantId, { timezone: 'America/Chicago' });
140:    techB = await insertTechnician(pool, tenantB.tenantId, {
343:  it('AC-4.4: cross-tenant negative — a tenant B technician resolves to "no upcoming appointment", never tenant A\'s row', async () => {
357:      { tenantId: tenantB.tenantId, recordingId: 'rec-cross-tenant', jobReference: 'the Garcia job' },
366:    const crossTenantRead = await appointmentRepo.findById(tenantB.tenantId, appointmentId);
373:      [tenantB.tenantId],

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" en-route-voice.test.ts
124:    await settingsRepo.upsertIdentityFields(tenantA.tenantId, { timezone: 'America/Chicago' });
127:    // pass for the WRONG reason — declining on a missing timezone rather than
130:    await settingsRepo.upsertIdentityFields(tenantB.tenantId, { timezone: 'America/Chicago' });
204:      timezone: 'America/Chicago',
391:    await settingsRepo.upsertIdentityFields(t.tenantId, { timezone: 'America/Chicago' });
450:      timezone: 'America/Chicago',

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" en-route-voice.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" en-route-voice.test.ts  → 2
grep -c "InMemoryAuditRepository" en-route-voice.test.ts                                       → 0
grep -c "createTestTenant(" en-route-voice.test.ts                                             → 3
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" en-route-voice.test.ts       → 24
```

**Anomalies:** This file is the real weight behind 4.5's "voice, phone and chat legs proven" — it has voice, #847-phone-leg, and #847-chat-leg tests, all against real Postgres, with a strong cross-tenant negative (AC-4.4) and 24 audit-assertion grep hits (highest of any file in this batch). Consistent with PRD's flag that the SMS-keyword leg (the 4th of "four entry points") is NOT here — no SMS-specific test name appears in this file.

### beta-verify-schema-fixes.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/beta-verify-schema-fixes.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:43:34
   Duration  2.46s (transform 104ms, setup 0ms, import 250ms, tests 53ms, environment 0ms)
```

**✓/✗ lines (3 total, 0 failures):**
```
 ✓ test/integration/beta-verify-schema-fixes.test.ts > beta-verify schema fixes (real Postgres) > claimForExecution accepts the string worker label — proposals.claimed_by is TEXT (migration 215) 28ms
 ✓ test/integration/beta-verify-schema-fixes.test.ts > beta-verify schema fixes (real Postgres) > PgDelayNoticeStateRepository.upsert round-trips — delay_notice_state exists (migration 216) 8ms
 ✓ test/integration/beta-verify-schema-fixes.test.ts > beta-verify schema fixes (real Postgres) > GET /api/interactions resolves service_locations.street1 — returns 200, not 500 16ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" beta-verify-schema-fixes.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" beta-verify-schema-fixes.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" beta-verify-schema-fixes.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" beta-verify-schema-fixes.test.ts  → 2
grep -c "InMemoryAuditRepository" beta-verify-schema-fixes.test.ts                                       → 0
grep -c "createTestTenant(" beta-verify-schema-fixes.test.ts                                             → 3
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" beta-verify-schema-fixes.test.ts       → 0
```

**Anomalies:** This file is NOT an "on my way"/"running late" feature test — it's a schema-migration regression suite (migrations 215/216 + an unrelated 500-fix). Its single keyword hit ("running-late" at line 14) is incidental context/comment text, not a feature test of 4.5/4.6. It contributes nothing toward 4.5/4.6 coverage despite matching the grep.

### auto-pick-appointment-920.test.ts

**Command:**
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/auto-pick-appointment-920.test.ts
```

**Summary:**
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:43:43
   Duration  4.45s (transform 1.48s, setup 0ms, import 2.04s, tests 216ms, environment 0ms)
```

**✓/✗ lines (3 total, 0 failures):**
```
 ✓ test/integration/auto-pick-appointment-920.test.ts > Integration — #920 auto-picked appointmentId survives dropUnverifiedIds > A11 reschedule_appointment: single active appointment auto-picks, survives dropUnverifiedIds, approves, executes 120ms
 ✓ test/integration/auto-pick-appointment-920.test.ts > Integration — #920 auto-picked appointmentId survives dropUnverifiedIds > A27 confirm_appointment: single active appointment auto-picks, survives dropUnverifiedIds, approves, executes 30ms
 ✓ test/integration/auto-pick-appointment-920.test.ts > Integration — #920 auto-picked appointmentId survives dropUnverifiedIds > reschedule_appointment stays gated (still asks) when the tenant has more than one active appointment 64ms
```

**Tenant-grade greps:**
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant|across tenants" auto-pick-appointment-920.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" auto-pick-appointment-920.test.ts
133:      `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
196:      timezone: TZ,

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" auto-pick-appointment-920.test.ts
no match
```

**Counts:**
```
grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" auto-pick-appointment-920.test.ts  → 2
grep -c "InMemoryAuditRepository" auto-pick-appointment-920.test.ts                                       → 0
grep -c "createTestTenant(" auto-pick-appointment-920.test.ts                                             → 1
grep -c -E "expect\([^)]*audit|audit_events|eventType|event_type" auto-pick-appointment-920.test.ts       → 0
```

**Anomalies:** Not an "on my way"/"running late" feature test either — it's about `reschedule_appointment`/`confirm_appointment` auto-picking a single active appointment ID (its "notify_delay" hit at line 27 is incidental/comment context). Contributes nothing toward 4.5/4.6 coverage despite matching the grep. No cross-tenant coverage in this file.

---

## Overall pass/fail table

| Row | File | Test Files | Tests | Pass/Fail | Re-run needed |
|---|---|---|---|---|---|
| 4.1 | dispatch.test.ts | 1 passed | 2 passed | PASS | no |
| 4.4 | dispatch-technician-day-window.test.ts | 1 passed | 3 passed | PASS | no |
| 4.4 | technician-location-authz.test.ts | 1 passed | 6 passed | PASS | no |
| 4.8 | tech-status-sms.test.ts | 1 passed | 3 passed | PASS | no |
| 7.1 | draft-estimate-execution.test.ts | 1 passed | 4 passed | PASS | no |
| 7.1 | mms-to-quote.int.test.ts | 1 passed | 3 passed | PASS | no |
| 7.3 (D) | estimates.test.ts | 1 passed | 10 passed | PASS | no |
| 7.4/7.5/7.6/7.8/7.9 | estimate-phases.test.ts | 1 passed | 11 passed | PASS | no |
| 7.10 | estimate-nudge.test.ts | 1 passed | 16 passed | PASS | no |
| 4.2 | (grep only — no integration file found) | — | — | N/A (0 files) | n/a |
| 4.5/4.6 | en-route-voice.test.ts | 1 passed | 7 passed | PASS | no |
| 4.5/4.6 | beta-verify-schema-fixes.test.ts | 1 passed | 3 passed | PASS | no |
| 4.5/4.6 | auto-pick-appointment-920.test.ts | 1 passed | 3 passed | PASS | no |

**Totals: 12 files run, 12/12 files passed, 71/71 individual tests passed, 0 failures, 0 re-runs required.**

## Per-file pool/InMemory/createTestTenant/audit-assertion counts

| File | pool/testDb count | InMemoryAuditRepository | createTestTenant( | audit-assertion grep |
|---|---|---|---|---|
| dispatch.test.ts | 2 | 0 | 2 | 0 |
| dispatch-technician-day-window.test.ts | 2 | 0 | 1 | 0 |
| technician-location-authz.test.ts | 2 | 0 | 7 | 0 |
| tech-status-sms.test.ts | 2 | 0 | 1 | 1 |
| draft-estimate-execution.test.ts | 2 | 0 | 2 | 4 |
| mms-to-quote.int.test.ts | 2 | 0 | 1 | 1 |
| estimates.test.ts | 2 | 0 | 2 | 0 |
| estimate-phases.test.ts | 2 | 10 | 1 | 0 |
| estimate-nudge.test.ts | 4 | 2 | 5 | 6 |
| en-route-voice.test.ts | 2 | 0 | 3 | 24 |
| beta-verify-schema-fixes.test.ts | 2 | 0 | 3 | 0 |
| auto-pick-appointment-920.test.ts | 2 | 0 | 1 | 0 |

## Anomalies (rollup)

1. **4.2** — `grep -rln "createSchedulingProposal" packages/api/test/integration` returns nothing: confirms zero integration coverage as the PRD states. No file to run.
2. **4.4** split across two files — cross-tenant negative for 4.4 lives ONLY in `technician-location-authz.test.ts` (verbatim test name matches acceptance criterion); `dispatch-technician-day-window.test.ts` covers only the timezone-bucketing half and has zero cross-tenant grep hits.
3. **4.8** — 3 tests total; test names don't show a distinct multi-appointment/"per affected customer" scenario by name (acceptance criterion asks for "a reschedule proposal per appointment").
4. **7.1 (draft-estimate-execution.test.ts)** — emits a recurring warn-log every run: `resolveAutoApproveThreshold called with no supervision signal — falling through to the permissive LEGACY_AUTO_APPROVE_THRESHOLD (0.9) with no unsupervised hard-block`, logged once per run at the `draft_estimate::unknown` key.
5. **7.4/7.5/7.6/7.8/7.9 (estimate-phases.test.ts)** — `InMemoryAuditRepository` appears 10 times (confirms PRD's flagged in-memory audit leg); zero cross-tenant grep hits in this file; a single `listTenantIds: async () => [tenant.tenantId]` single-tenant stub feeds the auto-expiry worker. No test name mentions "signature" or "conflict"/"settlement" mapping — 7.6's and 7.8's flagged gaps are not visible as named tests here.
6. **7.10 (estimate-nudge.test.ts)** — PRD text says "nine real-DB tests" but the file runs 16 tests total (6 claim-before-send + 7 voice/cooldown + 3 surname-resolution groups); it also contains 2 `InMemoryAuditRepository` references despite being characterized as real-DB, alongside 6 real audit-assertion grep hits and 4 pool/testDb references — a mixed file, not purely in-memory or purely real-DB.
7. **4.5/4.6 candidate selection** — of the 4 files the keyword grep matched, only `en-route-voice.test.ts` substantively tests "on my way"/"running late" (20 keyword hits, voice+phone+chat legs, 24 audit-assertion hits, cross-tenant negative). `beta-verify-schema-fixes.test.ts` and `auto-pick-appointment-920.test.ts` each matched on a single incidental keyword occurrence (a comment/description string) and are unrelated feature tests (schema-migration regressions; appointment auto-pick-ID survival, respectively) — running them added no 4.5/4.6 coverage evidence. The 4th candidate, `entity-resolution.test.ts`, was not run (stayed within the "up to 3" cap); it also matched on a single incidental `notify_delay` occurrence, same category as the already-run `auto-pick-appointment-920.test.ts`.
8. No `TESTCONTAINERS`/Docker container-start failures occurred on any of the 12 runs — no 60s-wait retries were needed.
9. Disk space never moved from ~2.7Gi free across all four checkpoints — well clear of the 600 MiB stop threshold.
