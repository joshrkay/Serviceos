# G1-B Docker-gated integration run log (wayfinder #1006, G1 entry audit)

Worktree: `/Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane` @ `2c2aa6d906f437fb8109f3f3828626cfb5229cd2` (branch `lane/rung-map-env`, = `origin/main`)
Invocation base (from `packages/api`):
```
DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose <path>
```
All 21 test-file runs below returned `EXIT_CODE=0` with a "Test Files … passed" summary line (i.e. all ran for real against Docker/Postgres, none skipped). **Zero ✗/× lines occurred anywhere across the whole session** — no re-runs were needed, and the known `onboarding-pack-seed-concurrency` flake (repo issue #976) did not occur.

Disk-space checks (`df -h / | tail -1`), taken after every 3rd test-file run:
| After run # | Filesystem | Size | Used | Avail | Capacity |
|---|---|---|---|---|---|
| start | /dev/disk3s1s1 | 228Gi | 12Gi | 1.9Gi | 87% |
| 3 (onboarding-pack) | /dev/disk3s1s1 | 228Gi | 12Gi | 1.9Gi | 87% |
| 6 (billing-trial) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.4Gi | 83% |
| 9 (onboarding-conversation) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.9Gi | 81% |
| 11 (update-brand-voice-voice-execution, checked early) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.9Gi | 81% |
| 13 (voice-idempotency) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.9Gi | 81% |
| 15 (entity-resolution) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.9Gi | 81% |
| 17 (update-estimate-execution) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.8Gi | 81% |
| 19 (log-expense-job-link) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.8Gi | 81% |
| 21 / final (material-items) | /dev/disk3s1s1 | 228Gi | 12Gi | 2.8Gi | 81% |

Free space never dropped below 600 MiB (it actually grew from 1.9Gi → ~2.8Gi over the run, presumably from container/image cache churn). No stop condition was hit.

---

## Setup notes (§8.1)

- Row 1.7 confirm cell names no specific file. Ran: `ls packages/api/test/integration | grep -i -E "trial|subscription|billing-plan|stripe-subscription"` → matched exactly two files: `billing-trial.test.ts` and `trial-provisioning-first-value.test.ts`. Both run below (≤2, per instructions).
- Row 5.2 confirm cell names no specific file. Ran: `ls packages/api/test/integration | grep -i -E "photo|attachment"` → **no match** (empty output) in `packages/api/test/integration`. Broader search found `packages/api/test/jobs/job-photos.test.ts` and `packages/api/test/attachments/{pg-attachment,attachment-service}.test.ts` plus `packages/api/test/routes/attachments.route.test.ts`, but none of these live under `test/integration/`, so per the literal instruction no file was run for 5.2. See Anomalies.
- Row 6.3: per instructions, `chat-entity-resolution.test.ts` was **not re-run** — citing the §5 lane's existing result of 20/20 passed instead. `entity-resolution.test.ts` was run fresh (see below).
- All 20 explicitly-named files were confirmed present under `packages/api/test/integration/` before running (`ls`/`-f` check), including `chat-entity-resolution.test.ts` (present, not executed here).

---

## Per-file results

### 1. `clerk-owner-membership.test.ts` (rows 1.1, 1.11)

Command:
```
cd .../packages/api && DOCKER_HOST=... TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=... RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose test/integration/clerk-owner-membership.test.ts
```
Summary:
```
Test Files  1 passed (1)
     Tests  1 passed (1)
  Start at  06:25:36
  Duration  3.00s (transform 297ms, setup 0ms, import 594ms, tests 44ms, environment 0ms)
```
✓ lines (1 of 1):
```
✓ test/integration/clerk-owner-membership.test.ts > Postgres integration — Clerk owner membership bootstrap > creates an owner users row (role=owner) and does not duplicate on replay 41ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant` → no match
- `timezone|business_hours|labor_rate|threshold|pack|digest_enabled` → no match
- `listTenantIds:\s*async\s*\(\)\s*=>\s*\[` → no match

Counts: `getSharedTestDb|TEST_DB_URL|new Pool(|withTestDb|testDb`=2; `InMemoryAuditRepository`=0; `createTestTenant(`=0

---

### 2. `onboarding-identity.test.ts` (row 1.2)

Summary:
```
Test Files  1 passed (1)
     Tests  5 passed (5)
  Start at  06:25:53
  Duration  2.78s (transform 177ms, setup 0ms, import 332ms, tests 130ms, environment 0ms)
```
✓ lines (5 of 5):
```
✓ test/integration/onboarding-identity.test.ts > PUT /api/onboarding/identity > rejects payload missing businessName with 400 38ms
✓ test/integration/onboarding-identity.test.ts > PUT /api/onboarding/identity > upserts on valid payload (no prior row) and step 2 becomes done 42ms
✓ test/integration/onboarding-identity.test.ts > PUT /api/onboarding/identity > updates an existing tenant_settings row in place (idempotent) 18ms
✓ test/integration/onboarding-identity.test.ts > PUT /api/onboarding/identity > serviceAreaRadius: omitted keeps the stored value, null clears it (#874) 22ms
✓ test/integration/onboarding-identity.test.ts > PUT /api/onboarding/identity > emits a tenant.identity_set audit event 8ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|...` → no match
- `timezone|business_hours|...`:
```
8:import { PgPackActivationRepository } from '../../src/settings/pg-pack-activation';
20:    const packActivationRepo = new PgPackActivationRepository(pool);
34:    app.use('/api/onboarding', createOnboardingRouter({ settingsRepo, packActivationRepo, auditRepo, pool }));
65:      // every spoken booking into a timezone clarification rather than guess
69:      timezone: 'America/Chicago',
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=1

---

### 3. `onboarding-pack.test.ts` (row 1.3)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:26:07
  Duration  2.67s (transform 169ms, setup 0ms, import 328ms, tests 94ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/onboarding-pack.test.ts > POST /api/onboarding/pack > rejects unknown packId with 400 VALIDATION_ERROR 33ms
✓ test/integration/onboarding-pack.test.ts > POST /api/onboarding/pack > activates hvac pack and step 3 becomes done 29ms
✓ test/integration/onboarding-pack.test.ts > POST /api/onboarding/pack > is idempotent: activating hvac twice results in single entry 29ms
```
No ✗.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...pack...`:
```
8:import { PgPackActivationRepository } from '../../src/settings/pg-pack-activation';
12:describe('POST /api/onboarding/pack', () => {
20:    const packActivationRepo = new PgPackActivationRepository(pool);
34:    app.use('/api/onboarding', createOnboardingRouter({ settingsRepo, packActivationRepo, auditRepo, pool }));
45:  it('rejects unknown packId with 400 VALIDATION_ERROR', async () => {
46:    const res = await request(app).post('/api/onboarding/pack').send({
47:      packId: 'electrical',
53:  it('activates hvac pack and step 3 becomes done', async () => {
54:    const res = await request(app).post('/api/onboarding/pack').send({
55:      packId: 'hvac',
58:    expect(res.body.packId).toBe('hvac');
60:    // Verify pack status in GET /status
62:    expect(status.body.steps.find((s: any) => s.id === 'pack').status).toBe('done');
71:    const packRow = await pool.query(
72:      `SELECT pack_id, status FROM pack_activations WHERE tenant_id=$1 AND pack_id=$2`,
75:    expect(packRow.rows).toHaveLength(1);
76:    expect(packRow.rows[0].status).toBe('active');
81:    await request(app).post('/api/onboarding/pack').send({ packId: 'hvac' });
83:    await request(app).post('/api/onboarding/pack').send({ packId: 'hvac' });
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=1

df check after this run (3rd): `2.8Gi/228Gi 1.9Gi avail 87%` (see table above — recorded as 1.9Gi).

---

### 4. `onboarding-pack-seed-concurrency.test.ts` (row 1.3)

Summary:
```
Test Files  1 passed (1)
     Tests  1 passed (1)
  Start at  06:26:21
  Duration  2.60s (transform 174ms, setup 0ms, import 226ms, tests 89ms, environment 0ms)
```
✓ lines (1 of 1):
```
✓ test/integration/onboarding-pack-seed-concurrency.test.ts > Postgres integration — concurrent onboarding pack-seed execution (review finding #3) > two sibling proposals executing concurrently for the SAME pack do NOT duplicate catalog items or templates 88ms
```
No ✗. **Known flake (repo #976, `tenant_settings_tenant_id_key`) did NOT occur** — passed clean on the single run.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...pack...`:
```
6: * call `activatePackWithSeed` with NO lock at all (unlike POST /pack's
9: * `onboarding_service_category`) targeting the SAME pack execute on their
10: * OWN idempotency locks (keyed by proposal id, not by pack) in a
20: * tenant + pack. It proves the fix (a session-level advisory lock on the
21: * SAME (tenant, pack) key the HTTP route's xact lock uses, threaded through
28:import { PgPackActivationRepository } from '../../src/settings/pg-pack-activation';
38:describe('Postgres integration — concurrent onboarding pack-seed execution (review finding #3)', () => {
54:    const packActivationRepo = new PgPackActivationRepository(pool);
58:    const packSeedDeps = { catalogRepo, templateRepo };
59:    return { settingsRepo, packActivationRepo, auditRepo, catalogRepo, templateRepo, packSeedDeps };
69:        timezone: 'America/Phoenix',
90:  it('two sibling proposals executing concurrently for the SAME pack do NOT duplicate catalog items or templates', async () => {
91:    const { settingsRepo, packActivationRepo, auditRepo, packSeedDeps } = buildDeps();
95:      packActivationRepo,
97:      packSeedDeps,
102:      packActivationRepo,
104:      packSeedDeps,
111:    // DIFFERENT proposal ids, same tenant + pack, no shared idempotency key.
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=1

---

### 5. `onboarding-status.test.ts` (row 1.4)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:26:34
  Duration  2.53s (transform 163ms, setup 0ms, import 264ms, tests 57ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/onboarding-status.test.ts > GET /api/onboarding/status > returns identity as current step for a fresh tenant (no settings row) 37ms
✓ test/integration/onboarding-status.test.ts > GET /api/onboarding/status > marks identity done when all four fields present 9ms
✓ test/integration/onboarding-status.test.ts > GET /api/onboarding/status > isComplete=true when all 7 steps satisfied 8ms
```
No ✗.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...`:
```
8:import { PgPackActivationRepository } from '../../src/settings/pg-pack-activation';
20:    const packActivationRepo = new PgPackActivationRepository(pool);
34:    app.use('/api/onboarding', createOnboardingRouter({ settingsRepo, packActivationRepo, auditRepo, pool }));
55:      `INSERT INTO tenant_settings (id, tenant_id, business_name, business_hours, job_buffer_minutes, hourly_rate_cents, timezone, estimate_prefix, invoice_prefix, next_estimate_number, next_invoice_number, default_payment_term_days)
62:    expect(res.body.currentStep).toBe('pack');
66:    // Identity + pack (via terminology_preferences._activeVerticalPacks) +
69:      `INSERT INTO tenant_settings (id, tenant_id, business_name, business_hours, job_buffer_minutes, hourly_rate_cents, terminology_preferences, ai_model, ai_verification_status, timezone, estimate_prefix, invoice_prefix, next_estimate_number, next_invoice_number, default_payment_term_days)
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=1

---

### 6. `billing-trial.test.ts` (row 1.7, match 1 of 2)

Summary:
```
Test Files  1 passed (1)
     Tests  6 passed (6)
  Start at  06:26:48
  Duration  3.18s (transform 214ms, setup 0ms, import 244ms, tests 44ms, environment 0ms)
```
✓ lines (6 of 6):
```
✓ test/integration/billing-trial.test.ts > BillingService — trial flows > createTrialCheckoutSession > calls Stripe Checkout API with trial_period_days=14 and returns url 33ms
✓ test/integration/billing-trial.test.ts > BillingService — trial flows > createTrialCheckoutSession > throws when STRIPE_PRICE_ID is unset 2ms
✓ test/integration/billing-trial.test.ts > BillingService — trial flows > createTrialCheckoutSession > throws when billingService has no api key 1ms
✓ test/integration/billing-trial.test.ts > BillingService — trial flows > endTrialNow > calls Stripe Update Subscription with trial_end=now 3ms
✓ test/integration/billing-trial.test.ts > BillingService — trial flows > endTrialNow > throws NotFoundError when no subscription is on file 2ms
✓ test/integration/billing-trial.test.ts > BillingService — trial flows > endTrialNow > throws NotFoundError when tenant does not exist 1ms
```
No ✗.

df check after this run (6th): `12Gi used, 2.4Gi avail, 83%`.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=5

---

### 7. `trial-provisioning-first-value.test.ts` (row 1.7, match 2 of 2)

Summary:
```
Test Files  1 passed (1)
     Tests  6 passed (6)
  Start at  06:27:03
  Duration  4.77s (transform 1.64s, setup 0ms, import 2.25s, tests 213ms, environment 0ms)
```
✓ lines (6 of 6):
```
✓ test/integration/trial-provisioning-first-value.test.ts > Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking) > step a — signed Clerk user.created bootstraps tenant + owner + settings, idempotently 63ms
✓ test/integration/trial-provisioning-first-value.test.ts > Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking) > step b — GET /api/me returns the authenticated workspace (tenant id, owner role, permissions) 4ms
✓ test/integration/trial-provisioning-first-value.test.ts > Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking) > step c — signed Stripe customer.subscription.created (trialing) mirrors trial entitlement 15ms
✓ test/integration/trial-provisioning-first-value.test.ts > Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking) > step d — provision-twilio dev stub reaches full_readiness; onboarding phone step done; test-call skip works 18ms
✓ test/integration/trial-provisioning-first-value.test.ts > Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking) > step e — first-value in-app: seed customer, voice-book, approve, execute → real appointment; cross-tenant isolated 108ms
✓ test/integration/trial-provisioning-first-value.test.ts > Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking) > step f — evidence block 0ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
521:  it('step e — first-value in-app: seed customer, voice-book, approve, execute → real appointment; cross-tenant isolated', async () => {
658:    const otherTenant = await createTestTenant(pool);
660:      userId: otherTenant.userId,
661:      tenantId: otherTenant.tenantId,
666:    expect(await appointmentRepo.findById(otherTenant.tenantId, appointmentId)).toBeNull();
```
- `timezone|business_hours|labor_rate|threshold|pack|digest_enabled` (21 matches, representative incl. all "pack"/"timezone"):
```
2: * Trial / provisioning proof pack (Agent E, R4) —
4: * pack" section.
26: * block so docs/verification-runs/trial-provision-proof-pack-2026-09-09.md
29: * Run: cd packages/api && RLS_RUNTIME_ROLE=true npx vitest run \
46:import { PgPackActivationRepository } from '../../src/settings/pg-pack-activation';
93:const STRIPE_SECRET = 'whsec_test_trial_provisioning_proof_pack';
175:                COALESCE(timezone, $2) AS timezone
183:          timezone: DEFAULT_TENANT_TIMEZONE,
191:        timezone: String(row.timezone),
219:async function setTenantTimezone(pool: Pool, tenantId: string, timezone: string): Promise<void> {
225:      `UPDATE tenant_settings SET timezone = $1, updated_at = now() WHERE tenant_id = $2`,
226:      [timezone, tenantId],
237:describe('Postgres integration — trial/provisioning proof pack (signup → workspace → trial → provisioned phone → in-app booking)', () => {
242:  let packActivationRepo: PgPackActivationRepository;
272:    packActivationRepo = new PgPackActivationRepository(pool);
296:    process.env.STRIPE_PRICE_ID = 'price_mock_trial_proof_pack';
301:        throw new Error('unexpected Stripe fetch — this proof pack never opens real checkout');
327:   * mounting every router this proof pack drives over HTTP. Matches the
340:      createOnboardingRouter({ settingsRepo, packActivationRepo, auditRepo, pool }),
414:    // pack does not exercise the identity step.
545:    // exist) — `pool` alone self-wires PgEntityResolver + tenant-timezone
671:    console.log('\n=== Trial / provisioning proof pack — evidence ===');
```
- `listTenantIds:...` → no match

Counts: pool/testDb=3; InMemoryAuditRepository=0; createTestTenant(=1

---

### 8. `onboarding-ai-check.test.ts` (row 1.8)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:27:20
  Duration  2.75s (transform 236ms, setup 0ms, import 490ms, tests 70ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/onboarding-ai-check.test.ts > onboarding AI self-check > passing verification → ai_check step done and DB status passed 42ms
✓ test/integration/onboarding-ai-check.test.ts > onboarding AI self-check > failing verification → ai_check step error with ai_verification_failed blocker 11ms
✓ test/integration/onboarding-ai-check.test.ts > onboarding AI self-check > retry route resets status to pending and enqueues a verify_ai job 15ms
```
No ✗.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...`:
```
8:import { PgPackActivationRepository } from '../../src/settings/pg-pack-activation';
34:    `INSERT INTO tenant_settings (id, tenant_id, business_name, ai_model, timezone, estimate_prefix, invoice_prefix, next_estimate_number, next_invoice_number, default_payment_term_days)
50:    const packActivationRepo = new PgPackActivationRepository(pool);
65:    app.use('/api/onboarding', createOnboardingRouter({ settingsRepo, packActivationRepo, auditRepo, pool, queue }));
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=3; createTestTenant(=1

---

### 9. `onboarding-conversation.test.ts` (row 1.9)

Summary:
```
Test Files  1 passed (1)
     Tests  6 passed (6)
  Start at  06:27:34
  Duration  2.35s (transform 76ms, setup 0ms, import 109ms, tests 66ms, environment 0ms)
```
✓ lines (6 of 6):
```
✓ test/integration/onboarding-conversation.test.ts > onboarding_session — integration > creates a session with the migration-defined defaults 10ms
✓ test/integration/onboarding-conversation.test.ts > onboarding_session — integration > round-trips transcript turns + extractions + clarification counts via JSONB columns 8ms
✓ test/integration/onboarding-conversation.test.ts > onboarding_session — integration > migration 195 pins ENABLE + FORCE row-level security and the tenant_isolation policy 6ms
✓ test/integration/onboarding-conversation.test.ts > onboarding_session — integration > RLS prevents cross-tenant reads under the unprivileged app role 7ms
✓ test/integration/onboarding-conversation.test.ts > onboarding_session — integration > the partial index supports the cross-tenant created_at DESC ordering used by status listings 7ms
✓ test/integration/onboarding-conversation.test.ts > onboarding_session — integration > marks the session completed when completedAt is set 7ms
```
No ✗.

df check after this run (9th): `12Gi used, 2.9Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
35:  let secondTenant: { tenantId: string; userId: string };
54:    secondTenant = await createTestTenant(pool);
128:  it('RLS prevents cross-tenant reads under the unprivileged app role', async () => {
133:    const visibleToB = await asTenant(pool, secondTenant.tenantId, async (c) => {
152:  it('the partial index supports the cross-tenant created_at DESC ordering used by status listings', async () => {
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=2

---

### 10. `brand-voice.integration.test.ts` (row 1.10, match 1 of 2)

Summary:
```
Test Files  1 passed (1)
     Tests  5 passed (5)
  Start at  06:27:47
  Duration  2.55s (transform 110ms, setup 0ms, import 156ms, tests 127ms, environment 0ms)
```
✓ lines (5 of 5):
```
✓ test/integration/brand-voice.integration.test.ts > Postgres integration — Brand-Voice Configurator (N-011) > onboarding captures all six fields, writes v1 history, and round-trips to a tone 40ms
✓ test/integration/brand-voice.integration.test.ts > Postgres integration — Brand-Voice Configurator (N-011) > an explicit edit is audit-logged and cool-down enforced (423), then succeeds after the window 25ms
✓ test/integration/brand-voice.integration.test.ts > Postgres integration — Brand-Voice Configurator (N-011) > persists a Clerk-style non-UUID actor as changed_by (PUT + rollback) — changed_by is TEXT 20ms
✓ test/integration/brand-voice.integration.test.ts > Postgres integration — Brand-Voice Configurator (N-011) > PR#663 concurrency: two overlapping edits serialize on the FOR UPDATE lock — exactly one wins, no stale-merge lost update 18ms
✓ test/integration/brand-voice.integration.test.ts > Postgres integration — Brand-Voice Configurator (N-011) > rollback re-persists an older snapshot as a new bump (history never mutated) 23ms
```
No ✗.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=5

---

### 11. `update-brand-voice-voice-execution.test.ts` (row 1.10, match 2 of 2)

Summary:
```
Test Files  1 passed (1)
     Tests  6 passed (6)
  Start at  06:28:01
  Duration  4.96s (transform 697ms, setup 0ms, import 981ms, tests 191ms, environment 0ms)
```
✓ lines (6 of 6):
```
✓ test/integration/update-brand-voice-voice-execution.test.ts > Postgres integration — voice update_brand_voice → approve → execute → persist + audit > attributes a technician-drafted, owner-approved config write to the OWNER through the real sweep 60ms
✓ test/integration/update-brand-voice-voice-execution.test.ts > Postgres integration — voice update_brand_voice → approve → execute → persist + audit > persists a new brand_voice_versions row with the spoken register + sign-off, version incremented 5ms
✓ test/integration/update-brand-voice-voice-execution.test.ts > Postgres integration — voice update_brand_voice → approve → execute → persist + audit > emits exactly one brand_voice.updated audit event with actor attribution 2ms
✓ test/integration/update-brand-voice-voice-execution.test.ts > Postgres integration — voice update_brand_voice → approve → execute → persist + audit > does not expose the brand voice to another tenant (scoped read) 5ms
✓ test/integration/update-brand-voice-voice-execution.test.ts > Postgres integration — voice update_brand_voice → approve → execute → persist + audit > a second voice-approved edit inside the cool-down honestly fails execution — never a silent skip 22ms
✓ test/integration/update-brand-voice-voice-execution.test.ts > Postgres integration — voice update_brand_voice → approve → execute → persist + audit > F-2: a spoken "lock my brand voice" can NEVER set brand_voice_locked — gated draft, approval refused, forged execution refused, real column stays false 27ms
```
No ✗.

df check after this run (11th): `12Gi used, 2.9Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
11: * (never a silent skip) and that a cross-tenant reader sees nothing.
264:  it('does not expose the brand voice to another tenant (scoped read)', async () => {
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=5

---

### 12. `log-time-entry-execution.test.ts` (row 5.3)

Summary:
```
Test Files  1 passed (1)
     Tests  4 passed (4)
  Start at  06:28:17
  Duration  3.47s (transform 713ms, setup 0ms, import 957ms, tests 76ms, environment 0ms)
```
✓ lines (4 of 4):
```
✓ test/integration/log-time-entry-execution.test.ts > Postgres integration — voice log_time_entry ("Clock 2 hours on the Patel job") → approve → execute → persist + audit > persists the time_entries row with durationMinutes 120 and the resolved jobId 1ms
✓ test/integration/log-time-entry-execution.test.ts > Postgres integration — voice log_time_entry ("Clock 2 hours on the Patel job") → approve → execute → persist + audit > emits exactly one time-entry audit event with actor attribution 1ms
✓ test/integration/log-time-entry-execution.test.ts > Postgres integration — voice log_time_entry ("Clock 2 hours on the Patel job") → approve → execute → persist + audit > does not expose the entry to another tenant (scoped read) 3ms
✓ test/integration/log-time-entry-execution.test.ts > Postgres integration — voice log_time_entry ("Clock 2 hours on the Patel job") → approve → execute → persist + audit > is counted by the job-profit query for the resolved job (P&L linkage) 8ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
217:  it('does not expose the entry to another tenant (scoped read)', async () => {
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=2

---

### 13. `voice-idempotency.test.ts` (row 5.4, D half)

Summary:
```
Test Files  1 passed (1)
     Tests  4 passed (4)
  Start at  06:28:33
  Duration  2.73s (transform 103ms, setup 0ms, import 272ms, tests 87ms, environment 0ms)
```
✓ lines (4 of 4):
```
✓ test/integration/voice-idempotency.test.ts > Postgres integration — voice create idempotency (U11) > same key twice → one row, same recording id, exactly one effective job 31ms
✓ test/integration/voice-idempotency.test.ts > Postgres integration — voice create idempotency (U11) > create-then-crash replay (row exists, no job) re-enqueues so the recording can complete 7ms
✓ test/integration/voice-idempotency.test.ts > Postgres integration — voice create idempotency (U11) > tenant-isolates the key: the same value in two tenants → two independent rows 15ms
✓ test/integration/voice-idempotency.test.ts > Postgres integration — voice create idempotency (U11) > different keys in one tenant → different rows 13ms
```
No ✗.

df check after this run (13th): `12Gi used, 2.9Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=2

---

### 14. `voice-inbound-appointment.test.ts` (row 6.2, D half)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:28:46
  Duration  4.41s (transform 1.37s, setup 0ms, import 1.92s, tests 114ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/voice-inbound-appointment.test.ts > Integration — inbound voice appointment-setting (real Postgres) > routes a dialed number to its tenant via the real phoneE164 column 3ms
✓ test/integration/voice-inbound-appointment.test.ts > Integration — inbound voice appointment-setting (real Postgres) > persists the spoken reason to appointments.notes only after approval + execution 25ms
✓ test/integration/voice-inbound-appointment.test.ts > Integration — inbound voice appointment-setting (real Postgres) > books the spoken sentence end to end: free text → PgEntityResolver → REAL drafting task → approve → production execution registry 55ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
14: *      cross-tenant system_lookup path).
315:    // invisible to another tenant. Same scoped-read form the sibling voice
```
- `timezone|business_hours|labor_rate|threshold|pack|digest_enabled`:
```
254:        timezone: 'America/Chicago',
298:    expect(booked!.timezone).toBe('America/Chicago');
397:        // Below the legacy 0.9 auto-approve threshold, so the booking lands in
412:      tenantSchedulingResolver: async () => ({ timezone: BOOKING_TZ }),
439:    expect(payload.timezone).toBe(BOOKING_TZ);
489:    expect(booked!.timezone).toBe(BOOKING_TZ);
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=4

---

### 15. `entity-resolution.test.ts` (row 6.3)

Summary:
```
Test Files  1 passed (1)
     Tests  94 passed (94)
  Start at  06:29:01
  Duration  5.44s (transform 1.22s, setup 0ms, import 1.71s, tests 1.31s, environment 0ms)
```
94 ✓ lines total; first 40 shown (rest omitted per instructions):
```
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > exact display_name match resolves with score 1.0 4ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > transcription typo resolves via trigram similarity 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > duplicate display_names → ambiguous with both candidates 7ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > unknown reference → not_found with the raw reference 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > never resolves across tenants 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > archived customers are excluded 4ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > exact full-name match resolves with role hint 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > transcription typo resolves via trigram similarity 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > two technicians with the same name → ambiguous with both candidates 3ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > soft-deleted users never resolve 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > never resolves across tenants 2ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > router end-to-end: spoken technician name lands as a verified toTechnicianId 16ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > a FIRST-NAME-ONLY reference resolves against a full name (7fbff6e) 5ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > a PARTIAL first name stays below the floor and does not resolve 4ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > two technicians sharing only a FIRST name → ambiguous, never a guess 4ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > overflow guard (six confident matches escalate, five do not) > SIX technicians sharing a first name → not_found, not an arbitrary five-candidate picker 6ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > technician kind (U1) > overflow guard (six confident matches escalate, five do not) > FIVE technicians sharing a first name → ambiguous with all five, not escalated 5ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > resolves jobs by summary (schema column, not the nonexistent title) 10ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > an ambiguous job NAME answers with APPOINTMENT candidates, not job candidates 27ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3 positive: a named job reference with ONE upcoming appointment resolves to that appointment 14ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3: a named job reference with MULTIPLE upcoming appointments → ambiguous, ≤5 candidates carrying date + assigned tech 26ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3 (the defect fix): a NAME-BEARING reference matching NO job returns not_found — even when the tenant has exactly one upcoming appointment (the case that would otherwise resolve via the nameless fallback) 14ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-3 regression pin: a NAMELESS reference with exactly one upcoming appointment still resolves (SCH-03 unbroken) 12ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-4: two technicians sharing a FIRST NAME → reassign_appointment drafting short-circuits to voice_clarification (technician picker) 19ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by job name (AC-3 / AC-4) > AC-4 (identical full name, retained): two technicians with the SAME full name → reassign_appointment drafting also short-circuits to voice_clarification 16ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > "move the Garcia job" resolves through customers → jobs when the SUMMARY is "AC repair" and only the CUSTOMER carries the name 14ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a LAST-NAME-ONLY reference clears the confirm floor, which whole-string similarity() provably does not 22ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > two jobs for the SAME customer stay a one-tap clarification, in appointment kind 24ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a customer-named reference never crosses tenants 13ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an ARCHIVED customer cannot answer a named reference 12ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > "the 2pm" resolves the appointment at 2pm in the TENANT's zone, not at 14:00 UTC 36ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > "tomorrow at 2" resolves TOMORROW's 2pm in the tenant zone 47ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > two appointments at the stated time stay a one-tap clarification carrying what distinguishes them 24ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a canceled appointment is not a reschedule target even at the stated time 15ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a clock-time reference never crosses tenants 16ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > a clock time that matches nothing REFUSES — it never answers with a different appointment 19ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > a tenant with NO tenant_settings row does not resolve "the 2pm" 12ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > an explicit NULL timezone refuses the same way — migration 263 made "unchosen" representable precisely so it could be told apart from a real Eastern tenant 12ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > an UNRECOGNIZED timezone string refuses too — a garbage zone is not a chosen one 12ms
✓ test/integration/entity-resolution.test.ts > Postgres integration — entity resolution (P8) > appointment resolution by CUSTOMER name and by CLOCK TIME > an unset tenant timezone refuses instead of defaulting to Eastern > CONTROL: the identical fixture DOES resolve once the tenant actually states Eastern — so the refusals above are the missing zone, not an unmatchable appointment 14ms
```
(remaining 54 ✓ lines, tests 41–94, all passed — omitted here; full log at `/tmp/g1b-out/15-entity-resolution.log`)

No ✗.

df check after this run (15th): `12Gi used, 2.9Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant` → **no match** (0 hits — despite many "never resolves ... across tenants" test names, none uses these exact tokens)
- `timezone|business_hours|labor_rate|threshold|pack|digest_enabled` (22 matches, full list):
```
561:          timezone: 'America/Chicago',
635:        timezone: 'America/Chicago', status: 'scheduled', holdPendingApproval: false,
916:      timezone?: string;
920:      if (opts.timezone) {
922:          `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
924:          [crypto.randomUUID(), t.tenantId, 'Test Shop', opts.timezone],
1031:        timezone: 'America/Chicago',
1227:        timezone: TZ,
1258:        timezone: TZ,
1284:        timezone: TZ,
1314:        timezone: TZ,
1332:        timezone: TZ,
1337:        `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
1363:        timezone: TZ,
1388:    // -- an UNSET tenant timezone must REFUSE, never default ---------------
1403:    describe('an unset tenant timezone refuses instead of defaulting to Eastern', () => {
1407:        // Omitting `timezone` seeds no tenant_settings row at all.
1422:      it('an explicit NULL timezone refuses the same way — migration 263 made "unchosen" representable precisely so it could be told apart from a real Eastern tenant', async () => {
1428:          `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
1442:      it('an UNRECOGNIZED timezone string refuses too — a garbage zone is not a chosen one', async () => {
1448:          `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
1466:          timezone: EASTERN,
```
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=22

**Note (row 6.3, other file):** `chat-entity-resolution.test.ts` was NOT re-run per instructions — the §5 lane already ran it with result 20/20 passed. Cited, not executed, here.

---

### 16. `update-job-execution.test.ts` (row 6.4)

Summary:
```
Test Files  1 passed (1)
     Tests  11 passed (11)
  Start at  06:29:34
  Duration  3.84s (transform 867ms, setup 0ms, import 1.15s, tests 329ms, environment 0ms)
```
✓ lines (11 of 11):
```
✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > changes the real job row status + priority via the production registry 22ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > changes the real job row title + description 20ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > emits a job.updated audit event (regression guard for handler+registry audit wiring) 24ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > does not expose the job to another tenant (scoped read) and a cross-tenant jobId fails cleanly 7ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — voice update_job → approve → execute → persist + audit > a completed transition stamps completed_at AND runs completion effects (auto-drafts an invoice proposal) 68ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > the ILIKE display-text search CANNOT reach this job from the spoken name — only the customer traversal can 5ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > the REAL resolver reaches the job from the spoken surname, past a prefix decoy 3ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > drafting: the RESOLVER id lands on the payload and lifts the gate — the model's hallucinated id loses 5ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > end to end: the drafted proposal approves and the REAL job row completes, with both audit events 34ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > an ILLEGAL lifecycle move is REFUSED end to end — a resolved job is not a licence to skip the state machine 22ms
✓ test/integration/update-job-execution.test.ts > Postgres integration — B7.7 drafting leg: spoken "Mark the Garcia job complete" → resolve → draft → approve → execute > cross-tenant: the same sentence resolves to nothing for another tenant, and a borrowed jobId stays GATED 16ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
11: * completion effects, the job.updated audit event, and the cross-tenant
228:  it('does not expose the job to another tenant (scoped read) and a cross-tenant jobId fails cleanly', async () => {
242:        summary: 'cross-tenant attempt',
252:    // The row is untouched — no cross-tenant write leaked through.
759:  it('cross-tenant: the same sentence resolves to nothing for another tenant, and a borrowed jobId stays GATED', async () => {
```
- `timezone|business_hours|labor_rate|threshold|pack|digest_enabled`:
```
297:      timezone: 'UTC',
```
- `listTenantIds:...` → no match

Counts: pool/testDb=3; InMemoryAuditRepository=0; createTestTenant(=5

---

### 17. `update-estimate-execution.test.ts` (row 6.5)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:29:49
  Duration  3.34s (transform 674ms, setup 0ms, import 917ms, tests 195ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/update-estimate-execution.test.ts > Postgres integration — spoken update_estimate → resolve → draft → approve → execute > "Add two hours of labor to the Garcia estimate" persists the line (qty 2, unit hour) and recomputes totals in integer cents 69ms
✓ test/integration/update-estimate-execution.test.ts > Postgres integration — spoken update_estimate → resolve → draft → approve → execute > emits exactly one estimate.updated audit event 45ms
✓ test/integration/update-estimate-execution.test.ts > Postgres integration — spoken update_estimate → resolve → draft → approve → execute > cross-tenant negative: the spoken reference does not resolve from another tenant, and the estimate is not readable or editable there 50ms
```
No ✗.

df check after this run (17th): `12Gi used, 2.8Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
42: *      another tenant, and the estimate is not readable/editable by another
389:  it('cross-tenant negative: the spoken reference does not resolve from another tenant, and the estimate is not readable or editable there', async () => {
399:    // The SAME sentence, spoken inside another tenant, resolves nothing —
415:    // The execution handler itself refuses a cross-tenant target: scoping
440:        summary: 'cross-tenant attempt',
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=2

---

### 18. `add-note-voice-execution.test.ts` (row 6.6, match 1 of 2)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:30:03
  Duration  3.36s (transform 726ms, setup 0ms, import 973ms, tests 66ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/add-note-voice-execution.test.ts > Postgres integration — voice add_note ("Note on the Patel job") → approve → execute → persist + audit > persists the dictated note on the job 1ms
✓ test/integration/add-note-voice-execution.test.ts > Postgres integration — voice add_note ("Note on the Patel job") → approve → execute → persist + audit > emits exactly one note-created audit event with actor attribution 1ms
✓ test/integration/add-note-voice-execution.test.ts > Postgres integration — voice add_note ("Note on the Patel job") → approve → execute → persist + audit > does not expose the note to another tenant (scoped read) 3ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
179:  it('does not expose the note to another tenant (scoped read)', async () => {
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=2

---

### 19. `log-expense-job-link.test.ts` (row 6.6, match 2 of 2)

Summary:
```
Test Files  1 passed (1)
     Tests  3 passed (3)
  Start at  06:30:18
  Duration  3.16s (transform 707ms, setup 0ms, import 942ms, tests 120ms, environment 0ms)
```
✓ lines (3 of 3):
```
✓ test/integration/log-expense-job-link.test.ts > Integration — U3 spoken expense keeps its job link (real Postgres + real resolver) > "$40 in parts for the Henderson job" → resolver links the job → persisted expense row carries job_id → job P&L counts it 76ms
✓ test/integration/log-expense-job-link.test.ts > Integration — U3 spoken expense keeps its job link (real Postgres + real resolver) > ambiguous job (two Henderson jobs) → the resolver answers ambiguous — a clarification upstream, never a guess 18ms
✓ test/integration/log-expense-job-link.test.ts > Integration — U3 spoken expense keeps its job link (real Postgres + real resolver) > no job mention → the expense logs UNLINKED and job P&L does NOT count it (today's behavior preserved) 25ms
```
No ✗.

df check after this run (19th): `12Gi used, 2.8Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|...` → no match
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=1

---

### 20. `voice-lookup-answer.test.ts` (row 6.7)

Summary:
```
Test Files  1 passed (1)
     Tests  6 passed (6)
  Start at  06:30:31
  Duration  2.78s (transform 94ms, setup 0ms, import 122ms, tests 83ms, environment 0ms)
```
✓ lines (6 of 6):
```
✓ test/integration/voice-lookup-answer.test.ts > Postgres integration — voice lookup answers (U3) > creates the in-app memo with answerStatus=pending on the real column 10ms
✓ test/integration/voice-lookup-answer.test.ts > Postgres integration — voice lookup answers (U3) > holds the two-phase contract: completed + answerStatus=pending, then answered 11ms
✓ test/integration/voice-lookup-answer.test.ts > Postgres integration — voice lookup answers (U3) > is write-once: a redelivered stamp cannot clobber a terminal outcome 12ms
✓ test/integration/voice-lookup-answer.test.ts > Postgres integration — voice lookup answers (U3) > keeps 'failed' writable so a transcription retry can land a fresh outcome 8ms
✓ test/integration/voice-lookup-answer.test.ts > Postgres integration — voice lookup answers (U3) > tenant-isolates the answer write and read 10ms
✓ test/integration/voice-lookup-answer.test.ts > Postgres integration — voice lookup answers (U3) > rejects an out-of-enum answer_status at the DB (CHECK constraint) 5ms
```
No ✗.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
131:    const otherTenant = await createTestTenant(pool);
135:      otherTenant.tenantId,
142:    const crossRead = await voiceRepo.findById(otherTenant.tenantId, recording.id);
145:    // Same-tenant state untouched by the failed cross-tenant write.
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=2

---

### 21. `material-items.test.ts` (row 6.9)

Summary:
```
Test Files  1 passed (1)
     Tests  25 passed (25)
  Start at  06:30:45
  Duration  2.66s (transform 93ms, setup 0ms, import 140ms, tests 167ms, environment 0ms)
```
✓ lines (25 of 25):
```
✓ test/integration/material-items.test.ts > Postgres integration — material items > round-trips create -> listPending -> markPurchased 13ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > scopes listPending by jobId when given 12ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByBefore filter (real SQL) > returns only rows with needed_by strictly before the boundary 9ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByBefore filter (real SQL) > excludes rows with a NULL needed_by — three-valued logic, no special-case needed 8ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByBefore filter (real SQL) > orders date-scoped results by needed_by ascending, not created_at 7ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByBefore filter (real SQL) > breaks an exact needed_by tie on created_at (insertion order), not the random uuid 10ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByBefore filter (real SQL) > orders an UN-SCOPED listPending by needed_by, undated rows last, under the LIMIT 14ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByFrom lower bound (real SQL) > brackets a single day: includes needed_by = from, excludes anything earlier or later 12ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > neededByFrom lower bound (real SQL) > returns [] for a malformed date bound instead of the whole pending list 3ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > scopes listPending by limit, applied in SQL 9ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > does not mark another tenant's item purchased (repo-level tenant check, cross-tenant-null) 4ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > returns null on a second markPurchased call against the same row (double-purchase) 7ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > returns pending items in a stable order across repeated calls (determinism, not insertion-order fidelity — see the created_at,id tiebreak in pg-material-item.ts) 11ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > graceful handling of malformed ids (Task 9: an LLM-invented id must not 500) > listPending returns [] for a non-UUID-shaped tenantId instead of throwing 0ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > graceful handling of malformed ids (Task 9: an LLM-invented id must not 500) > listPending returns [] for a well-formed but unknown tenantId 2ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > graceful handling of malformed ids (Task 9: an LLM-invented id must not 500) > markPurchased returns null for a non-UUID-shaped item id instead of throwing 0ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > graceful handling of malformed ids (Task 9: an LLM-invented id must not 500) > markPurchased returns null for a non-UUID-shaped tenantId instead of throwing 2ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > graceful handling of malformed ids (Task 9: an LLM-invented id must not 500) > listPending returns [] for a non-UUID-shaped jobId instead of throwing or falling back to the tenant's whole pending list 2ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > graceful handling of malformed ids (Task 9: an LLM-invented id must not 500) > markPurchased rejects an empty actorId 3ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > RLS enforcement (DB-level, unprivileged role) > a tenant reading material_items directly sees only its own rows 7ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > RLS enforcement (DB-level, unprivileged role) > an unknown tenant context cannot enumerate any material_items rows 4ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > RLS enforcement (DB-level, unprivileged role) > cannot INSERT a material_items row attributed to another tenant (WITH CHECK) 2ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > RLS enforcement (DB-level, unprivileged role) > an unprivileged role scoped to tenant A cannot UPDATE tenant B's row 5ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > migration 273 — urgency index for listPending (A3) > creates idx_material_items_pending_urgency on the FULL sort key, partial on pending 1ms
✓ test/integration/material-items.test.ts > Postgres integration — material items > migration 273 — urgency index for listPending (A3) > keeps migration 272's idx_material_items_pending — 273 supersedes it but must not DROP it 1ms
```
No ✗.

df check after this run (21st/final): `12Gi used, 2.8Gi avail, 81%`.

Tenant-grade greps:
- `tenantB|otherTenant|secondTenant|cross-tenant|another tenant`:
```
368:  it('does not mark another tenant\'s item purchased (repo-level tenant check, cross-tenant-null)', async () => {
371:      description: 'cross-tenant guard',
498:    it('cannot INSERT a material_items row attributed to another tenant (WITH CHECK)', async () => {
```
- `timezone|business_hours|...` → no match
- `listTenantIds:...` → no match

Counts: pool/testDb=2; InMemoryAuditRepository=0; createTestTenant(=12

---

## Anomalies

1. **Row 5.2 has no matching integration test.** The literal instruction (`ls packages/api/test/integration | grep -i -E "photo|attachment"`) returned zero matches. Job-photo / attachment integration coverage exists elsewhere in the tree — `packages/api/test/jobs/job-photos.test.ts`, `packages/api/test/attachments/pg-attachment.test.ts`, `packages/api/test/attachments/attachment-service.test.ts`, `packages/api/test/routes/attachments.route.test.ts` — but none live under `test/integration/`, so none were run under the given invocation/config. Not evaluated for rung purposes here; flagging for the judging step.
2. **`listTenantIds: async () => [` never matched in any of the 20 files run.** Zero hits across the entire batch — worth double-checking whether that helper/pattern exists anywhere in the codebase at all, or whether the grep pattern doesn't match current code style.
3. **`entity-resolution.test.ts` tenantB-style grep returned 0** despite the file containing numerous "never resolves ... across tenants" test titles — the specific tokens (`tenantB|otherTenant|secondTenant|cross-tenant|another tenant`) just don't appear there; it uses "across tenants" phrasing instead. Not a test defect, just a grep-pattern/phrasing mismatch worth noting for anyone judging cross-tenant coverage by this grep alone.
4. **Known flake (repo #976) did not reproduce.** `onboarding-pack-seed-concurrency.test.ts` passed clean (88ms) on the only run; no `tenant_settings_tenant_id_key` violation observed.
5. **All 21 runs were clean on the first try** — the "if a run fails, re-run once" contingency was never invoked.
6. **`createTestTenant(` counts varied widely with no `config`-grep hits to match** (e.g. `billing-trial.test.ts`: createTestTenant count 5 but 0 config-grep matches; `brand-voice.integration.test.ts` similarly 5/0) — these files build tenants without touching tenant_settings columns named in the config-grep pattern, consistent with being lower-level/service-mock-style tests rather than full HTTP+settings integration tests.
