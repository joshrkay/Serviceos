# G1 §5 Invariants — Docker Integration Test Run Log

Ticket: #1005 (wayfinder G1 entry audit)
Worktree: /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane (branch lane/rung-map-env @ 2c2aa6d90 = origin/main)
Started: 2026-09-12

Initial disk check: `df -h / | tail -1`
```
/dev/disk3s1s1   228Gi    12Gi   1.6Gi    88%    455k   17M    3%   /
```

---
## I1 — integration/create-job-execution.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/create-job-execution.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:19:26
   Duration  3.76s (transform 631ms, setup 0ms, import 856ms, tests 43ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/create-job-execution.test.ts > Postgres integration — voice create_job → approve → execute → persist + audit > persists the job row with real columns 1ms
 ✓ test/integration/create-job-execution.test.ts > Postgres integration — voice create_job → approve → execute → persist + audit > emits exactly one job.created audit event (regression guard for handler+registry audit wiring) 1ms
 ✓ test/integration/create-job-execution.test.ts > Postgres integration — voice create_job → approve → execute → persist + audit > does not expose the job to another tenant (scoped read) 3ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/create-job-execution.test.ts
144:  it('does not expose the job to another tenant (scoped read)', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/create-job-execution.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/create-job-execution.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/create-job-execution.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/create-job-execution.test.ts
0
```

Anomalies: none.

---
## I4 (Docker half) — integration/invoice-pricing-source.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/invoice-pricing-source.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:19:51
   Duration  3.82s (transform 175ms, setup 0ms, import 229ms, tests 118ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — invoice_line_items.pricing_source (migration 255) > round-trips pricingSource on create() → findById() 9ms
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — invoice_line_items.pricing_source (migration 255) > persists pricingSource through update() (DELETE + re-INSERT of line items) 10ms
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — invoice_line_items.pricing_source (migration 255) > carries pricingSource through an approved update_invoice edit (invoice-editor → repo → DB) 10ms
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — invoice_line_items.pricing_source (migration 255) > P1 fix — description-based remove_line_item removes the CORRECT line from a real multi-line invoice 10ms
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — invoice_line_items.pricing_source (migration 255) > P1 fix — an ambiguous description throws a clean error and leaves the invoice untouched 6ms
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — invoice_line_items.pricing_source (migration 255) > rejects an out-of-vocabulary pricing_source via the CHECK constraint 3ms
 ✓ test/integration/invoice-pricing-source.test.ts > Postgres integration — estimate_line_items.pricing_source (edit round-trip) > carries pricingSource through an approved update_estimate edit (estimate-editor → repo → DB) 13ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/invoice-pricing-source.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/invoice-pricing-source.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/invoice-pricing-source.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/invoice-pricing-source.test.ts
3

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/invoice-pricing-source.test.ts
0
```

Anomalies: none.

---
## I5 — integration/chat-entity-resolution.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/chat-entity-resolution.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  20 passed (20)
   Start at  06:20:20
   Duration  5.08s (transform 1.43s, setup 0ms, import 2.05s, tests 732ms, environment 0ms)
```

All test lines (20 total, under 40 threshold — all shown):
```
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > A11 reschedule_appointment by CUSTOMER NAME: chat draft → resolve → approve → execute moves the real row 128ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > lead kind — new SQL, pinned against the real leads table > A26 convert_lead: resolves a real lead row and lifts the leadId gate 26ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > lead kind — new SQL, pinned against the real leads table > A28 mark_lead_lost: same resolution on the other lead intent 23ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > lead kind — new SQL, pinned against the real leads table > a WON or LOST lead is never resolved — converting one again would mint a duplicate 14ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > lead kind — new SQL, pinned against the real leads table > is tenant-scoped: another tenant's lead is invisible 20ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > lead kind — new SQL, pinned against the real leads table > two same-named live leads are AMBIGUOUS — a picker, never a guess 14ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > ambiguity asks ONE question; the next chat turn answers it and approval unblocks 36ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > LIVE ARRANGEMENT: no conversationId on turn 1 (server mints it) — the SAME id returned is what the answer turn must resolve against 34ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > an unknown reference keeps the gate and approval keeps refusing 21ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > appointment resolution — a customer with many jobs (live sweep A12–A15) > A12 cancel_appointment by BARE customer name: gate lifts even with 7 unrelated jobs on the account 48ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > appointment resolution — a customer with many jobs (live sweep A12–A15) > A13 reassign_appointment by "<customer>'s appointment": both technicianId AND appointmentId lift in one pass 49ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > invoice resolution — customer-name traversal (live sweep A20) > A20 send_payment_reminder by CUSTOMER NAME (no document number extracted): gate lifts to the one open invoice 29ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > invoice resolution — customer-name traversal (live sweep A20) > two invoices on the same customer are AMBIGUOUS — one question, never a guess, and approval unblocks after the answer 37ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > invoice resolution — customer-name traversal (live sweep A20) > MORE non-void invoices than the picker can show (sweep 9/10 live shape): the reply says so instead of going silent 51ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > invoice resolution — customer-name traversal (live sweep A20) > A21 apply_late_fee: the same overflow shape gets the same nudge, not silence 25ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > invoice resolution — customer-name traversal (live sweep A20) > A21 apply_late_fee with NO reference at all (the actual live shape) still gets the honest line, never silence 17ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > estimate resolution — send_estimate_nudge (live sweep A19, #909) > A19: MORE sent estimates than the picker can show (sweep 9/10 live shape) — the reply says so instead of going silent 64ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > estimate resolution — send_estimate_nudge (live sweep A19, #909) > a UNIQUE sent estimate resolves the gate unambiguously — no ask needed 31ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > catalog item resolution — update_catalog_item (live sweep A36, #909) > the exact live utterance against a DUPLICATE-named catalog (sweep 9/10 shape): asks instead of dropping the price silently 41ms
 ✓ test/integration/chat-entity-resolution.test.ts > Integration — #909 chat entity resolution (real Postgres + real resolver) > catalog item resolution — update_catalog_item (live sweep A36, #909) > a unique item name resolves the gate unambiguously — no ask needed 22ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/chat-entity-resolution.test.ts
584:    it('is tenant-scoped: another tenant\'s lead is invisible', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/chat-entity-resolution.test.ts
139:      `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
202:      timezone: TZ,
270:      timezone: TZ,

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/chat-entity-resolution.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/chat-entity-resolution.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/chat-entity-resolution.test.ts
0
```

Anomalies: none.

Disk check after 3rd run: `df -h / | tail -1`
```
/dev/disk3s1s1   228Gi    12Gi   1.6Gi    88%    455k   17M    3%   /
```

---
## I10 — integration/live-call-booking-timezone.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/live-call-booking-timezone.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:21:06
   Duration  5.20s (transform 1.52s, setup 0ms, import 2.12s, tests 196ms, environment 0ms)
```

All test lines (verbatim, including an interleaved warn log line — see anomalies):
```
 ✓ test/integration/live-call-booking-timezone.test.ts > Integration — U4 live-call booking resolves in the tenant timezone (real Postgres) > live-call "2:00 PM" for an America/Chicago tenant persists 2pm CHICAGO (19:00Z) — and the memo path persists the IDENTICAL instant 166ms
{"level":"warn","message":"voice payload failed its proposal contract","timestamp":"2026-09-12T13:21:11.641Z","service":"ai.voice-turn.processor","environment":"test","sessionId":"963b674f-3de0-4702-9b6b-73769aaad99c","tenantId":"d909e4c4-0f8c-460c-9c5b-cfec6c7aa2bd","intent":"create_appointment","requestedProposalType":"create_appointment","outcome":"gated_with_missing_fields","errors":["scheduledStart: Required","scheduledEnd: Required"]}
 ✓ test/integration/live-call-booking-timezone.test.ts > Integration — U4 live-call booking resolves in the tenant timezone (real Postgres) > no-tz tenant: the live-call draft gates on the missing window, approval REFUSES, and no appointment row exists (never silent UTC) 28ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/live-call-booking-timezone.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/live-call-booking-timezone.test.ts
2: * U4 (Part E punch #1) — tenant timezone on LIVE-CALL datetime parsing,
9: * tenant's zone — "the July timezone fixes missed this module"
125:describe('Integration — U4 live-call booking resolves in the tenant timezone (real Postgres)', () => {
156:        `INSERT INTO tenant_settings (id, tenant_id, business_name, timezone, region)
331:        return s?.timezone ? { timezone: s.timezone } : undefined;

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/live-call-booking-timezone.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/live-call-booking-timezone.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/live-call-booking-timezone.test.ts
0
```

Anomalies: a single structured JSON warn log line ("voice payload failed its proposal contract") is interleaved between the two ✓ lines in the raw reporter output — this is expected application log output for the second test case (which deliberately exercises the missing-window gate), not a test failure. No ✗/× lines present.

---
## I11 — three files: rls-force-catalog.test.ts, rls-runtime-audit.test.ts, rls-runtime-role.test.ts

### I11a — integration/rls-force-catalog.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/rls-force-catalog.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:21:40
   Duration  2.61s (transform 9ms, setup 0ms, import 25ms, tests 27ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/rls-force-catalog.test.ts > RLS FORCE — runtime catalog property (every tenant_id table) > discovers a substantial set of tenant-scoped tables (sanity: migrations applied) 1ms
 ✓ test/integration/rls-force-catalog.test.ts > RLS FORCE — runtime catalog property (every tenant_id table) > every tenant_id table (except the two documented exemptions) has RLS ENABLED and FORCED at runtime 0ms
 ✓ test/integration/rls-force-catalog.test.ts > RLS FORCE — runtime catalog property (every tenant_id table) > the runtime exemption set is EXACTLY the two documented tables (never silently grows) 0ms
 ✓ test/integration/rls-force-catalog.test.ts > RLS FORCE — runtime catalog property (every tenant_id table) > each documented exemption really exists in the catalog with a tenant_id column 0ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/rls-force-catalog.test.ts
85:      )}. A tenant-path query under the NOBYPASSRLS app role would read EVERY tenant's rows — a cross-tenant leak. Add a migration: ALTER TABLE <t> ENABLE ROW LEVEL SECURITY; ALTER TABLE <t> FORCE ROW LEVEL SECURITY; + a tenant_isolation policy.`,

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/rls-force-catalog.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/rls-force-catalog.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/rls-force-catalog.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/rls-force-catalog.test.ts
0
```

createTestTenant count:
```
$ grep -nE "createTestTenant\(" packages/api/test/integration/rls-force-catalog.test.ts | wc -l
       0
```

Anomalies: none.

### I11b — integration/rls-runtime-audit.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/rls-runtime-audit.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:21:50
   Duration  2.07s (transform 50ms, setup 0ms, import 74ms, tests 64ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/rls-runtime-audit.test.ts > runtime RLS audit — live pg catalog > every tenant_id table has RLS enabled AND forced 31ms
 ✓ test/integration/rls-runtime-audit.test.ts > runtime RLS audit — live pg catalog > every tenant_id table has at least one policy in pg_policies 5ms
 ✓ test/integration/rls-runtime-audit.test.ts > runtime RLS audit — live pg catalog > migration 219 revokes every rls_app_runtime grant on a tenant_id table that lacks RLS (deny-list) 10ms
 ✓ test/integration/rls-runtime-audit.test.ts > runtime RLS audit — live pg catalog > cross-tenant SELECT through the tenant GUC returns zero rows for a non-superuser 16ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/rls-runtime-audit.test.ts
122:  it('cross-tenant SELECT through the tenant GUC returns zero rows for a non-superuser', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/rls-runtime-audit.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/rls-runtime-audit.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/rls-runtime-audit.test.ts
5

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/rls-runtime-audit.test.ts
0
```

createTestTenant count:
```
$ grep -nE "createTestTenant\(" packages/api/test/integration/rls-runtime-audit.test.ts | wc -l
       1
```

Anomalies: none.

### I11c — integration/rls-runtime-role.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/rls-runtime-role.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:22:00
   Duration  2.26s (transform 118ms, setup 0ms, import 237ms, tests 69ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role enforcement (real Postgres, RLS_RUNTIME_ROLE=true) > R1: a tenant-filter-less query under the role sees only the active tenant 29ms
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role enforcement (real Postgres, RLS_RUNTIME_ROLE=true) > R2: an unset tenant GUC under the role fails closed (not all rows) 4ms
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role enforcement (real Postgres, RLS_RUNTIME_ROLE=true) > R3: normal same-tenant read + write succeed under the role (no missing grant) 4ms
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role enforcement (real Postgres, RLS_RUNTIME_ROLE=true) > R4: the privileged path (no role drop) still sees all tenants 4ms
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role enforcement (real Postgres, RLS_RUNTIME_ROLE=true) > pool hygiene: clearTenantContext returns the connection to the privileged role 4ms
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role: system-level lookups set their GUC transactionally (SEC-02) > S1: PgPortalSessionRepository.findByTokenHash returns the row under the enforcing role 15ms
 ✓ test/integration/rls-runtime-role.test.ts > RLS runtime-role: system-level lookups set their GUC transactionally (SEC-02) > S2: AccountingIntegrationRepository.findAllActive returns active integrations under the enforcing role 7ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/rls-runtime-role.test.ts
6: * cannot see another tenant's rows even when the SQL omits a tenant_id filter,

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/rls-runtime-role.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/rls-runtime-role.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/rls-runtime-role.test.ts
3

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/rls-runtime-role.test.ts
0
```

createTestTenant count:
```
$ grep -nE "createTestTenant\(" packages/api/test/integration/rls-runtime-role.test.ts | wc -l
      12
```

Anomalies: none.

Disk check after 6th run: `df -h / | tail -1`
```
/dev/disk3s1s1   228Gi    12Gi   1.9Gi    86%    455k   20M    2%   /
```

---
## I12 — integration/executor-audit-atomicity.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/executor-audit-atomicity.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:22:47
   Duration  2.96s (transform 161ms, setup 0ms, import 214ms, tests 84ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/executor-audit-atomicity.test.ts > ProposalExecutor — WS11 audit-event atomicity > happy path: exactly one proposal.executed audit row commits atomically with the state change 49ms
 ✓ test/integration/executor-audit-atomicity.test.ts > ProposalExecutor — WS11 audit-event atomicity > audit-insert failure rolls back the WHOLE unit: no state change commits without its audit row 18ms
 ✓ test/integration/executor-audit-atomicity.test.ts > ProposalExecutor — WS11 audit-event atomicity > handler failure commits the proposal.execution_failed audit row atomically with the status write 15ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/executor-audit-atomicity.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/executor-audit-atomicity.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/executor-audit-atomicity.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/executor-audit-atomicity.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/executor-audit-atomicity.test.ts
0
```

createTestTenant count:
```
$ grep -nE "createTestTenant\(" packages/api/test/integration/executor-audit-atomicity.test.ts | wc -l
       3
```

Anomalies: none.

---
## I14 — two files: consent-cross-channel.test.ts, stop-reply-unify.test.ts

### I14a — integration/consent-cross-channel.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/consent-cross-channel.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  6 passed (6)
   Start at  06:23:11
   Duration  2.63s (transform 111ms, setup 0ms, import 154ms, tests 117ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/consent-cross-channel.test.ts > Postgres integration — WS12 one consent model (cross-channel) > an SMS STOP blocks the CALL even when consent_status still reads granted 23ms
 ✓ test/integration/consent-cross-channel.test.ts > Postgres integration — WS12 one consent model (cross-channel) > a manual sms_consent opt-out (updateCustomer + ledger) suppresses SMS at the gate 20ms
 ✓ test/integration/consent-cross-channel.test.ts > Postgres integration — WS12 one consent model (cross-channel) > a recording objection does NOT suppress SMS (kind-scoping) 9ms
 ✓ test/integration/consent-cross-channel.test.ts > Postgres integration — WS12 one consent model (cross-channel) > SMS re-opt-in (ledger grant) restores SMS but never grants the voice rollup 25ms
 ✓ test/integration/consent-cross-channel.test.ts > Postgres integration — WS12 one consent model (cross-channel) > a granted voice customer with a clean ledger can still be texted AND called 10ms
 ✓ test/integration/consent-cross-channel.test.ts > Postgres integration — WS12 one consent model (cross-channel) > suppression still surfaces as SmsSuppressedError (caller contract intact) 11ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/consent-cross-channel.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/consent-cross-channel.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/consent-cross-channel.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/consent-cross-channel.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/consent-cross-channel.test.ts
0
```

Anomalies: none.

### I14b — integration/stop-reply-unify.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/stop-reply-unify.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  3 passed (3)
   Start at  06:23:22
   Duration  2.66s (transform 113ms, setup 0ms, import 151ms, tests 92ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/stop-reply-unify.test.ts > Postgres integration — STOP/START opt-out unification > STOP updates DNC, the consent ledger, and the customer rollup together 24ms
 ✓ test/integration/stop-reply-unify.test.ts > Postgres integration — STOP/START opt-out unification > START clears DNC + ledgers the re-grant, but never re-grants the voice rollup (WS12/D-017) 29ms
 ✓ test/integration/stop-reply-unify.test.ts > Postgres integration — STOP/START opt-out unification > does not roll up consent onto another tenant sharing the number 19ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/stop-reply-unify.test.ts
4: * and the customers.consent_status rollup. Also proves cross-tenant isolation
102:  it('does not roll up consent onto another tenant sharing the number', async () => {

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/stop-reply-unify.test.ts
no match

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/stop-reply-unify.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/stop-reply-unify.test.ts
2

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/stop-reply-unify.test.ts
0
```

Anomalies: none.

Disk check after 9th run: `df -h / | tail -1`
```
/dev/disk3s1s1   228Gi    12Gi   1.9Gi    86%    455k   20M    2%   /
```

---
## I17 (Docker half) — integration/settings-autonomous-booking.test.ts

Command:
```
cd /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api && DOCKER_HOST=unix:///Users/joshuakay/.colima/default/docker.sock TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock RLS_RUNTIME_ROLE=true npx vitest run --config vitest.integration.config.ts --reporter=verbose integration/settings-autonomous-booking.test.ts
```

Summary:
```
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  06:23:56
   Duration  2.47s (transform 84ms, setup 0ms, import 123ms, tests 53ms, environment 0ms)
```

All test lines:
```
 ✓ test/integration/settings-autonomous-booking.test.ts > Postgres integration — autonomous booking settings (UB-D / migration 231) > fresh row reads the migration defaults (enabled=false, threshold=0.95) 27ms
 ✓ test/integration/settings-autonomous-booking.test.ts > Postgres integration — autonomous booking settings (UB-D / migration 231) > round-trips enabled + threshold through the repo (real column names) 11ms
 ✓ test/integration/settings-autonomous-booking.test.ts > Postgres integration — autonomous booking settings (UB-D / migration 231) > DB CHECK rejects a threshold below 0.90 (raw UPDATE, app validation bypassed) 9ms
 ✓ test/integration/settings-autonomous-booking.test.ts > Postgres integration — autonomous booking settings (UB-D / migration 231) > DB CHECK rejects a threshold above 0.99 (raw UPDATE) 4ms
```

Tenant-grade greps:
```
$ grep -nE "tenantB|otherTenant|secondTenant|cross-tenant|another tenant" packages/api/test/integration/settings-autonomous-booking.test.ts
no match

$ grep -nE "timezone|business_hours|labor_rate|threshold|pack|digest_enabled" packages/api/test/integration/settings-autonomous-booking.test.ts
7: *   - autonomous_booking_enabled / autonomous_booking_threshold round-trip
10: *   - Column defaults: a fresh row reads enabled=false, threshold=0.95.
11: *   - The DB CHECK rejects thresholds outside [0.90, 0.99] (raw UPDATE,
30:    timezone: 'America/New_York',
55:  it('fresh row reads the migration defaults (enabled=false, threshold=0.95)', async () => {
64:  it('round-trips enabled + threshold through the repo (real column names)', async () => {
85:        `SELECT autonomous_booking_enabled, autonomous_booking_threshold
90:      expect(Number(rows[0].autonomous_booking_threshold)).toBe(0.97);
96:  it('DB CHECK rejects a threshold below 0.90 (raw UPDATE, app validation bypassed)', async () => {
103:          `UPDATE tenant_settings SET autonomous_booking_threshold = 0.89 WHERE tenant_id = $1`,
112:  it('DB CHECK rejects a threshold above 0.99 (raw UPDATE)', async () => {
119:          `UPDATE tenant_settings SET autonomous_booking_threshold = 1.00 WHERE tenant_id = $1`,

$ grep -nE "listTenantIds:\s*async\s*\(\)\s*=>\s*\[" packages/api/test/integration/settings-autonomous-booking.test.ts
no match
```

Pool/InMemory greps:
```
$ grep -c "getSharedTestDb\|TEST_DB_URL\|new Pool(\|withTestDb\|testDb" packages/api/test/integration/settings-autonomous-booking.test.ts
4

$ grep -c "InMemoryAuditRepository" packages/api/test/integration/settings-autonomous-booking.test.ts
0
```

Anomalies: none.

Final disk check: `df -h / | tail -1`
```
/dev/disk3s1s1   228Gi    12Gi   1.9Gi    86%    455k   20M    2%   /
```

---

## All runs complete. No re-runs were needed (all files passed on first attempt).
