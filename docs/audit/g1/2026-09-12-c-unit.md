# G1 Entry Audit — §8.2 Capture / §8.3 Book — Unit-runner lane (rows 2.2–3.12)

Worktree: `/Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane`
Branch: `lane/rung-map-env` @ `2c2aa6d90` (= origin/main)
Runner: `cd packages/api && npx vitest run --reporter=verbose <path>` (vitest v4.1.10)

Rows 2.1, 2.3, 2.8, 2.10, 3.1, 3.2, 3.5, 3.6, 3.7, 3.10 are Docker-lane — SKIPPED, not run.

---

## Row 2.2 (S) — recording-consent ordering

Commands (from `packages/api/`):
```
grep -c "new Pool(" test/integration/conversation-consent-ordering.test.ts
grep -n "vi.fn()" test/integration/conversation-consent-ordering.test.ts | head -5
sed -n '1,25p' test/integration/conversation-consent-ordering.test.ts
```

`grep -c "new Pool("` → `0`

`grep -n "vi.fn()" | head -5`:
```
111:    send: vi.fn(),
112:    finish: vi.fn(),
113:    destroy: vi.fn(),
159:    gateway: { complete: vi.fn() } as never,
```

Header (`sed -n '1,25p'`), verbatim — the file's own claim about Postgres is the last line of the doc comment:
```
/**
 * Recording-consent ordering — the recording disclosure always completes
 * before any caller audio is captured, on BOTH telephony transports, and a
 * disclosure-init failure hangs up instead of continuing to record.
 *
 * Three independently-asserted paths:
 *   1. Gather/PSTN — buildTwiML emits the disclosure `<Say>` BEFORE the async
 *      `<Start><Record>` block, so Twilio speaks the notice before it arms
 *      recording (twilio-adapter.ts).
 *   2. Media Streams — the WS adapter does NOT forward inbound `media` frames
 *      to Deepgram until `initializeSession` (which runs disclosure) resolves;
 *      frames that arrive while the disclosure is still playing are dropped
 *      (mediastream-adapter.ts).
 *   3. DISCLOSURE_INIT_FAILED — when `initializeSession` throws, the leg is
 *      HUNG UP (WS closed) rather than left recording an undisclosed caller.
 *
 * The suite prints a single machine-greppable gate line summarising the two
 * failure counters across all three paths:
 *   CONSENT-ORDER: <n> armed-before-disclosure, <n> fail-open
 *
 * Both counters must be 0. This lives under test/integration/ (following the
 * conversation-*.test.ts convention) so it runs in the same PR CI lane; it is
 * a pure adapter-behavior check and does not touch Postgres.
 */
```
→ confirms: **"it is a pure adapter-behavior check and does not touch Postgres."**

Attempted unit-runner execution:
```
npx vitest run --reporter=verbose test/integration/conversation-consent-ordering.test.ts
```
Output (verbatim):
```
 RUN  v4.1.10 /Users/joshuakay/.config/superpowers/worktrees/Serviceos/rung-map-lane/packages/api

No test files found, exiting with code 1

filter: test/integration/conversation-consent-ordering.test.ts
include: test/**/*.test.ts
exclude:  node_modules/**, dist/**, test/integration/**, test/voice-quality/voice-quality.test.ts
```

**Anomaly / result: the unit config EXCLUDES `test/integration/**` — could NOT execute this file via the unit runner**, despite the file itself being DB-free. Exclusion line from `packages/api/vitest.config.ts`:
```
    exclude: [
      'node_modules/**',
      'dist/**',
      'test/integration/**',
      // Layer 1 corpus runner — only via vitest.voice-quality.config.ts + npm run voice-quality
      'test/voice-quality/voice-quality.test.ts',
    ],
```
with the adjacent comment:
```
    // Integration tests run under vitest.integration.config.ts, which owns
    // the testcontainer lifecycle via globalSetup. Excluding them here means
    // `npm test` (and `npm run test:coverage`) cover unit tests only and
    // don't need Docker.
```
So the file is a NO-DB file (`new Pool(` count 0, ledger write is `vi.fn()`) but is nonetheless walled off from the unit lane purely by its directory path (`test/integration/`), not by any DB dependency. No pass/fail counts to report — it never ran.

---

## Row 2.4 (S) — off-surface intent classification

```
grep -rln "intent_off_surface" test/integration/
```
→ (no output, exit code 1 = no match). Confirms PRD: **empty** in `test/integration/`.

```
grep -rln "intent_off_surface" test src | head
```
(run from `packages/api/`) →
```
test/telephony/twilio-adapter.test.ts
test/voice/inbound-caller-booking-execution.test.ts
test/ai/voice-turn/voice-turn-processor.test.ts
test/ai/orchestration/classifier-profile.test.ts
test/workers/voice-action-router.test.ts
src/ai/voice-turn/create-voice-turn-processor.ts
src/ai/orchestration/classifier-profile.ts
src/ai/orchestration/intent-classifier.ts
src/proposals/voice-intent-map.ts
```
So it IS implemented (classifier-profile.ts, intent-classifier.ts, voice-intent-map.ts, create-voice-turn-processor.ts) and IS unit-tested (4 test files above), just never referenced from `test/integration/`. No test run performed for this row per the task's own confirm text ("fixture-proven only").

---

## Row 2.5 — emergency tier (E1/E2/E3) unit files

```
ls test/ai/agents/customer-calling/ | grep -i -E "emergency|triage|e1"
```
→
```
emergency-detector.test.ts
emergency-tier-transitions.test.ts
emergency-tier.test.ts
vulnerability-triage-hook.test.ts
```

Command:
```
npx vitest run --reporter=verbose \
  test/ai/agents/customer-calling/emergency-detector.test.ts \
  test/ai/agents/customer-calling/emergency-tier.test.ts \
  test/ai/agents/customer-calling/emergency-tier-transitions.test.ts \
  test/ai/agents/customer-calling/complaint-guardrail.test.ts
```

Runner summary (verbatim):
```
 Test Files  4 passed (4)
      Tests  107 passed (107)
   Start at  06:24:39
   Duration  233ms (transform 183ms, setup 0ms, import 273ms, tests 118ms, environment 0ms)
```

✓ lines (all 107 passed; showing first 40, remainder identical pattern — all ✓, no ✗):
```
 ✓ test/ai/agents/customer-calling/complaint-guardrail.test.ts > complaint guardrail global guard (escalates, D-027) > acknowledges, escalates to a human, and emits one paper-trail create_proposal 2ms
 ✓ test/ai/agents/customer-calling/complaint-guardrail.test.ts > complaint guardrail global guard (escalates, D-027) > emits the quality event for the transports that execute it 0ms
 ✓ test/ai/agents/customer-calling/complaint-guardrail.test.ts > complaint guardrail global guard (escalates, D-027) > escalates from closing too — the guard is global across live states 0ms
 ✓ test/ai/agents/customer-calling/complaint-guardrail.test.ts > complaint guardrail global guard (escalates, D-027) > creates no second follow-up when already flagged (paper trail is one-shot) 0ms
 ✓ test/ai/agents/customer-calling/complaint-guardrail.test.ts > complaint guardrail global guard (escalates, D-027) > no-ops once the call is escalating or terminated (idempotent, like operator_request) 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from greeting: E1 goes terminal, never books, never bridges, revokes + notifies 2ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from identifying: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from ask_caller: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from intent_capture: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from entity_resolution: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from intent_confirm: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from proposal_draft: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > from closing: E1 goes terminal, never books, never bridges, revokes + notifies 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > speaks the reviewed life-safety script first, tagged priority:safety 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > audits the call as an E1 life-safety event 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E1 — life safety never books, from any state > is idempotent once terminated (no double action) 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E2 — existing dispatcher-escalation behavior is unchanged > tier=undefined escalates with dispatcher bridge + emergency_dispatch proposal (regression guard) 0ms
 ✓ test/ai/agents/customer-calling/emergency-tier-transitions.test.ts > ANS-001 E2 — existing dispatcher-escalation behavior is unchanged > tier=E2 escalates with dispatcher bridge + emergency_dispatch proposal (regression guard) 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "I think we have a gas leak in the basement" 1ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "it smells like gas down here" 1ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "my carbon monoxide alarm is going off" 2ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "the water heater caught fire" 1ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "the outlet is sparking" 4ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "I smell electrical burning from the panel" 4ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "the basement is flooding fast" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches "we have a burst pipe upstairs" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > matches the compound no-heat + at-risk phrasing 12ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > does NOT match a plain no-heat report without the risk phrasing 19ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > does NOT match routine scheduling language 7ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > is case-insensitive and word-bounded 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > keyword table is non-empty platform defaults (per-tenant merge is out of scope) 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > RV-140 — detectEmergency (deterministic keyword scan) > safety line references 911 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "creo que hay una fuga de gas en el sótano" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "huele a gas aquí abajo" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "la alarma de monóxido de carbono está sonando" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "la alarma de monoxido de carbono esta sonando" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "hay un incendio en la cocina" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "el calentador se está quemando" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "el calentador se esta quemando ahora" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "tenemos una inundación en el sótano" 0ms
 ✓ test/ai/agents/customer-calling/emergency-detector.test.ts > UB-C3 — detectEmergency Spanish keywords > matches "el sótano está inundado" 0ms
```
(...67 more ✓ lines omitted, all passing — full 107/107; includes `e1ScriptReadiness (boot gate) > reports NOT ready while E1_SCRIPT_REVIEW_REQUIRED is true, with a prominent message` ✓ and `e1ScriptReadiness (boot gate) > never throws (pure, boot-safe)` ✓, both PASS, confirming the flag exists and correctly reports NOT-ready.)

No ✗ lines. 0 failures.

```
grep -rln "E1_SCRIPT_REVIEW_REQUIRED\|LIFE_SAFETY_E1_SCRIPT" src | head
```
→
```
src/app.ts
src/settings/pg-settings.ts
src/settings/settings.ts
src/db/schema.ts
src/telephony/twilio-adapter.ts
src/ai/agents/customer-calling/emergency-tier.ts
```
Confirms PRD note: "the E1 script is still a self-declared placeholder" (the boot-gate test explicitly asserts `e1ScriptReadiness` reports NOT ready while this flag is true).

Tenant-grade greps (all 4 files) — all **no match**, except one string-literal coincidence:
```
##### emergency-detector.test.ts ##### — grep1: no match / grep2: no match / grep3: no match
##### emergency-tier.test.ts ##### — grep1: no match / grep2 (one hit, not a tenant-config match, just a doc comment):
  109:  // The Docker runtime image ships only packages/api/dist — corpus/data is
  (matched on "pack" substring in "packages") / grep3: no match
##### emergency-tier-transitions.test.ts ##### — grep1: no match / grep2: no match / grep3: no match
##### complaint-guardrail.test.ts ##### — grep1: no match / grep2: no match / grep3: no match
```

---

## Row 2.6 (U) — vulnerability triage hook

```
npx vitest run --reporter=verbose test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts
```

stderr (verbatim, expected — these are the "fails closed"/"swallowed" negative-path tests logging their warn):
```
{"level":"warn","message":"vulnerability triage: patch action failed","timestamp":"2026-09-12T13:24:44.394Z","service":"ai.vulnerability-triage-hook","environment":"test","tenantId":"t1","sessionId":"98b38c9b-118f-4494-9896-65b4cb835c13","error":"twilio down"}
{"level":"warn","message":"vulnerability triage: outcome persist failed","timestamp":"2026-09-12T13:24:44.395Z","service":"ai.vulnerability-triage-hook","environment":"test","tenantId":"t1","sessionId":"ac43eead-c7f5-44f1-8dab-83f546459b14","error":"pg down"}
{"level":"warn","message":"vulnerability triage: mark-customer-vulnerable failed","timestamp":"2026-09-12T13:24:44.397Z","service":"ai.vulnerability-triage-hook","environment":"test","tenantId":"t1","sessionId":"739bb146-3c44-429a-a635-748126ddcc8b","error":"proposal repo down"}
```

✓ lines (all 13, verbatim):
```
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > does nothing when the tenant flag is off (no grade call, no rows) 3ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > fails closed when the flag lookup throws 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > persists EVERY evaluateTriage outcome with signals (RV-120) 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > a vulnerable but non-urgent grade records high_priority_booking 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > invokes the patch action for patch_owner decisions and records what it did (RV-121) 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > records patch_owner:failed when the patch action throws (outcome still persisted) 1ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > zero grade (score 0, tier none) is not persisted — signal log, not firehose 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > profanity-only counter-example: angry caller grades zero → nothing fires 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-120/RV-122 — vulnerability triage hook > a persist failure is swallowed (the call is never disturbed) 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > gradeToMatrixScore > uses discrete signal weights when present 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > gradeToMatrixScore > falls back to the threshold mapping when no signals landed 0ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-123 — markCustomerVulnerable wiring in the hook > fires for non-normal decisions on identified callers, never for normal/anonymous 1ms
 ✓ test/ai/agents/customer-calling/vulnerability-triage-hook.test.ts > RV-123 — markCustomerVulnerable wiring in the hook > a mark failure is swallowed and the triage event still persists 0ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  13 passed (13)
   Start at  06:24:44
   Duration  312ms (transform 162ms, setup 0ms, import 227ms, tests 7ms, environment 0ms)
```
0 failures.

```
grep -rn "vulnerability_triage\|vulnerabilityTriage" src/flags src/routes
```
→ (no output — empty; the flag name isn't referenced under those two dirs specifically).

Broader search (src, all dirs) for context:
```
src/app.ts:4207:      // itself on the per-tenant `voice_vulnerability_triage` flag, grades
src/app.ts:4226:      const vulnerabilityTriageHookDep = llmGateway
src/app.ts:4307:                    reason: 'vulnerability_triage',
src/app.ts:4321:      // voice_vulnerability_triage flag.
src/app.ts:4322:      if (vulnerabilityTriageHookDep) {
src/app.ts:4323:        twilioAdapter.setVulnerabilityTriageHook(vulnerabilityTriageHookDep);
src/telephony/twilio-adapter.ts:161:   * `voice_vulnerability_triage` flag (fail-closed). When undefined, the
src/telephony/twilio-adapter.ts:165:  vulnerabilityTriageHook?: VulnerabilityTriageHook;
src/telephony/twilio-adapter.ts:2270:      this.deps.vulnerabilityTriageHook &&
src/telephony/twilio-adapter.ts:2275:        .vulnerabilityTriageHook({
src/telephony/twilio-adapter.ts:3208:    this.deps.vulnerabilityTriageHook = hook;
src/telephony/media-streams/mediastream-adapter.ts:369:   * gate (`voice_vulnerability_triage`), grading, triage persistence
src/telephony/media-streams/mediastream-adapter.ts:373:  vulnerabilityTriageHook?: (args: {
src/telephony/media-streams/mediastream-adapter.ts:1782:      this.deps.vulnerabilityTriageHook
src/telephony/media-streams/mediastream-adapter.ts:1785:        .vulnerabilityTriageHook({
src/ai/agents/customer-calling/vulnerability-triage-hook.ts:7: *   1. Gates on the per-tenant `voice_vulnerability_triage` flag
src/ai/agents/customer-calling/vulnerability-triage-hook.ts:44:export const VOICE_VULNERABILITY_TRIAGE_FLAG = 'voice_vulnerability_triage';
```
Flag reader: `src/ai/agents/customer-calling/vulnerability-triage-hook.ts:113` — `enabled = await deps.isEnabledForTenant(tenantId, VOICE_VULNERABILITY_TRIAGE_FLAG)`.

`setTenantFlag` (non-test) across `packages/api/src packages/web/src`:
```
packages/api/src/flags/pg-tenant-feature-flags.ts:21: * setTenantFlag busts the local cache immediately so the writing process
packages/api/src/flags/pg-tenant-feature-flags.ts:76:   * setTenantFlag busts the local cache immediately for the affected key.
packages/api/src/flags/pg-tenant-feature-flags.ts:173:  async setTenantFlag(
```
→ **only the method definition; zero callers** in either `src` tree. Confirms PRD: "its dedicated flag writer (`setTenantFlag`) is unwired."

Tenant-grade greps on the proving file:
```
-- grep1 (tenantB|otherTenant|...) -- no match
-- grep2 (timezone|business_hours|...) -- 188:  it('falls back to the threshold mapping when no signals landed', () => {
-- grep3 (listTenantIds: async () => [...]) -- no match
```

---

## Row 2.7 (structural, admin-API-only) — dropped-call recovery SMS

```
grep -rn "setTenantFlag" packages/api/src packages/web/src | grep -v "\.test\."
```
→
```
packages/api/src/flags/pg-tenant-feature-flags.ts:21: * setTenantFlag busts the local cache immediately so the writing process
packages/api/src/flags/pg-tenant-feature-flags.ts:76:   * setTenantFlag busts the local cache immediately for the affected key.
packages/api/src/flags/pg-tenant-feature-flags.ts:173:  async setTenantFlag(
```
→ confirms **zero production callers** anywhere in api or web `src`.

```
grep -n "tenantIds" packages/api/src/routes/feature-flags.ts packages/api/src/flags/feature-flags.ts
```
→
```
packages/api/src/flags/feature-flags.ts:5:  tenantIds?: string[];
packages/api/src/flags/feature-flags.ts:58:  if (flag.tenantIds && flag.tenantIds.length > 0) {
packages/api/src/flags/feature-flags.ts:59:    if (!context.tenantId || !flag.tenantIds.includes(context.tenantId)) return false;
packages/api/src/routes/feature-flags.ts:23:  tenantIds: z.array(z.string()).optional(),
packages/api/src/routes/feature-flags.ts:98:        tenantIds: parsed.tenantIds,
packages/api/src/routes/feature-flags.ts:126:                tenantIds: saved.tenantIds,
```
→ confirms the admin-ramp-by-`tenantIds`, platform-flag mechanism is real and present.

```
ls test/integration | grep -i -E "dropped|recovery"
```
→ `dropped-call-worker.test.ts` — **Docker file, listed only, not run** (owned by another lane).

No unit-runner execution for this row (structural/admin-API-only + Docker-gated pipeline test — out of scope for this lane).

---

## Row 2.9 — public self-service `/book` (in-memory supertest)

```
grep -rln "/book" test --include='*.ts' | grep -v integration | head
```
→
```
test/settings/business-phone.test.ts
test/portal/portal-booking.test.ts
test/app/route-manifest.test.ts
test/scheduling/booking-availability.test.ts
test/scheduling/appointment-scheduling.launch.test.ts
test/voice/spanish-booking-rate.test.ts
test/ai/voice-turn/emergency-immediate-dial.test.ts
test/ai/voice-turn/inbound-booking-completion.test.ts
test/ai/agents/customer-calling/emergency-tier-transitions.test.ts
test/routes/public-booking.route.test.ts
```
Inspection narrowed the actual public-page `/book` route test to `test/routes/public-booking.route.test.ts` (`describe('POST /:tenantId>` under `describe('public-booking route'` mounted at `/api/public/booking`); the other files matched on the substring `/book` incidentally (unrelated routes, imports, or comments).

```
npx vitest run --reporter=verbose test/routes/public-booking.route.test.ts
```
✓ lines (all 18, verbatim):
```
 ✓ test/routes/public-booking.route.test.ts > public-booking route > GET /:tenantId/availability > returns open slots in the tenant timezone 29ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > GET /:tenantId/availability > returns 404 for an unknown tenant 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > GET /:tenantId/availability > returns 400 for a malformed tenant id 1ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > GET /:tenantId/availability > returns 400 for a malformed date range 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > creates a customer, location, job, held appointment, and a create_booking proposal 10ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > rejects a second booking of the same slot with 409 + alternatives (no double-book) 5ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > rejects a slot in the past 1ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > rejects an out-of-bounds booking duration (multi-day hold) 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > rejects a slot outside business hours (e.g. 02:00) the UI would never offer 1ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > rejects a payload missing both phone and email 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > rejects a payload missing the service address 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > honeypot returns 200 and writes nothing 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > does not leak across tenants — a booking for tenant A is invisible to tenant B 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > POST /:tenantId > returns 404 for an unknown tenant 1ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > service area (F2 term 5) > rejects an out-of-area postal code when a ZIP allowlist is configured 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > service area (F2 term 5) > accepts an in-area postal code against the configured allowlist 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > service area (F2 term 5) > accepts any postal code when no allowlist is configured (unbounded area) 2ms
 ✓ test/routes/public-booking.route.test.ts > public-booking route > Codex P1 — self-service booking gates on a CHOSEN timezone > an unset tenant timezone makes availability and booking 409 instead of guessing Eastern 1ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  18 passed (18)
   Start at  06:25:20
   Duration  420ms (transform 177ms, setup 0ms, import 271ms, tests 68ms, environment 0ms)
```
0 failures — matches file uses `request(app)` supertest, confirming "in-memory supertest only."

Tenant-grade greps on `test/routes/public-booking.route.test.ts`:
```
-- grep1 -- no match
-- grep2 --
51:    // local-timezone drift.
56:      timezone: 'UTC',
111:    it('returns open slots in the tenant timezone', async () => {
116:      expect(res.body.timezone).toBe('UTC');
305:  describe('Codex P1 — self-service booking gates on a CHOSEN timezone', () => {
306:    it('an unset tenant timezone makes availability and booking 409 instead of guessing Eastern', async () => {
-- grep3 -- no match
```

---

## Row 2.11 — negotiation/discount guardrails (all unit)

```
ls test/proposals/guardrails/
```
→
```
discount-evaluator.test.ts
negotiation-guardrail.test.ts
negotiation-invariant.test.ts
```

```
npx vitest run --reporter=verbose \
  test/proposals/guardrails/negotiation-invariant.test.ts \
  test/proposals/guardrails/discount-evaluator.test.ts \
  test/proposals/guardrails/negotiation-guardrail.test.ts
```
Runner summary:
```
 Test Files  3 passed (3)
      Tests  59 passed (59)
   Start at  06:25:30
   Duration  267ms (transform 341ms, setup 0ms, import 511ms, tests 19ms, environment 0ms)
```
✓ lines (first 40 of 59):
```
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > ambiguous parse → CLARIFY (step 1, before everything else) 2ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > ambiguous wins even on an ungrounded quote (order: step 1 before step 3) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > currentQuotedCents 0 → CLARIFY (step 2: no base to evaluate) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > negative currentQuotedCents → CLARIFY (step 2) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > requestedFinal === quoted (not a discount) → CLARIFY (step 5) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > requestedFinal > quoted (a price increase) → CLARIFY (step 5) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > requestedFinal <= 0 (e.g. amount off >= quote) → CLARIFY (step 5) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — CLARIFY guards (steps 1, 2, 5) > requestedFinal negative (amount off > quote) → CLARIFY (step 5) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — fail-closed (maxDiscountBps 0) ≡ V1 > KEY PROPERTY: policyAllowsCents === currentQuotedCents at maxDiscountBps 0 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — fail-closed (maxDiscountBps 0) ≡ V1 > a tiny 1% ask still escalates under fail-closed 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — fail-closed (maxDiscountBps 0) ≡ V1 > a large 50% ask escalates (no floor configured → not a counter) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > 8% ask → $230 → ALLOW 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > exactly-at-cap: 10% → $225, requestedFinal === policyAllows → ALLOW (boundary) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > 12% ask → $220 → NEEDS_APPROVAL (over the 10% cap, below policyAllows) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > below floor: target $100 → REJECT_WITH_COUNTER at floor $150 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > one cent below the floor → REJECT_WITH_COUNTER (boundary) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > exactly at the floor → NOT rejected; routes by policy (here NEEDS_APPROVAL) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — configured policy (cap 10%, floor $150, quote $250) > floor dominates the policy cap (stricter-of-both): within-cap ask still allowed only above floor 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — ungrounded quote (step 3, before normalization) > within-policy ask still → NEEDS_APPROVAL when !catalogGrounded 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — ungrounded quote (step 3, before normalization) > target_price ungrounded → echoes target, null bps 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — ungrounded quote (step 3, before normalization) > discount_amount ungrounded → derives target, null bps 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — parsed-input variants normalize correctly (step 4) > discount_amount maps to currentQuoted - amount 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — parsed-input variants normalize correctly (step 4) > discount_percent maps via applyBps (no float drift) 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — parsed-input variants normalize correctly (step 4) > target_price maps to the named price directly 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — combined-discount sanity (step 7) > member 9000 bps + requested 1500 bps (≥ 10000 combined) → REJECT_WITH_COUNTER 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — combined-discount sanity (step 7) > combined just under 100% does NOT trip the sanity guard 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — combined-discount sanity (step 7) > sanity counter uses zero floor when no floors configured 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — catalog floor + neverBelowCatalog interaction > neverBelowCatalog true: catalogFloorCents raises the effective floor 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — catalog floor + neverBelowCatalog interaction > neverBelowCatalog false: catalogFloorCents is ignored 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — catalog floor + neverBelowCatalog interaction > stricter of absolute vs catalog floor wins when both set 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — schema compliance across all decision kinds > 'ALLOW' decision validates against discountDecisionSchema 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — schema compliance across all decision kinds > 'NEEDS_APPROVAL' decision validates against discountDecisionSchema 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — schema compliance across all decision kinds > 'CLARIFY' decision validates against discountDecisionSchema 0ms
 ✓ test/proposals/guardrails/discount-evaluator.test.ts > evaluateDiscountAsk — schema compliance across all decision kinds > 'REJECT_WITH_COUNTER' decision validates against discountDecisionSchema 0ms
 ✓ test/proposals/guardrails/negotiation-invariant.test.ts > P2-036 negotiation guardrail invariant > only ever emits a capture-class callback that never auto-executes 4ms
 ✓ test/proposals/guardrails/negotiation-invariant.test.ts > P2-036 negotiation guardrail invariant > the payload carries advice but no committed discount/price field 1ms
 ✓ test/proposals/guardrails/negotiation-invariant.test.ts > P2-036 negotiation guardrail invariant > no proposal type exists for an AI-applied ad-hoc discount/negotiation 1ms
 ✓ test/proposals/guardrails/negotiation-guardrail.test.ts > detectNegotiationAskType > detects discount asks 1ms
 ✓ test/proposals/guardrails/negotiation-guardrail.test.ts > detectNegotiationAskType > detects scope-change asks 0ms
 ✓ test/proposals/guardrails/negotiation-guardrail.test.ts > detectNegotiationAskType > detects refund-as-leverage asks 0ms
```
(remaining 19 ✓ lines: detects owner/manager escalation asks; detects deadline/review threats; prefers the concrete lever (refund) over the generic threat; returns null when no negotiation pattern matches; recommendNegotiationResponse [returns a distinct non-empty recommendation per ask type; returns a generic recommendation for a null ask type; never recommends conceding a discount]; negotiationAskLabel labels each ask type and the null fallback; NEGOTIATION_GUARDRAIL_MARKER_REASON is a stable, greppable marker reason; customerValueTier [valued_repeat / established / new_or_unknown]; recommendNegotiationResponse with customer context [4 cases]; buildNegotiationCallbackContent customer context [2 cases] — all ✓, all passed)

0 ✗ lines — 59/59 passed.

Tenant-grade greps (all 3 files) — all **no match** on all three patterns.

---

## Row 2.12 (S) — b2b account context (structural, no test run)

```
grep -rn "b2bAccountContext" packages/api/src | grep -v test
```
→
```
src/telephony/twilio-adapter.ts:953:        session.b2bAccountContext = ctx;
src/ai/agents/customer-calling/voice-session-store.ts:259:  b2bAccountContext?: import('./b2b-account-context').B2bAccountContext;
```
→ confirms: written once at `twilio-adapter.ts:953`; the only other hit is a **type declaration** (not a reader) in `voice-session-store.ts:259`. No reader logic anywhere.

```
grep -rn "buildAccountContextPromptSection" packages/api/src | grep -v test
```
→
```
src/ai/agents/customer-calling/b2b-account-context.ts:264:export function buildAccountContextPromptSection(
```
→ **definition only** — zero production callers, confirmed.

---

## Row 3.3 — availability "defaults" labelling (in-memory supertest; mobile-only caller)

```
grep -rln -i "stale\|freshness" test --include='*.ts' | grep -v integration | head
```
→ returned unrelated files (auth.test.ts, material-item.test.ts, financing.test.ts, pg-tenant-feature-flags.test.ts, invoices/*, quality/metrics.test.ts, appointments/assignment.test.ts, estimates/*) — none about availability defaults; false positives on the word "stale"/"freshness" used in unrelated contexts.

Narrowed by content inspection to the actual file: `test/scheduling/availability-route.test.ts` (`describe('GET /api/dispatch/availability'` with a `// V18 — a cold tenant must be able to SEE that defaults are in play` comment block and tests asserting `timezoneSource`/`businessHoursSource`/`bufferSource` === `'default'`).

```
npx vitest run --reporter=verbose test/scheduling/availability-route.test.ts
```
✓ lines (all 9, verbatim):
```
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > returns 200 with { timezone, durationMin, slots[] } in ISO shape 33ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > scopes the slot query to the authenticated tenant 2ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > falls back to the default timezone when no settings repo is wired 2ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > defaults durationMin to 60 when omitted 1ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > 400s on a non-YYYY-MM-DD from 1ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > 400s on an out-of-range durationMin 1ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > scopes to one technician calendar when technicianId is supplied 1ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > flags default config sources and explains them for a cold tenant 1ms
 ✓ test/scheduling/availability-route.test.ts > GET /api/dispatch/availability > reports tenant config sources when settings carry hours and buffer 2ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  9 passed (9)
   Start at  06:25:49
   Duration  346ms (transform 79ms, setup 0ms, import 213ms, tests 46ms, environment 0ms)
```
0 failures. Uses `request(app)` — in-memory supertest, confirmed.

```
grep -rn -i "dispatch/availability\|availability.*stale\|stale.*availability" packages/web/src
```
→ (no output — **the web app has zero references to this endpoint**).

```
grep -rn -i "dispatch/availability\|availability.*stale\|stale.*availability" packages/mobile/src
```
→
```
packages/mobile/src/api/appointments.ts:7: *   - GET  /api/dispatch/availability   open slots (B1 book, B2 reschedule-pick)
packages/mobile/src/api/appointments.ts:40:/** GET /api/dispatch/availability — open slots in the tenant timezone. */
packages/mobile/src/api/appointments.ts:48:  const res = await client(`/api/dispatch/availability?${qs.toString()}`);
```
→ confirms: **only mobile calls this endpoint; the web app never does.**

Tenant-grade greps on `test/scheduling/availability-route.test.ts`:
```
-- grep1 -- no match
-- grep2 (20 hits, sample) --
26:  timezone?: string | null;
46:    opts.timezone === undefined && opts.settings === undefined
...
84:    const { app, findByDateRange } = makeApp({ timezone: 'UTC', tenantId: 'tenant-xyz' });
...
149:    expect(res.body.config.timezoneSource).toBe('default');
...
171:    expect(res.body.config.timezoneSource).toBe('tenant');
-- grep3 -- no match
```

---

## Row 3.4 — booking POST cannot take a held/out-of-hours slot (unit only)

```
grep -rln -i "hold\b|held slot|slot_hold|holds" test --include='*.ts' | grep -v integration | head
```
→
```
test/owner-loop-critical-path.test.ts
test/customers/dedup.test.ts
test/customers/customer.test.ts
test/customers/customer-group.test.ts
test/settings/autonomous-booking-settings.test.ts
test/middleware/tenant-context.test.ts
test/app/build-repositories.test.ts
test/portal/portal-booking.test.ts
test/app/wiring.test.ts
test/app/graceful-shutdown.test.ts
```
(most of these are false positives on the `\b`-only-right-anchored pattern matching words like "threshold"/"household"). Content search located the two genuinely relevant files:
- `test/scheduling/booking-availability.test.ts` — contains `describe('isWithinBusinessHours — write-side twin of slot generation'` (exact match to the PRD's "write-side twin" language for the out-of-hours POST-refusal criterion).
- `test/portal/portal-booking.test.ts` — contains `isSlotFree returns false once the slot is held` and `returns 409 with alternatives when the slot was already taken`.

```
npx vitest run --reporter=verbose test/scheduling/booking-availability.test.ts
```
✓ lines (all 22, verbatim):
```
 ✓ test/scheduling/booking-availability.test.ts > V15 — DST correctness of business-hour windows > places the 08:00 open at 12:00Z on the spring-forward day (America/New_York, 2026-03-08) 15ms
 ✓ test/scheduling/booking-availability.test.ts > V15 — DST correctness of business-hour windows > places the 08:00 open at 13:00Z on the fall-back day (America/New_York, 2026-11-01) 0ms
 ✓ test/scheduling/booking-availability.test.ts > V15 — DST correctness of business-hour windows > keeps America/Phoenix (no DST) at a fixed offset across both transition dates 1ms
 ✓ test/scheduling/booking-availability.test.ts > V15 — DST correctness of business-hour windows > control: a non-transition summer day agrees with the fixed-offset math 0ms
 ✓ test/scheduling/booking-availability.test.ts > V17 (unit) — tenant business hours propagate into slot generation > uses per-day tenant hours and treats a null day as closed 1ms
 ✓ test/scheduling/booking-availability.test.ts > V17 (unit) — tenant business hours propagate into slot generation > a changed business-hours setting changes the offered slots (change-then-observe) 0ms
 ✓ test/scheduling/booking-availability.test.ts > V17 (unit) — tenant business hours propagate into slot generation > falls back to the 08:00–17:00 default when weekly hours are empty ({} = not set) 0ms
 ✓ test/scheduling/booking-availability.test.ts > V17 (unit) — travel buffer propagates into slot generation > tenant buffer of 0 allows back-to-back slots 1ms
 ✓ test/scheduling/booking-availability.test.ts > V17 (unit) — travel buffer propagates into slot generation > tenant buffer of 60 blocks the adjacent hour on both flanks 0ms
 ✓ test/scheduling/booking-availability.test.ts > V17 (unit) — travel buffer propagates into slot generation > defaults to the 30-minute buffer when the tenant has not configured one 0ms
 ✓ test/scheduling/booking-availability.test.ts > F2 — technician working hours and time-off constrain per-tech slots > clamps windows to the tech working hours when the tech is modeled 0ms
 ✓ test/scheduling/booking-availability.test.ts > F2 — technician working hours and time-off constrain per-tech slots > falls back to business hours when the tech has no working-hours rows 0ms
 ✓ test/scheduling/booking-availability.test.ts > F2 — technician working hours and time-off constrain per-tech slots > subtracts the tech time-off blocks from offered slots 0ms
 ✓ test/scheduling/booking-availability.test.ts > isWithinBusinessHours — write-side twin of slot generation > accepts a slot inside tenant hours and rejects one outside 0ms
 ✓ test/scheduling/booking-availability.test.ts > isWithinBusinessHours — write-side twin of slot generation > rejects any slot on a closed day 0ms
 ✓ test/scheduling/booking-availability.test.ts > isWithinBusinessHours — write-side twin of slot generation > applies the 08:00–17:00 default when the tenant has no configured hours 0ms
 ✓ test/scheduling/booking-availability.test.ts > schedulingConfigFromSettings — the single settings→scheduling seam > extracts timezone, weekly hours, and buffer 0ms
 ✓ test/scheduling/booking-availability.test.ts > schedulingConfigFromSettings — the single settings→scheduling seam > degrades to nulls for a cold tenant with no settings row 0ms
 ✓ test/scheduling/booking-availability.test.ts > isSlotFree — trailing-buffer fetch horizon (Codex P1) > rejects a slot ending just before a later appointment when the buffer covers it 0ms
 ✓ test/scheduling/booking-availability.test.ts > isSlotFree — trailing-buffer fetch horizon (Codex P1) > accepts the same slot with no buffer configured 0ms
 ✓ test/scheduling/booking-availability.test.ts > Codex round 4 — window overrun + non-curated IANA zones > never emits slots past close while walking toward a later-day time-off block 0ms
 ✓ test/scheduling/booking-availability.test.ts > Codex round 4 — window overrun + non-curated IANA zones > honors a valid IANA zone outside the curated list (America/Juneau, UTC-8 in June) 0ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  22 passed (22)
   Start at  06:26:31
   Duration  197ms (transform 72ms, setup 0ms, import 98ms, tests 22ms, environment 0ms)
```
0 failures.

```
npx vitest run --reporter=verbose test/portal/portal-booking.test.ts
```
✓ lines (all 30, verbatim):
```
 ✓ test/portal/portal-booking.test.ts > booking-availability service > returns business-hours slots and excludes busy windows 15ms
 ✓ test/portal/portal-booking.test.ts > booking-availability service > clamps to business hours in winter (EST, UTC-5) 1ms
 ✓ test/portal/portal-booking.test.ts > booking-availability service > treats an expired hold as free 0ms
 ✓ test/portal/portal-booking.test.ts > booking-availability service > snaps same-day slots to the 30-minute grid when clamped to now 0ms
 ✓ test/portal/portal-booking.test.ts > booking-availability service > isSlotFree returns false once the slot is held 0ms
 ✓ test/portal/portal-booking.test.ts > clampBookingHorizon > returns the window unchanged when it is within the horizon 1ms
 ✓ test/portal/portal-booking.test.ts > clampBookingHorizon > clamps `to` down to the horizon 0ms
 ✓ test/portal/portal-booking.test.ts > clampBookingHorizon > returns null when `from` is already beyond the horizon 0ms
 ✓ test/portal/portal-booking.test.ts > clampBookingHorizon > a larger (priority) horizon admits a window the standard one rejects 0ms
 ✓ test/portal/portal-booking.test.ts > clampBookingHorizon > computes the horizon in the tenant timezone, not UTC 0ms
 ✓ test/portal/portal-booking.test.ts > GET /:token/availability > returns slots within the standard horizon for a non-member 19ms
 ✓ test/portal/portal-booking.test.ts > GET /:token/availability > rejects a malformed date 3ms
 ✓ test/portal/portal-booking.test.ts > GET /:token/availability > clamps a non-member request past the standard horizon to no slots 2ms
 ✓ test/portal/portal-booking.test.ts > GET /:token/availability > lets a priority member book past the standard horizon 3ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > creates a held appointment + create_booking proposal (pending confirmation) 4ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > publishes a dispatch-board update so the held slot appears live 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > returns 422 when the customer has no service location on file 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > returns 409 with alternatives when the slot was already taken 3ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > rejects a past slot 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > rejects a non-member slot beyond the standard horizon 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book > lets a priority member book beyond the standard horizon 4ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/cancel > emits a cancel_appointment proposal (pending confirmation) 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/cancel > refreshes the board so the change-requested badge appears live 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/cancel > refuses to cancel another customer's appointment with 404 1ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/cancel > refuses to cancel inside the cutoff window with 409 1ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/cancel > returns 409 NOT_CHANGEABLE for an already-canceled appointment 1ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/reschedule > emits a reschedule_appointment proposal for an open slot 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/appointments/:id/reschedule > returns 409 with alternatives when the requested new slot is taken 3ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book — service area > rejects a booking whose resolved location is outside the configured ZIP allowlist 2ms
 ✓ test/portal/portal-booking.test.ts > POST /:token/book — service area > accepts a booking when the location ZIP is on the allowlist 2ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  30 passed (30)
   Start at  06:26:32
   Duration  562ms (transform 247ms, setup 0ms, import 405ms, tests 80ms, environment 0ms)
```
0 failures.

Tenant-grade greps:
```
##### test/scheduling/booking-availability.test.ts #####
-- grep1 -- no match
-- grep2 -- 16 hits, all `timezone: 'America/New_York'` / `'America/Phoenix'` fixtures + 2 assertion lines (lines 37,60,75,90,105,125,139,153,162,177,201,227,254,275,299,370,372,376,383,434)
-- grep3 -- no match

##### test/portal/portal-booking.test.ts #####
-- grep1 -- no match
-- grep2 -- 12 hits, all `timezone:` fixture fields (lines 54,175,183,206,225,243,261,297,322,431,492,530,587)
-- grep3 -- no match
```

---

## Row 3.8 (structural) — appointment-confirmation dispatch row (no test run)

```
grep -rn -i "customerConfirmation\|confirmationNotifier\|NoopNotifier\|noop.*notifier" packages/api/src | grep -v test | head -20
```
→
```
src/notifications/appointment-confirmation-notifier.ts:2:  SchedulingConfirmationNotifier,
src/notifications/appointment-confirmation-notifier.ts:12:export interface AppointmentConfirmationNotifierDeps {
src/notifications/appointment-confirmation-notifier.ts:37:export class AppointmentConfirmationNotifier implements SchedulingConfirmationNotifier {
src/notifications/appointment-confirmation-notifier.ts:38:  constructor(private readonly deps: AppointmentConfirmationNotifierDeps) {}
src/notifications/transactional-comms-service.ts:7:  SchedulingConfirmationNotifier,
src/notifications/transactional-comms-service.ts:95:export class TransactionalCommsService implements SchedulingConfirmationNotifier {
src/proposals/execution/create-booking-handler.ts:5:import { SchedulingConfirmationNotifier } from './scheduling-notifications';
src/proposals/execution/create-booking-handler.ts:22:  // Awaits confirmationNotifier.enqueue — in production this is
src/proposals/execution/create-booking-handler.ts:31:    private readonly confirmationNotifier?: SchedulingConfirmationNotifier,
src/proposals/execution/create-booking-handler.ts:36:  // Also requires confirmationNotifier — without it, execute() silently skips
src/proposals/execution/create-booking-handler.ts:39:    return Boolean(this.appointmentRepo) && Boolean(this.confirmationNotifier);
src/proposals/execution/create-booking-handler.ts:123:    if (this.confirmationNotifier) {
src/proposals/execution/create-booking-handler.ts:124:      await this.confirmationNotifier.enqueue({
src/proposals/execution/handlers.ts:73:import { NoopSchedulingConfirmationNotifier, SchedulingConfirmationNotifier } from './scheduling-notifications';
src/proposals/execution/handlers.ts:370:  // Awaits confirmationNotifier.enqueue — in production this is
src/proposals/execution/handlers.ts:381:    private readonly confirmationNotifier: SchedulingConfirmationNotifier = new NoopSchedulingConfirmationNotifier(),
src/proposals/execution/handlers.ts:748:      await this.confirmationNotifier.enqueue({
src/proposals/execution/handlers.ts:1221:  schedulingNotifier?: SchedulingConfirmationNotifier;
src/proposals/execution/scheduling-notifications.ts:10:export interface SchedulingConfirmationNotifier {
src/proposals/execution/scheduling-notifications.ts:18:export class NoopSchedulingConfirmationNotifier implements SchedulingConfirmationNotifier {
```

```
grep -rn "new [A-Za-z]*ConfirmationNotifier\|new [A-Za-z]*CustomerNotifier" packages/api/src | head
```
→
```
src/proposals/execution/handlers.ts:381:    private readonly confirmationNotifier: SchedulingConfirmationNotifier = new NoopSchedulingConfirmationNotifier(),
```
→ **the only `new *Notifier` instantiation in `src` is `NoopSchedulingConfirmationNotifier` (the default parameter in handlers.ts:381).**

Follow-up check — all references to `AppointmentConfirmationNotifier` (non-test):
```
src/notifications/appointment-confirmation-notifier.ts:12:export interface AppointmentConfirmationNotifierDeps {
src/notifications/appointment-confirmation-notifier.ts:37:export class AppointmentConfirmationNotifier implements SchedulingConfirmationNotifier {
src/notifications/appointment-confirmation-notifier.ts:38:  constructor(private readonly deps: AppointmentConfirmationNotifierDeps) {}
```
→ **defined, never instantiated anywhere** (0 `new AppointmentConfirmationNotifier(` hits in `src`). Its doc-comment (lines 32–36) reads:
```
/**
 * Sends an appointment confirmation SMS/email after a create_appointment
 * proposal executes. Respects the tenant's autoSendAppointmentReminders flag.
 * Failures are best-effort — they do not block the scheduling flow.
 */
```
— i.e. the class's own doc-comment describes itself as the live send path, but it is dead code (never `new`'d). By contrast `TransactionalCommsService` (the other `SchedulingConfirmationNotifier` implementer) IS instantiated: `src/app.ts:1767: ? new TransactionalCommsService({`. Confirms PRD: "the default is a no-op notifier, and the class whose doc-comment claims to be the live path is never instantiated."

No unit-runner execution for this row (structural — the PRD's own point is that no test exists to prove the dispatch row).

---

## Row 3.9 — customer reminder (not real-DB)

```
grep -rln -i "reminder" test --include='*.ts' | grep -v integration | head -20
```
→
```
test/owner-loop-critical-path.test.ts
test/settings/reminder-offsets.test.ts
test/app/wiring.test.ts
test/invoices/dunning-config.test.ts
test/invoices/late-fee.test.ts
test/estimates/estimate-nudge.test.ts
test/estimates/estimate-revise-lock.test.ts
test/estimates/reapproval-invalidation.test.ts
test/digest/weekly-feedback-renderer.test.ts
test/ai/tasks/voice-send-payment-reminder.test.ts
test/ai/tasks/invoice-edit-task.test.ts
test/ai/tasks/voice-send-invoice.test.ts
test/ai/agents/customer-calling/inapp-entity-resolution-safety.test.ts
test/ai/agents/customer-calling/entity-resolution.test.ts
test/ai/orchestration/handler-registry.test.ts
test/ai/skills/lookup-pending-items.test.ts
test/ai/orchestration/intent-classifier.test.ts
test/db/migration-immutability.test.ts
test/workers/appointment-reminder-worker.test.ts
test/workers/overdue-invoice-worker.test.ts
```
`test/workers/appointment-reminder-worker.test.ts` is the customer-appointment-reminder file (`describe('appointment-reminder-worker'`, T-24h sweep).

```
npx vitest run --reporter=verbose test/workers/appointment-reminder-worker.test.ts
```
stderr (verbatim, both test cases logged this validation warning from appointment creation in the fixture setup):
```
stderr | test/workers/appointment-reminder-worker.test.ts > appointment-reminder-worker > sends a reminder for appointments in the T-24h window
Appointment validation warnings on create: Appointment is scheduled in the past

stderr | test/workers/appointment-reminder-worker.test.ts > appointment-reminder-worker > fires once per configured offset for the same appointment (Story 10.2)
Appointment validation warnings on create: Appointment is scheduled in the past
```
✓ lines (both, verbatim):
```
 ✓ test/workers/appointment-reminder-worker.test.ts > appointment-reminder-worker > sends a reminder for appointments in the T-24h window 6ms
 ✓ test/workers/appointment-reminder-worker.test.ts > appointment-reminder-worker > fires once per configured offset for the same appointment (Story 10.2) 1ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  06:26:58
   Duration  412ms (transform 242ms, setup 0ms, import 318ms, tests 8ms, environment 0ms)
```
0 failures. Only 2 test cases total for this worker — thin coverage relative to the reminder-sweep's importance, all against in-memory repos (no Postgres) per the PRD's framing ("the customer reminder is not [real-DB]").

Tenant-grade greps on `test/workers/appointment-reminder-worker.test.ts`:
```
-- grep1 -- no match
-- grep2 -- no match
-- grep3 -- no match
```

---

## Row 3.11 (U) — proposal expiry TTL (two coexisting regimes)

```
grep -rln -i "expiry\|expire\|ttl" test/workers test/proposals
```
→ (26 files; relevant ones: `test/workers/proposal-expiry-worker.test.ts`, `test/workers/hold-reaper-worker.test.ts`, `test/workers/estimate-expiry-worker.test.ts`, plus many `test/proposals/*` files unrelated to TTL specifically, e.g. `threshold-resolver.test.ts` (dollar-threshold, not TTL), `emergency-dispatch-hold.test.ts`). Full raw list:
```
test/workers/voice-invoice-entity-ids-end-to-end.test.ts
test/workers/google-reviews.test.ts
test/workers/hold-reaper-worker.test.ts
test/workers/failure-rate-monitor.test.ts
test/workers/dropped-call-worker.test.ts
test/workers/daily-digest-worker.test.ts
test/proposals/inbox.test.ts
test/proposals/record-refund-handler.test.ts
test/workers/proposal-expiry-worker.test.ts
test/workers/estimate-expiry-worker.test.ts
test/proposals/reject-releases-hold.test.ts
test/proposals/threshold-resolver.test.ts
test/proposals/emergency-dispatch-hold.test.ts
test/proposals/onboarding-execution-handlers.test.ts
test/proposals/proposal.test.ts
test/proposals/send-payment-reminder-handler.test.ts
test/proposals/create-booking-handler.test.ts
test/proposals/one-tap-undo.test.ts
test/proposals/lifecycle.test.ts
test/proposals/auto-approve.test.ts
test/proposals/sms/reply-handler.test.ts
test/proposals/supervisor/creation-hook.test.ts
test/proposals/autonomous-close-lane.test.ts
test/proposals/autonomous-lane.test.ts
test/proposals/prioritization.test.ts
test/proposals/actions.test.ts
```

`test/workers/proposal-expiry-worker.test.ts` (the schedule-proposal 48h sweep, `describe('runProposalExpirySweep (§5.5)'`):
```
npx vitest run --reporter=verbose test/workers/proposal-expiry-worker.test.ts
```
✓ lines (all 7, verbatim):
```
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > expires a stale schedule proposal and emits an audit event 2ms
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > expires a ready_for_review schedule proposal too 0ms
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > leaves a schedule proposal whose 48h window is still open 0ms
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > never expires a non-schedule proposal (no expiry → persists indefinitely) 0ms
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > does not expire a schedule proposal that has already been decided 0ms
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > does not expire a proposal approved between the status scan and the write 0ms
 ✓ test/workers/proposal-expiry-worker.test.ts > runProposalExpirySweep (§5.5) > isProposalExpired only fires for pending proposals past their expiry 0ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:27:09
   Duration  170ms (transform 66ms, setup 0ms, import 88ms, tests 3ms, environment 0ms)
```
0 failures.

TTL constant grep exactly as instructed:
```
grep -rn "48\|24\|4 \* 60\|TTL" packages/api/src/workers/proposal-expiry-worker.ts packages/api/src/proposals/guardrails/*.ts 2>/dev/null | grep -i -E "hour|ttl|ms" | head
```
→ **no output / no match** (this instructed grep path came up empty in both files).

Follow-up (own search) to resolve the PRD's "two TTL regimes" claim, since the instructed grep path found nothing: the 48h constant actually lives in `src/proposals/proposal.ts`, not in the worker file itself:
```
src/proposals/proposal.ts:  /** §5.5 — 48 hours, in milliseconds. */
src/proposals/proposal.ts:  export const SCHEDULE_PROPOSAL_EXPIRY_MS = 48 * 60 * 60 * 1000;
```
(covers `create_appointment`, `create_booking`, `reschedule_appointment` via `SCHEDULE_PROPOSAL_TYPES`; consumed by `defaultProposalExpiry()` and swept by `proposal-expiry-worker.ts`).

The second regime (24h default / 4h `create_appointment`) lives in a DIFFERENT directory than the row's instructed grep path assumed — `src/ai/guardrails/expiration.ts` (not `src/proposals/guardrails/`):
```
export interface ExpirationConfig {
  defaultTtlMs: number; // default 86400000 (24h)
  typeTtlMs?: Record<string, number>; // per-proposal-type TTL
}

export const DEFAULT_EXPIRATION_CONFIG: ExpirationConfig = {
  defaultTtlMs: 86400000, // 24 hours
  typeTtlMs: {
    'draft_estimate': 43200000, // 12 hours - financial proposals expire faster
    'create_appointment': 14400000, // 4 hours - time-sensitive
  },
};
```
Tested by `test/ai/guardrails-expiration.test.ts` (`describe('P2-015 — Proposal expiration and stale-context handling'`):
```
npx vitest run --reporter=verbose test/ai/guardrails-expiration.test.ts
```
✓ lines (all 9, verbatim):
```
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > happy path — non-expired proposal passes check 2ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > happy path — expired proposal detected 0ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > happy path — stale context detected via hash mismatch 0ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > happy path — fresh context passes check 0ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > happy path — batch expiration finds and expires stale proposals 1ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > validation — expired proposal cannot be executed 0ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > happy path — type-specific TTL respected 0ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > idempotency — already expired proposal not re-expired 0ms
 ✓ test/ai/guardrails-expiration.test.ts > P2-015 — Proposal expiration and stale-context handling > the expiration worker only expires ready_for_review proposals 0ms
```
Runner summary:
```
 Test Files  1 passed (1)
      Tests  9 passed (9)
   Start at  06:28:14
   Duration  185ms (transform 68ms, setup 0ms, import 92ms, tests 4ms, environment 0ms)
```
0 failures.

**Anomaly (significant):** `expireStaleProposals` / `DEFAULT_EXPIRATION_CONFIG` / any export of `src/ai/guardrails/expiration.ts` has **zero production callers** anywhere in `src` outside that file itself (`grep -rn "guardrails/expiration\|expireStaleProposals\|DEFAULT_EXPIRATION_CONFIG" src | grep -v "^src/ai/guardrails/expiration.ts"` → no output). So the "24h default / 4h create_appointment" TTL regime is fully unit-tested (9/9 passing) but **appears to be dead code, not wired into any running sweep** — the only regime actually driving production behavior is the 48h one in `proposal.ts` + `proposal-expiry-worker.ts`. Both regimes are real, coexisting source, confirming the PRD's "two TTL regimes coexist" note, but one of the two looks unreachable in production.

Tenant-grade greps:
```
##### test/workers/proposal-expiry-worker.test.ts #####
-- grep1 -- no match
-- grep2 -- no match
-- grep3 -- 34:      listTenantIds: async () => ['t1'],

##### test/ai/guardrails-expiration.test.ts #####
-- grep1 -- no match
-- grep2 -- no match
-- grep3 -- no match
```

---

## Row 3.12 (structural) — no feasibility check on booking-creation paths (no test run)

Instructed command exactly as given:
```
grep -rn "createAppointment(" packages/api/src | grep -v test | grep -v "async createAppointment\|createAppointment(" -c
```
→ `0` (this compound instruction filters out every line containing the literal substring `createAppointment(` in the second `grep -v`, so it necessarily counts to 0 — recorded verbatim as instructed, but not informative on its own).

```
grep -rn "\.createAppointment(" packages/api/src | grep -v test
```
→ (no output / no match) — because `createAppointment` in this codebase is a standalone exported function, never invoked via a `.createAppointment(` method-call pattern.

Own follow-up to actually identify the call sites (unfiltered):
```
grep -rn "createAppointment(" packages/api/src | grep -v test
```
→
```
src/seed/seed-runner.ts:141:    await createAppointment(
src/seed/operator-voice-fixture-runner.ts:486:        createAppointment(
src/appointments/appointment.ts:207:export async function createAppointment(
src/ai/scheduling/place-hold.ts:87: * createAppointment(holdPendingApproval). Never throws — a repo/validation
src/ai/scheduling/place-hold.ts:111:    const held = await createAppointment(
src/ai/voice-quality/inapp-50/world.ts:726:    const created = await createAppointment(
src/ai/voice-quality/inapp-50/world.ts:751:    const todayAppointment = await createAppointment(
src/recurring-jobs/materialize.ts:160:      const appointment = await createAppointment(
src/jobs/job-appointment-sync.ts:422:      appointment = await createAppointment(
src/jobs/from-estimate.ts:346:  let appointment = await createAppointment(
src/routes/appointments.ts:212:        const result = await createAppointment(
src/routes/public-portal.ts:764:        const held = await createAppointment(
src/routes/public-booking.ts:404:        const held = await createAppointment(
src/proposals/execution/emergency-dispatch-handler.ts:310:          const held = await createAppointment(
src/proposals/execution/handlers.ts:618:    const appointment = await createAppointment({
```
The three self-service/AI booking-creation paths named by the PRD: `src/ai/scheduling/place-hold.ts:111`, `src/routes/public-portal.ts:764`, `src/routes/public-booking.ts:404`.

```
grep -rln "checkFeasibility" packages/api/src | grep -v test
```
→
```
src/scheduling/routes.ts
src/scheduling/feasibility-types.ts
src/scheduling/feasibility.ts
src/proposals/execution/reschedule-handler.ts
src/proposals/create-scheduling.ts
src/proposals/execution/crew-handler.ts
src/proposals/execution/reassignment-handler.ts
```
→ **none of these are `place-hold.ts`, `public-portal.ts`, or `public-booking.ts`** (all dispatch-side move/reassign/reschedule files). Confirmed by direct inspection:
```
grep -n "checkFeasibility\|createAppointment" src/ai/scheduling/place-hold.ts src/routes/public-portal.ts src/routes/public-booking.ts
```
→ each file imports and calls `createAppointment` but has **zero** occurrences of `checkFeasibility`. Confirms PRD: "the three booking-creation paths call createAppointment with no feasibility check at all; only dispatch-side moves are checked."

No unit-runner execution for this row (pure structural/negative-evidence grep, per the task's row instructions).

---

## Per-row summary table

| Row | What ran | Pass/Fail | Counts |
|---|---|---|---|
| 2.2 (S) | grep+header read; unit runner attempted on `conversation-consent-ordering.test.ts` | N/A — excluded from unit config | `new Pool(` = 0; runner: "No test files found" (excluded by `vitest.config.ts`'s `test/integration/**` exclude) |
| 2.4 (S) | grep only, no test run | N/A | integration hits = 0; unit/src hits = 9 files |
| 2.5 | 4 unit files (emergency-detector, emergency-tier, emergency-tier-transitions, complaint-guardrail) | PASS | 107/107, 0 fail |
| 2.6 (U) | `vulnerability-triage-hook.test.ts` | PASS | 13/13, 0 fail |
| 2.7 (structural) | grep only, no test run (Docker file listed not run) | N/A | `setTenantFlag` callers = 0; `tenantIds` admin-ramp code confirmed present |
| 2.9 | `public-booking.route.test.ts` | PASS | 18/18, 0 fail |
| 2.11 | 3 unit files (negotiation-invariant, discount-evaluator, negotiation-guardrail) | PASS | 59/59, 0 fail |
| 2.12 (S) | grep only, no test run | N/A | `b2bAccountContext` writer=1 (twilio-adapter.ts:953), reader=0; `buildAccountContextPromptSection` callers=0 |
| 3.3 | `availability-route.test.ts` | PASS | 9/9, 0 fail |
| 3.4 | `booking-availability.test.ts` + `portal-booking.test.ts` | PASS | 22/22 + 30/30, 0 fail |
| 3.8 (structural) | grep only, no test run | N/A | `new *ConfirmationNotifier/*CustomerNotifier` = 1 hit (`NoopSchedulingConfirmationNotifier`); `AppointmentConfirmationNotifier` instantiations = 0 |
| 3.9 | `appointment-reminder-worker.test.ts` | PASS | 2/2, 0 fail |
| 3.11 (U) | `proposal-expiry-worker.test.ts` + `guardrails-expiration.test.ts` | PASS | 7/7 + 9/9, 0 fail |
| 3.12 (structural) | grep only, no test run | N/A | `checkFeasibility` present in 7 files, none of the 3 booking-creation call sites |

---

## Anomalies

1. **Row 2.2**: the file `test/integration/conversation-consent-ordering.test.ts` is DB-free (`new Pool(` count = 0, ledger write is a bare `vi.fn()`, and its own doc comment says "does not touch Postgres"), yet the unit runner (`vitest.config.ts`) hard-excludes the entire `test/integration/**` glob, so it could not be executed via the unit lane at all ("No test files found, exiting with code 1"). The file is gated purely by directory location, not by any actual DB dependency.
2. **Row 3.9**: both `appointment-reminder-worker.test.ts` cases log a stderr validation warning ("Appointment is scheduled in the past") during fixture setup — tests still pass, but the warning noise suggests the fixture creates appointments with already-past `scheduledStart` values, which is a minor fixture-quality smell worth a look even though it doesn't affect correctness here.
3. **Row 3.11**: the instructed grep for the TTL constants (`packages/api/src/workers/proposal-expiry-worker.ts packages/api/src/proposals/guardrails/*.ts`) found **nothing** — because neither the 48h constant (`proposal.ts`) nor the 24h/4h regime (`src/ai/guardrails/expiration.ts`) actually lives in the directories the row's grep pointed at. Root-caused by direct search: `SCHEDULE_PROPOSAL_EXPIRY_MS = 48 * 60 * 60 * 1000` lives in `src/proposals/proposal.ts`; the 24h/4h regime lives in `src/ai/guardrails/expiration.ts` (a different, non-`proposals/guardrails` directory). Further, that second regime's own functions (`expireStaleProposals`, `DEFAULT_EXPIRATION_CONFIG`) have **zero production callers** anywhere — it is fully unit-tested (9/9 pass) but appears to be dead/unwired code, coexisting with — but not actually competing at runtime with — the 48h regime that the worker sweep actually uses. This is a stronger finding than the PRD confirm text states ("two TTL regimes coexist") — one regime may not be reachable in production at all.
4. **Row 3.12**: the row's two instructed grep commands were both structured to be uninformative on their own (`grep -v` filters that remove every matching line, and a `.createAppointment(` pattern that doesn't match this codebase's free-function call style) — both produced 0/empty as literally executed. A follow-up unfiltered grep (`createAppointment(` non-test) was needed to actually enumerate the 15 call sites and confirm which three are the self-service/AI booking-creation paths named by the PRD (`place-hold.ts:111`, `public-portal.ts:764`, `public-booking.ts:404`) and that none of them call `checkFeasibility`.
5. **Row 2.5** grep-2 tenant-grade check on `emergency-tier.test.ts` had one incidental hit on the substring "pack" inside the word "packages" (line 109, a comment about the Docker runtime image) — not a real tenant-config match.
