# Track B — Voice reachability matrix (agent report, key findings; full verdicts merged into docs/PRD-v4-part-E-state.md §5)

- Per-🎙️ verdicts with probe outputs: 6 clean rung-5 paths; 6 blocked at approval (unconditional missingFields: voice-extended-tasks.ts:378,423,444,667,711,817); add_note approves-then-fails (targetId never set: :460-484 vs voice-extended-handlers.ts:94-99); log_expense drops job link (:1070, not in JOB_REF_INTENTS); B7.5 parts rung 0 (no intent, no unit field anywhere).
- Inverse enumerations: 3 handlers without on-ramps (create_booking, update_catalog_item, adopt_entity_alias — all intentional, contract-pinned); 0 intents without handlers; lookup_leads + lookup_catalog classify but hit the unsupported default (voice-lookup-answer.ts:675) and are silently skipped.
- Three-way drift: SUPPORTED_INTENTS 60 / map 35 / registry 38 / S1 allowlist 8 — no drift at the tested layer; both contract tests ran and PASSED (6/6, 5/5). The untested layer is drafting-task ↔ execution-handler payload contracts.
- D-014: Gather + Media Streams are one stack, two transports (same processCallerUtterance, app.ts:4425; degradation resumes same session). VAPI cannot book (webhook = telemetry only, webhooks/routes.ts:2579). Provisioning sets VoiceUrl to /api/telephony/voice (provision-twilio.ts:283) but step 4.5 (:364-410, gated on VAPI_API_KEY) imports the number into Vapi with repo-invisible effect. Undeterminable without (a) VAPI_API_KEY set-ness in prod, (b) live number VoiceUrl.
- Voice approval refusal: enforced at voice-action-router.ts:1331-1339 and routes/assistant.ts:1058-1090 (audited); owner-telephony-only path hard-gated on ownerSession (create-voice-turn-processor.ts:2115-2127,2196-2205); contract-pinned.
- Watchlist: add_equipment absent (zero hits); assign_technician absent as distinct intent and reassign is itself voice-blocked; correction-by-voice has no on-ramp (redraft factory is the one-tap picker only, resolve-entity.ts:418-437; edit_proposal refused on both non-telephony voice surfaces).
- Method note: read-only tsx probes of real task/execution handlers (scratchpad-hosted, no repo writes/DB/network) produced the observed outputs quoted in the state table.
