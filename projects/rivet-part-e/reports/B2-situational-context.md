# B2 — Situational context (agent report, verbatim)

## Rows

| Req | Rung | Evidence | Missing link |
|---|---|---|---|
| B2.1 | 2 | Territory model exists: `tenant_settings.service_area_zips` (`packages/api/src/db/schema.ts:3809`), `service_area_text`/`service_area_radius` (`schema.ts:2616-2617`); written from onboarding (`packages/api/src/routes/onboarding.ts:224-285`, registered route). `checkServiceArea()` (`packages/api/src/scheduling/service-area.ts:23-35`) is wired into `packages/api/src/routes/public-booking.ts:316` and `packages/api/src/routes/public-portal.ts:704-707`. | Territory is consulted only on structured-ZIP web-booking paths, never in voice/spoken address interpretation — not the "outermost prior on every location interpretation" the requirement names. No caller in `create-appointment-task.ts`, `create-customer-handler.ts`, or the entity resolver reads `service_area_*`. |
| B2.2 | 0 | No code implements a 4-level (territory → speaker location → customer addresses → recent jobs) bias order anywhere. Grep for `speaker.*location`, ordered candidate lists, or a bias/priority function over address sources returns nothing in `packages/api/src`. | Whole bias-order mechanism absent. |
| B2.3 | 0 | The only spoken-address code is `packages/shared/src/contracts/spoken-address.ts:134` `parseSpokenAddressParts` — a pure regex splitter (street/city/state/zip from trailing tokens). It does not query any candidate address set, does not use territory or proximity, and cannot resolve `"Elm Street"` to a specific known record — a partial address just yields `{street1}` and stalls as a review-card gap. | No candidate matching or proximity tie-break exists; this is text extraction, not resolution. |
| B2.4 | 2 | Structured-ZIP rejection exists and is non-silent for web-booking: `public-portal.ts:704-710` and `public-booking.ts:316-319` return `400 OUT_OF_SERVICE_AREA` when `checkServiceArea(...).inArea` is false. | That rejection is an HTTP error on a typed web form, not "a clarification or a confidence marker" on a spoken/AI-drafted address — and no equivalent check exists anywhere on the voice/AI path (no address resolver consults territory at all). |
| B2.5 | 0 | No geocoding code exists in the repository. `grep -rli "geocod" packages/api packages/web packages/shared` returns zero source hits (only stale references in `docs/archive/**`). `service_locations.latitude`/`longitude` (`packages/api/src/locations/location.ts:33-34,55-56,70-71`) are nullable, caller-supplied fields nobody populates automatically — the travel-time provider (`GoogleDistanceMatrixProvider`, `packages/api/src/scheduling/travel-time/google-provider.ts`) *consumes* lat/lng but nothing produces it from an address. | Geocoding is entirely absent — this contradicts the watchlist seed's premise that it "exists and is wired." Nothing geocodes or validates an address before it lands on a job/appointment. |
| B2.6 | 2 | The `voice_clarification` contract is real and broadly wired (`packages/api/src/proposals/voice-clarification.ts`, consumed across `workers/voice-action-router.ts`, `proposals/resolve-entity.ts`, `ai/resolution/entity-resolver.ts` ambiguous-kind result) for `customer`/`job`/`appointment`/`invoice`/`estimate`/`technician`/`pending_proposal`. | Because no `place`/address `EntityKind` exists, an ambiguous address can never enter this contract — it structurally cannot produce a `voice_clarification`; it falls to the separate incomplete-address "note"/gap path (`create-appointment-task.ts:417-468`) which asks a human to fill missing fields, not "which Elm Street." |
| B2.7 | 5 | `packages/api/src/ai/scheduling/resolve-datetime.ts` — chrono-node parses relative to tenant-local "now" (luxon `DateTime.fromJSDate(now).setZone(timezone)`, lines 171-180), converts wall-clock → UTC (`wallToUtc`, lines 123-131), rejects bare-date ambiguity (`ambiguous_no_time`, line 201) instead of guessing. Wired into `create-appointment-task.ts:562` (gated on a *required*, non-default tenant timezone, lines 514-527 — "NO DEFAULT TIMEZONE" note) and `voice-extended-tasks.ts:283` (reschedule). Both `create_appointment` and `reschedule_appointment` are in `SUPPORTED_INTENTS` (`intent-classifier.ts:157,165`) and `voice-intent-map.ts:57,65` → voice-reachable end to end. Unit test passes: `cd packages/api && npx vitest run test/ai/scheduling/resolve-datetime.test.ts` → 13/13 passed. | — |
| B2.8 | 2 | Role-based gating exists and fails closed: `packages/api/src/workers/voice-lookup-answer.ts:88-93` `OWNER_GRADE_LOOKUP_INTENTS` (`lookup_revenue`, `lookup_job_profit`, `lookup_pending_items`, `lookup_digest`) is checked at `voice-lookup-answer.ts:263-274` via `hasPermission(role, 'reports:view')`, refusing technicians. `resolveMemberRole` resolves the DB-authoritative role per actor. | The one "my schedule" analog, `lookup_day_overview` (`packages/api/src/ai/skills/lookup-day-overview.ts`), is explicitly documented "Owner/operator-scoped" (line 4) yet is **not** in `OWNER_GRADE_LOOKUP_INTENTS` — any caller, technician included, gets the identical tenant-wide answer with zero filtering by `assignedTechnicianId`/actor. The system gates *access* by role but does not reshape *content* — "my schedule" never means "my jobs only" for a technician anywhere in the codebase. |
| B2.9 | 5 | Tenant-scoped SQL confirmed by direct citation, e.g. technician-name resolution: `packages/api/src/ai/resolution/pg-entity-resolver.ts:459-472` — `SELECT id, ... FROM users WHERE tenant_id = $1 AND role IN ('technician','dispatcher','owner') AND deleted_at IS NULL AND similarity(...) > $3`, executed inside `withTenantConnection(this.pool, tenantId, ...)`. Same pattern for customer/job/invoice/appointment kinds (`pg-entity-resolver.ts:101,137,180,300,352,427`). Reachable via voice (entity resolver is invoked from `voice-action-router.ts` for every intent with a free-text reference). | — |
| B2.10 | 0 | No cross-street/landmark resolution exists anywhere. The only inbound-call identity flow, `handleCreateCustomerVoiceIntent` (`packages/api/src/telephony/twilio-adapter.ts:2977-3100`), builds a `create_customer` proposal from name + caller-ID phone only — no address/location handling at all; `resolveSpokenAddress`/`parseSpokenAddressParts` are used by `create-appointment-task.ts`, `create-customer-handler.ts`, `proposals/contracts.ts`, and `routes/assistant.ts` — never by `telephony/twilio-adapter.ts`. | No location context of any kind is threaded into the inbound-call agent's address handling. |

## Watchlist re-verification

- **Entity resolver kinds — confirmed CURRENT set is `customer | job | appointment | invoice | estimate | pending_proposal | technician`**, `packages/api/src/ai/resolution/entity-resolver.ts:23-36`. The `technician` kind is real and wired at `packages/api/src/ai/resolution/pg-entity-resolver.ts:477`, querying `users` tenant-scoped by role. **Still no `place`/address kind** — confirmed by exhaustive read of the `EntityKind` union (7 members, no address variant), corroborated by `reference-candidates.ts:37-38`.
- **Geocoding — the seed's premise is FALSE as stated.** Zero source hits for geocoding; no client, no call site. Only the **travel-time** piece is real (`packages/api/src/scheduling/travel-time/{google-provider,haversine-fallback}.ts`), both requiring lat/lng as *input*, never producing it. `service_locations.latitude/longitude` columns exist but nothing populates them.

## Deltas

- **Doc ahead of code**: PRD-v4's own Part-E note (line 262) states geocoding "exist[s] and [is] wired" — false; no geocoding code exists in the repository.
- **Reality ahead of documentation**: the "spoken addresses vanished without an error" bug has a concrete, tested remediation shipped: `packages/shared/src/contracts/spoken-address.ts` (`resolveSpokenAddress`) plus `detectServiceLocationGap`/`extractPreservedAddress` in `create-appointment-task.ts:417-483` and `unstructuredAddressNote` in `packages/api/src/proposals/execution/create-customer-handler.ts:67`. An incomplete spoken address is preserved verbatim and surfaced as a blocking `missingFields` gap (never silently dropped). Solves the *symptom* (silent loss), not the *substance* of B2.1–B2.5.
- **Role model mismatch (bears on B2.8/B1.4)**: B1.4 specifies `owner · admin · dispatcher · technician`, but `packages/api/src/auth/rbac.ts:1` defines `Role = 'owner' | 'dispatcher' | 'technician'` — no `admin` role exists.

## Judgment calls

- B2.1 at rung 2, not 3: the wired code serves only two structured-ZIP web-form routes, not the interpretation surfaces B2 names.
- B2.6 at rung 2: the requirement extends the clarification contract to addresses; no address kind exists, so no proof transfers.
- B2.9 cited by SQL literal (no test claim made).
- `lookup_day_overview` absence from `OWNER_GRADE_LOOKUP_INTENTS` treated as a substantive B2.8 gap (code comment says "Owner/operator-scoped", behavior is unrestricted).
- B2.10 scored 0: `resolveSpokenAddress` machinery is never invoked from `telephony/twilio-adapter.ts` (grep zero matches).
